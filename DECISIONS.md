# DECISIONS.md — Host Capability Substrate


Human-readable decision ledger. Complementary to ADRs. Agents check this before reopening settled questions.

- **Pending** are unresolved questions blocking a decision.
- **Accepted** are settled; changing them requires an amendment ADR.
- **Reversed** are decisions that were later changed; keep the history.

Research plan (system-config source): `~/Organizations/jefahnierocks/system-config/docs/host-capability-substrate-research-plan.md` (v0.3.0+ resolved all v0.2.0 open items).

## Pending

| ID | Question | Owner | Due before | Status |
|----|----------|-------|------------|--------|
| Q-003 | Shared-state / coordination-store scope, sequencing, and entity taxonomy. The 2026-04-24 coordination-lessons brief (`docs/host-capability-substrate/research/external/2026-04-24-coordination-lessons.md`) proposes a three-layer architecture (authoritative operational store — existing SQLite WAL — plus a new coordination state layer and a new retrieval/RAG index) with four new Ring-0 entities (`KnowledgeSource`, `KnowledgeChunk`, `CoordinationFact`, `DerivedSummary`), a promotion workflow (agent-proposes / verifier-promotes), and ADR 0019 + D-033 as the eventual landing. The brief is highly aligned with existing HCS posture (charter inv. 1/2/5/7/8/10, D-025/D-026, ADR 0004/0010/0011); the question is not whether to pursue the direction but how to sequence it and how to reconcile the new entities with existing `Evidence`. Five sub-decisions: **(a)** does the coordination layer land within Milestones 1–6 or post-Phase-1? **(b)** how do new entities relate to existing `Evidence` — peers or specializations/subtypes? **(c)** is `allowed_for_gate` a first-class schema boolean or derived from `authority` / `confidence`? **(d)** is "agent proposes / verifier promotes" a parallel track to the approval-grant pattern (M2) or reuse with different target? **(e)** does the "RAG may discover; only typed evidence may decide" rule warrant a new charter invariant 18 in v1.3.0, or does it remain a strong guideline under existing inv. 8? ADR 0019 should also account for ADR 0022's envelope-wraps-payload precedent if ADR 0022 is accepted; any departure should be explicit. Resolution target: ADR 0019 drafting window in Phase 1 synthesis (2026-05-07 → 2026-05-08) or later; D-033 batch commit on a post-Phase-1 decisions sweep (NOT the W3 closeout batch). | user | Phase 1 synthesis (2026-05-08) or later | **Q-003 accepted via ADR 0019 (revision 3, accepted 2026-05-03)** after a three-revision review cycle (v1 returned 8 blocking findings — 1 ontology, 3 policy, 4 security; v2 closed all 8 blockers and folded 12 non-blocking observations but introduced 2 new security blockers — re-indexing label-recheck gap, label-upgrade chunk-invalidation gap; v3 closed both new blockers and folded 4 additional non-blocking security observations + ontology NB-1; all four reviewers returned READY-FOR-ACCEPTANCE on v3 with no mechanical tweaks at acceptance). Five Q-003 sub-decisions settled per pre-draft human review (2026-05-03): **(a)** ADR posture commitment in Phase 1; schema implementation deferred to Milestone 4 or later; posture unblocks Q-008(d). **(b)** Four new entities are peers of `Evidence` (Q-011 bucket 2: standalone Ring 0 entities), not subtypes; they reference `Evidence` via `evidenceRefSchema`. **(c)** `allowed_for_gate` is a first-class kernel-set boolean per registry v0.3.2; producer-supplied values rejected at Layer 1 mint API; starts false; flips true only via the promotion workflow. **(d)** Promotion workflow is parallel track to `ApprovalGrant` in entity terms (different scope shape: facts vs operations); shared in infrastructure terms (typed Decision + audit chain + Ring 1 mint API). **(e)** Charter inv. 18 candidate ("derived retrieval results never decision authority") queued for charter v1.3.x or v1.4.0; amendment lands per change-policy in separate PR. Three-layer architecture: Layer 1 authoritative operational store (existing SQLite WAL, Ring 1 service, ADR 0004); Layer 2 coordination state layer (NEW; Ring 0 entities + Ring 1 mint/broker/gateway); Layer 3 derived retrieval index (NEW; Ring 1 indexer service against Ring 0 records; MCP/dashboard exposure Ring 2). Single-host posture per charter inv. 10; runtime state under `~/Library/Application Support/host-capability-substrate/`; hosted vector stores rejected. Four new Ring 0 no-suffix peer entities: `KnowledgeSource` (with `source_kind` + `security_label` discriminators; security_label propagates to chunks; re-indexing recomputes label on `content_hash` change; label upgrade to `secret_referenced` purges child chunks with non-null `embedding_ref`); `KnowledgeChunk` (with `text_hash` over reference-form content only; `chunk_kind` discriminator; `embedding_ref` null when parent is `secret_referenced`; deployment-bound to on-host `$HCS_STATE_DIR`; chunks display-only forbidding all paths to typed records consumed by gates including prompt-rendering); `CoordinationFact` (with `subject_kind` + `subject_ref` polymorphic FK; `predicate_kind` ontology-controlled vocabulary reserved for follow-up registry section `§Predicate-kind vocabulary` paralleling `§Boundary dimension registry`; `object_kind` discriminator + sibling `object`; `evidence_refs` array; kernel-set `allowed_for_gate` / `promoted_at` / `promotion_grant_id`; producer-asserted `valid_until` kernel-shortenable at promotion time per canonical policy); `DerivedSummary` (with `derived_from` array; `summary_kind` discriminator; `authority: derived` fixed; `confidence: best-effort` fixed; chain-promotion rule forbids promotion when `derived_from` graph contains any unpromoted record, sandbox-observation/self-asserted Evidence, or KnowledgeChunk reference). Promotion workflow shape: agent authors candidate (`allowed_for_gate: false`) → verifier inspects evidence chain (rejecting sandbox-observation / self-asserted authority per inv. 8) → verifier mints typed promotion grant (entity name reserved; canonical policy YAML rejects all three candidate names until selected) binding candidate evidence_id + verifier `agent_client_id` + `session_id` (per registry v0.3.1 canonical attribution; producer-supplied values rejected) + `verification_evidence_refs` → Ring 1 mint API consumes grant and flips `allowed_for_gate: false → true` with seven-field audit Decision (`agent_client_id`, `session_id`, `promotion_layer`, `candidate_evidence_id`, `verification_evidence_refs`, `promotion_grant_id`, `promoted_at`). §Verifier visibility-authority rule: minting-session-only read authority over `evidence_refs` and transitive `derived_from` graph (closes verifier-session-aggregation escalation surface); rejection class `coordination_promotion_visibility_unauthorized`. §Closed-list fail-mode mirrors ADR 0029 v2 (block for destructive_git/external_control_plane_mutation/worktree_mutation/merge_or_push; warn for read_only_diagnostic/agent_internal_state). Cross-context binding strict default: cross-context Evidence references rejected at Layer 1 unless future Q-003 amendment commits explicit typed mechanism. Eleven new `Decision.reason_kind` rejection-class names reserved (posture-only): `coordination_fact_unpromoted`, `coordination_fact_expired`, `coordination_fact_evidence_drift`, `coordination_promotion_visibility_unauthorized`, `derived_summary_unpromoted`, `derived_summary_unpromoted_dependency`, `knowledge_chunk_used_as_gate_authority`, `knowledge_source_content_drift`, `promotion_grant_scope_mismatch`, `secret_resolution_in_chunk` (plus placeholders `coordination_fact_unrecognized_failure` / `derived_summary_unrecognized_failure` from §Closed-list fail-mode). Two new `Decision.required_grant_kind` reservations: `coordination_promotion`, `summary_promotion` (scope keys disjoint from `ApprovalGrant.scope` per-class extension keys per ADR 0029 v2). Naming discipline binding: "evidence and coordination store," "shared state," "knowledge index," "coordination fact," "derived summary." Never "memory" / "agent memory" / "shared memory" / "LLM memory" / "persistent memory" / "long-term memory" / "context memory." Charter inv. 18 candidate text tightened in v2 to forbid promotion-laundering via unpromoted `derived_from` chains. Eight forward-looking acceptance notes deferred to schema PR / Milestone 2 / regression corpus / future ADRs (no ADR-level mechanical tweaks were applied at acceptance): graph-walk Ring 1 service-design fusion of chain-promotion + visibility-authority; kernel-side stale-marking + purge transitions; `chunk_state` discriminator review if materialized; canonical policy at Milestone 2 (producer-equals-verifier separation-of-duties enforcement; verifier-class privileges per fact-class; re-index frequency bounds; verifier-class for manual reclassification; retention/observability for purged chunks; canonical names for placeholder rejection classes); `§Predicate-kind vocabulary` registry update PR as schema-PR precondition; regression corpus expansion (re-index attack patterns, concurrent retrieval during purge, cross-session verifier identity, downgrade asymmetry, multi-entity chain promotion, prompt-rendering escape attempts); future ADRs (`CoordinationFact` ↔ `Lease` interaction-edge composition, `DerivedSummary` recursion depth + cycle detection, multi-host Postgres/pgvector, `provider_observed_via` chain-of-trust via `credential_source_evidence_ref`, embedding model commitment, dashboard views, MCP surface); charter inv. 18 amendment per change-policy in separate charter-edit PR. **Q-008(d) worktree-ownership composition is unblocked at the posture layer** (CoordinationFact shape committed; Q-008(d) can compose against it). D-033 ("HCS shared memory is typed evidence + coordination state + derived retrieval index, not agent memory") naming-discipline candidate is now embedded in this row's acceptance and AGENTS.md update queue; explicit decision-ledger D-033 entry deferred to next decisions-sweep PR (NOT the W3 closeout batch). No `KnowledgeSource` / `KnowledgeChunk` / `CoordinationFact` / `DerivedSummary` / promotion-grant Zod source, schema enum extensions, embedding storage shape, dashboard route, MCP adapter contract, charter invariant text, canonical policy YAML, multi-host coordination-store deployment, Q-008(d) worktree-ownership composition rules, regression-trap fixtures, or runtime behavior is accepted by this row. |
| Q-005 | CI runner compatibility boundary and evidence model. The 2026-04-26 proposed runner architecture report (`docs/host-capability-substrate/research/external/2026-04-26-proposed-runner-architecture.md`) recommends Proxmox-first/Linux-first/GitHub-orchestrated self-hosted runners, hosted smoke sentinels, Citadel-owned OpenTofu/PaC, manual-only MacBook runner use, and HCS as typed evidence consumer rather than CI control plane. Sub-decisions: **(a)** should runner/check facts land as `Evidence` subtypes or standalone Ring-0 entities (`RunnerHostObservation`, `RunnerIsolationObservation`, `WorkflowRunReceipt`, `CleanRoomSmokeReceipt`, `ResourceBudgetObservation`, `PolicyPlanReceipt`)? **(b)** which policy rules live in Citadel OPA versus HCS policy/gateway? **(c)** should public fork -> self-hosted runner, generic `runs-on: self-hosted`, MacBook always-on CI, runner tokens in OpenTofu state, and Docker socket exposure become HCS forbidden families or remain Citadel PaC-only? **(d)** should HCS model GitHub status source/app identity as gate evidence before consuming self-hosted check results? | user | Phase 1 ResourceBudget / external-control-plane synthesis | **Q-005 accepted via ADR 0032 (revision 2, accepted 2026-05-03)** after a two-revision review cycle (v1 returned 6 blocking findings — 2 ontology, 4 security; v2 closed all 6 with no new blockers; all four reviewers returned READY-FOR-ACCEPTANCE on v2; 22 non-blocking observations folded total — 16 from v1 + 6 from v2 re-review; five mechanical tweaks at acceptance). Four sub-decisions settled per pre-draft research-grounded human review (2026-05-03): (a) all six runner/check candidates land as Evidence subtypes (Q-011 bucket 1) matching ADR 0027 v2 / ADR 0030 v2 stage-1+2 pattern: `RunnerHostObservation` (evidenceSchema-direct, per-host-id grain, substrate_kind discriminator), `RunnerIsolationObservation` (BoundaryObservation envelope for the existing `runner_isolation` boundary dimension; promotes from proposed → accepted with this ADR; flat-field composition with five independent payload fields including `docker_socket_exposure_kind` 4-state discriminator), `WorkflowRunReceipt` (evidenceSchema-direct, per-(repository_id, workflow_run_id) grain, conclusion_kind discriminator, preserves run-vs-check distinction from Q-006 candidate `CheckRunReceipt`), `CleanRoomSmokeReceipt` (bucket 1 receipt; promotion to proof composite reserved for follow-up), `ResourceBudgetObservation` (evidenceSchema-direct, per-host per-time-window grain, **feeds existing accepted ResourceBudget entity** per Q-011 dedupe rule — does NOT duplicate as standalone), `PolicyPlanReceipt` (bucket 1 receipt with §Secret-bearing content rule per inv. 5: opentofu_plan_hash over redacted plan output, redaction_mode discipline, scrubber-eligibility for policy_ids and workflow_path, no raw plan content); (b) two-side Citadel-vs-HCS policy boundary preserves charter inv. 1 (no policy duplication) + inv. 16 (external-control-plane evidence-first) — Citadel OPA owns runner provisioning (OpenTofu plans + state lifecycle), runner group assignments by repo trust class, network egress at runner host level, runner-registration-token lifecycle (ephemeral, no state storage), workflow-YAML required-check shapes, action version pinning; HCS policy/gateway owns per-operation evidence consumption, mutation_scope per inv. 7, approval-grant binding, audit, gate consumption of self-hosted check results, forbidden-family enforcement at the operation boundary; HCS does NOT become a parallel CI control plane; (c) all five HCS forbidden runner families enforced at the operation boundary in addition to Citadel PaC workflow-level blocks (non-escalable per inv. 6; flattened to lower_snake_case per registry §Sub-rule 9): `forbidden_runner_public_fork_to_self_hosted` (inv. 8 + inv. 16), `forbidden_runner_generic_self_hosted_label` (inv. 17), `forbidden_runner_macbook_always_on_ci` (inv. 15; cross-context binding requires human-driven dispatching session, with interim total-block until `ExecutionContext.actor_kind` schema commits), `forbidden_runner_tokens_in_state` (inv. 5 + inv. 13; enforcement at policy-lint / capability-registration layer, not per-invocation gateway), `forbidden_runner_docker_socket_to_untrusted` (inv. 17). Forbidden-family rejection layer (registration / operation-shape) explicitly distinguished from ADR 0029 v2 §`block` matrix layer (receipt-consumption per-invocation); both non-escalable but for different structural reasons. (d) `StatusCheckSourceObservation` required before HCS consumes self-hosted check results as gate evidence — required fields: commit_sha matching target branch tip, check_name + (expected_github_app_id OR expected_workflow_path) binding, conclusion_kind + concluded_at + valid_until freshness window, source_kind discriminator, pair with WorkflowPolicyObservation evidence_ref. Check-name-only consumption rejected with `Decision.reason_kind: status_check_source_required` (closes GitHub-App-impersonation attack surface). Q-005 names the requirement; Q-006 stage-2/3 owns the receipt's full Zod shape. **Interim total-block rule**: until Q-006 stage-2/3 ADR commits the receipt shape, ALL self-hosted check-result consumption rejects unconditionally per inv. 16. Six new evidence subject_kind enum values reserved: runner_host, runner_isolation, workflow_run, clean_room_smoke, resource_budget, policy_plan. Seven new Decision.reason_kind reservations (posture-only): runner_isolation_unverified, runner_substrate_forbidden (audit-chain rejection-class discriminator at rejecting layer for forbidden-family per-invocation rejections, not Decision-level matrix state), status_check_source_required, workflow_run_evidence_drift, policy_plan_outcome_failed, runner_observation_stale_post_deregistration (closes Citadel runner-token rotation audit-integrity gap), runner_capability_registration_forbidden (registration-layer rejection class for structural forbidden-runner patterns; distinct attribution from gateway-layer rejection). Plus shared `secret_resolution_in_chunk` rejection class (with ADR 0019 v3 KnowledgeChunk surface; surface-of-origin disambiguated via co-recorded evidence_subject_kind). One new Decision.required_grant_kind reservation pending: ApprovalGrant.scope per-class extension for `runner_registration` / `runner_deregistration` operation classes binds target_ref: { runner_host_id } + execution_context_id; scope-key disjointness preserved per ADR 0019 v3 / ADR 0031 v1. Authority discipline per registry v0.3.2: `runner_host_id` is **producer-asserted, kernel-verifiable** (Citadel mints; HCS consumes; HCS does NOT mint identifiers in Citadel's authority domain); `last_seen_at` kernel-set freshness anchor; `labels` and `workflow_path` declared scrubber-eligible per registry v0.3.0 §Field-level scrubber rule. New `runner_deregistration` event Decision class (acquire / release / expiry / force-break parallels): Citadel-signaled rotation/force-deregistration emits typed Decision in audit hash chain per registry v0.3.1 (extended to lifecycle events by inheritance); Layer 2/3 re-check invalidates stale RunnerHostObservation records via `registration_epoch` mismatch. Cross-context binding rules per Ring 1 layer explicit per registry v0.3.0. `runner_isolation` boundary dimension promotes from proposed → accepted (registry update PR queued post-acceptance). Out of scope: Zod schema source for the six entities; canonical policy YAML; Citadel OPA policy bundles; StatusCheckSourceObservation receipt shape (Q-006 stage-2/3 territory); CleanRoomSmokeReceipt / PolicyPlanReceipt proof-composite promotion; `ExecutionContext.actor_kind` schema commitment (follow-up Q-* candidate); charter invariant text; Q-007 (b)-(f) sub-decisions; stage-2 runner/check receipts; multi-runner-host CoordinationFact composition. **Q-007(b) (QualityGate deferral cadence) is partially unblocked**: Q-005 settles; Q-006(g) check-result gateability remains (under Q-006 b-g cycle). |
| Q-006 | GitHub/version-control authority model. The 2026-04-29 local investigation (`docs/host-capability-substrate/research/local/2026-04-29-github-version-control-agentic-surface.md`) shows GitHub is multiple authority planes on this host: SSH transport aliases, Git authorship/signing config, `gh` keyring sessions, GitHub MCP PAT/OAuth/Copilot auth, per-workspace env overrides, repo settings/rulesets/Actions, check status sources, and local worktree/remote state. The 2026-05-01 consult synthesis (`docs/host-capability-substrate/research/local/2026-05-01-version-control-authority-consult-synthesis.md`) adds source-control continuity, expected check source identity, Actions posture, typed branch deletion proof, and split GitHub credential authority as explicit design inputs. Proposed ADR 0020 (`docs/host-capability-substrate/adr/0020-version-control-authority.md`) records the evidence-subtype-first path for human review. Sub-decisions: **(a)** should Git/GitHub facts land as standalone Ring-0 entities or `Evidence` subtypes (`GitRemoteObservation`, `GitConfigResolution`, `GitIdentityBinding`, `GitHubRepositorySettingsObservation`, `BranchProtectionObservation`, `WorkflowPolicyObservation`, `CheckRunReceipt`, `StatusCheckSourceObservation`, `SourceControlContinuityReceipt`)? **(b)** which GitHub mutations, if any, may use human PAT/`gh` authority versus requiring GitHub App/OIDC or brokered credentials? **(c)** what branch/ruleset baseline should protect HCS and `system-config` before agentic push/merge workflows become normal? **(d)** how should HCS reconcile local path, remote owner, SSH alias, signing principal, and MCP/`gh` credential identity? **(e)** should GitHub MCP read and mutation authority be split into separate credentials/toolsets? **(f)** what minimum `BranchDeletionProof` is required before local or remote branch cleanup? **(g)** when is a check result gateable: name only, or name plus expected source app/integration, commit SHA, workflow path, and freshness? | user | Phase 1 external-control-plane / schema synthesis | Approved in limited posture — ADR 0020 accepted. Q-006 starts evidence-subtype/receipt-first and commits only five near-term review names: `GitConfigResolution`, `GitIdentityBinding`, `BranchDeletionProof`, `StatusCheckSourceObservation`, and `SourceControlContinuityReceipt`. Broader receipt/entity inventory remains deferred to Q-011-guided ontology review. **Stage-1 accepted via ADR 0027 (revision 2, accepted 2026-05-02)**: three foundational receipts — `GitRepositoryObservation`, `GitRemoteObservation`, `BranchProtectionObservation`. The first two use `evidenceSchema`-direct typed payloads (Q-011 bucket 1); `BranchProtectionObservation` is a `BoundaryObservation` payload for a new `branch_protection` dimension (registry/enum update lands with schema implementation, not with this ADR). `GitRemoteObservation` adopts per-(repository, remote_name, ref) grain to close the ScopeCam motivating failure typed-evidence-first. **Stage-2 accepted via ADR 0030 (revision 2, accepted 2026-05-03)** after a two-revision review cycle (v1 returned 2 blocking findings from ontology — provider_id violated `<entity>_id` reservation, vacuous_evidence ↔ empty_branch_evidence_refs naming gap with ADR 0025 v2 — plus 17 non-blocking concerns; v2 closed both blockers and folded ten consolidated non-blocking observations; all four reviewers returned READY-FOR-ACCEPTANCE on v2; two cosmetic mechanical tweaks at acceptance — revision-history self-loop fix + stray provider_id in §Out of scope renamed). Six receipts: `GitWorktreeObservation` (single-worktree state with kernel-set lease_id / owning_session_id / last_lease_check_at; composition rules deferred to Q-008(d)), `GitWorktreeInventoryObservation` (per-(repository_id, branch_ref) grain; cross-execution_context_id worktree references rejected at Layer 1), `GitBranchAncestryObservation` (single observation with internal `ancestry_kind: ancestry | patch_equivalence | vacuous` discriminator + sibling proof fields `ancestry_evidence` / `patch_equivalence_evidence` / `empty_branch_evidence` matching ADR 0025 v2 `merge_proof_kind` discriminator and `empty_branch_evidence_refs` consumption-side naming), `GitDirtyStateObservation` (per-worktree grain with `dirty_state_kind` discriminator), `PullRequestReceipt` (`pr_state_kind: open | merged | closed_unmerged` + `provider_kind: "github"` discriminator; PR titles/bodies/review-comments out of scope at schema boundary), `PullRequestAbsenceReceipt` (positive-absence proof with `absence_observed_at` freshness anchor; Layer 3 gateway re-derive enforces freshness binding; cross-execution_context_id reuse rejected at Layer 1). All six are `evidenceSchema`-direct typed payloads (Q-011 bucket 1) matching ADR 0027 stage-1 pattern. Authority discipline codifies a three-way naming convention: `observed_via` (local-Git), `provider_observed_via` (provider state), `query_observed_via` (provider query/absence) — all kernel-set per registry v0.3.2; future receipts in this family pick one of the three. Per-receipt cross-context binding rules explicitly assigned to Ring 1 layers (mint API / broker FSM / gateway) per registry v0.3.0 requirement. `worktree_path` declared scrubber-eligible per registry v0.3.0 §Field-level scrubber rule. Six new `Decision.reason_kind` rejection-class names reserved (posture-only): `worktree_attachment_drift`, `worktree_inventory_partial`, `ancestry_proof_invalid`, `dirty_state_blocks_destructive_op`, `pr_state_drift`, `pr_absence_stale` — all *Decision-level* per ADR 0029 v2 §`block` vs forbidden-tier framing (do not promote operation to forbidden tier). Two new `Decision.required_grant_kind` reservations (posture-only): `worktree_clean_acknowledgment` (binds `destructive_git` per-class extension + `worktree_path` for per-worktree grain; non-transferable across worktrees in same repo); `pr_absence_acknowledgment` (binds the specific stale `evidence_id`; non-transferable across different absence receipts; fresh absence at gateway re-derive invalidates grant). Six receipt subject-kind enum values reserved (`git_worktree`, `git_worktree_inventory`, `git_branch_ancestry`, `git_dirty_state`, `pull_request`, `pull_request_absence`) per registry Sub-rule 7. Five forward-looking acceptance notes deferred to downstream work: (i) registry update PR adding the six subject-kind enum values; (ii) reopen worktree-ownership composition rules under Q-008(d) once Q-003 settles; (iii) schema PR enumerating `Decision.reason_kind` and amending ADR 0029 v2's acceptance forward-look #3 to include the six new values; (iv) future amendment for `provider_observed_via` chain-of-trust via `credential_source_evidence_ref`; (v) regression corpus expansion with multi-worktree non-transferability, cross-context inventory, stale-absence at gateway, scrubber smoke, content-leak schema-boundary rejection tests. Schema implementation lands per `.agents/skills/hcs-schema-change` after acceptance and unblocks ADR 0025 v2 BranchDeletionProof Zod source. ADR 0026 substrate hook architecture remains gated on stage-1's `BranchProtectionObservation` schema, not on stage-2. **Sub-decisions (b)-(g) accepted via ADR 0033 (revision 2, accepted 2026-05-03)** after a two-revision review cycle (v1 returned 8 blocking findings — 1 architect Q-011 bucket-2 mislabel, 3 policy escalation surfaces, 4 security blockers; v2 closed all 8 + 1 borderline ontology + folded 11 non-blocking observations; v2 re-review surfaced 3 documentation-hygiene defects from the v1→v2 replace_all; three mechanical tweaks at acceptance — self-referential rename declarations + stale Q-011 bucket-2 §Accepts citation). (b) GitHub mutation authority split by credential class: `GitHubMutationAuthority` inline value type (NOT a Q-011 bucket member; carried on operation-shape evidence and ApprovalGrant.scope) with `authority_kind: human_pat | github_app | oidc | actions_token | unknown` discriminator. Mutation-class × authority-class matrix posture: agentic content-write / admin / merge / ref-deletion require github_app or oidc; human_pat reserved for exploratory / manual operations only. system-config repo (canonical policy authority) requires github_app OR human break-glass; human_pat rejected regardless of class. (c) Branch / ruleset baseline: minimum protection state for HCS + system-config before agentic push / merge. New evidence subtype `RulesetObservation` (Q-011 bucket 1; evidenceSchema-direct typed payload) per-(repository_id, ruleset_id) grain with `ruleset_kind` + `enforcement_kind` discriminators. Coexists with `BranchProtectionObservation` (ADR 0027 v2); conflict resolution = union of restrictions across the two shapes (per-axis maximum). (d) Five-plane identity reconciliation via `RepositoryIdentityReconciliation` evidence subtype (bucket 1): local filesystem path canonical (scrubber-eligible), remote URL / repository_id, SSH alias, signing principal, credential account identity. `reconciliation_verdict_kind: all_planes_consistent | plane_disagreement` discriminator; Layer 2 broker FSM re-check at operation-execution time per inv. 17 produces NEW reconciliation record bound to execution-time execution_context_id (NOT re-validated mint-time record). Sandbox-promotion rejection rule per inv. 8: Evidence.authority sandbox-observation/self-asserted blocks promotion to host-authoritative gate evidence. Cross-org rejection at Layer 1: remote_url_canonical resolving to different org than WorkspaceContext.org_id rejects. (e) MCP read vs mutation auth split: split into two distinct credentials; until split deployed, all mutating GitHub MCP calls require typed ApprovalGrant. New evidence subtype `MCPCredentialAudienceObservation` (bucket 1) with `mcp_server_kind` (closed enum discriminator per Sub-rule 6) + `credential_audience_kind: read_only | mutation | unscoped` discriminator + `credential_scope_tokens` (array<closed-enum scope_token>; replaces v1's free-form credential_scope_summary; closes secret-shaped-string paste vector) + `query_observed_via` (per ADR 0030 v2 three-way naming convention) + `provider_verified_at` (kernel-set; matches StatusCheckSourceObservation). (f) Confirmed minimum BranchDeletionProof: ADR 0025 v2 sufficient (composite already distinguishes local vs remote branch deletion; multi-worktree branches; merge_proof_kind discriminator with ancestry / patch_equivalence / vacuous; pr_state_kind discriminator with absent / open / closed_unmerged / merged). No additional schema or posture additions. (g) Committed StatusCheckSourceObservation receipt shape (Q-011 bucket 1; evidenceSchema-direct typed payload — observational, not boundary fact). Required fields: repository_id, commit_sha, check_name, expected_github_app_id OR expected_workflow_path (mutually inclusive of source identity via Zod .refine pattern), conclusion_kind, concluded_at, valid_until (24-hour Phase 1 interim hard cap at Layer 1 mint API; canonical policy at Milestone 2 commits per-repository_id maxima), source_kind, provider_observed_via, kernel-set provider_verified_at. App-permission-surface drift handling closes ADR 0032 v2 Security N-3 bypass: Layer 2/3 re-check rejects with status_check_source_app_drift on permission revocation OR App uninstall (404 on provider re-query rejects, NOT silent success on cached state). ADR 0032 v2 §StatusCheckSourceObservation interim total-block lifts on schema PR landing (NOT on this ADR's acceptance). Cross-cutting: ApprovalGrant.scope per-class extension for external_control_plane_mutation: kernel-set `expected_authority_kind` (derived from operation's matrix-cell minimum) + kernel-set `observed_authority_kind` (derived at Layer 3 from invocation's actual GitHubMutationAuthority evidence) — mismatch rejects with github_mutation_authority_unverified; producer-supplied values rejected at Layer 1. Closes the v1 escalation hole where producer could claim github_app while presenting human_pat. system-config policy-path mutation rule: mutations against `policies/host-capability-substrate/**` require BOTH github_app/oidc authority AND typed ApprovalGrant regardless of GitHub-side ruleset state (closes v1 P-B2 escalation hole). Grant scope binds `mcp_server_kind` when MCP-dispatched; cross-MCP-server grant reuse rejected. Six new Decision.reason_kind reservations (posture-only; Decision-level per ADR 0029 v2 framing): github_mutation_authority_unverified, ruleset_baseline_unmet, repository_identity_mismatch, mcp_credential_audience_mismatch, status_check_source_unverified, status_check_source_app_drift. Four new evidence_subject_kind enum values reserved: ruleset, repository_identity_reconciliation, mcp_credential_audience, status_check_source. `pat_keyring_account` declared scrubber-eligible per registry v0.3.0 §Field-level scrubber rule (closes fingerprinting risk via audit-chain co-recording). Authority-class signals follow registry v0.3.2: identity and freshness fields kernel-set; ruleset / reconciliation / audience / check-source observation fields producer-asserted but kernel-verifiable. Cross-context binding rules per Ring 1 layer explicit per registry v0.3.0. plane_disagreement audit-chain attribution explicit (agent_client_id, session_id, rejecting layer, plane_disagreements array). Eight forward-looking concerns deferred to schema PR / Milestone 2 / follow-up ADRs: schema PR for the five new subtypes + value type + six reason_kind reservations + ApprovalGrant.scope per-class extension; evidenceSubjectKindSchema enum extension; canonical policy YAML at Milestone 2 (mutation-class × authority-class matrix entries, ruleset baseline IDs, per-repository_id freshness windows, per-mcp_server_kind audience-class enforcement); ADR 0032 v2 interim total-block lift trigger on schema PR landing; Decision.reason_kind accumulating-cohort registry update PR (~25 reservations across ADRs 0029 v2 / 0030 v2 / 0032 v2 / 0033 v2); ExecutionContext.actor_kind field commitment (separate Q-*; gates ADR 0032 v2 MacBook always-on cross-context human-driven binding rule); ADR 0026 substrate hook architecture (gated on stage-1 BranchProtectionObservation schema landing; not gated on this ADR); Q-007 (b)-(f) sub-decisions (`QualityGate` deferral cadence, composition with ExecutionContext / CredentialSource / GitIdentityBinding / ToolProvenance, dashboard views, charter v1.4 candidate). **Q-007(b) (QualityGate deferral cadence) is now FULLY UNBLOCKED at the posture layer** (Q-005 + Q-006 (b)-(g) settled). Q-007 (b)-(f) becomes the next candidate Q-row. No repo settings, canonical policy YAML, schema source, SSH, Git, MCP, workflow, CODEOWNERS, branch-protection, ruleset, credential migration, hook, adapter, dashboard route, runtime probe, or mutation endpoint is accepted by ADRs 0027 / 0030 / 0033 alone. |
| Q-007 | Quality-management and boundary-accommodation model. The 2026-04-29 quality-management synthesis (`docs/host-capability-substrate/research/local/2026-04-29-quality-management-synthesis.md`) reconciles two source reports from `/private/tmp` and concludes that macOS app/TCC/filesystem boundaries, Git/GitHub identity routing, package-manager shims, credential helpers, and multi-account use are loose enough that HCS must treat boundary uncertainty as first-class evidence. Proposed ADR 0022 (`docs/host-capability-substrate/adr/0022-boundary-observation-envelope.md`) splits Q-007a from the larger quality-gate question and proposes `BoundaryObservation` as an `Evidence` subtype envelope before `QualityGate`. Sub-decisions: **(a)** should `BoundaryObservation` land as an `Evidence` subtype envelope with freshness and execution-context binding, independent envelope/evidence/payload schema versions, explicit target references, primary-target binding, ontology-reviewed singular `boundary_dimension` taxonomy, `observed_payload` / `expected_payload` domain payload semantics, and a full `Evidence` base-shape prerequisite? **(b)** should `QualityGate` remain deferred until `BoundaryObservation`, Q-005 runner/check evidence, and Q-006 source-control evidence settle? **(c)** how does boundary evidence compose with `ExecutionContext`, `CredentialSource`, `GitIdentityBinding`, and `ToolProvenance` candidates? **(d)** what operations degrade to warning versus require approval when boundary evidence is stale, missing, or contradictory, and how do `Decision` / `ApprovalGrant` consume those evidence refs? **(e)** which dashboard views are needed before humans can review quality gates effectively? **(f)** does "boundary claims are freshness-bound and execution-context-bound" warrant a charter v1.3.0 invariant, or stay as a Phase 1 design principle? | user | Phase 1 schema / quality-gate synthesis | Q-007(a) accepted via ADR 0022 (2026-05-02): the `BoundaryObservation` envelope shape is settled as an `Evidence` subtype envelope, and `docs/host-capability-substrate/ontology-registry.md` is the named registry for `boundary_dimension` values. **Sub-decisions (b)-(f) accepted via ADR 0034 (revision 2, accepted 2026-05-03)** after a two-revision review cycle (v1 returned 7 blocking findings — 3 policy escalation surfaces, 4 security blockers; v2 closed all 7 with no new blockers; all four reviewers returned READY-FOR-ACCEPTANCE on v2; 13 non-blocking observations folded; 2 mechanical tweaks at acceptance — posture-vacuum body-promotion + ${USERS_SHARED} placeholder root). Q-007 (b)-(f) was fully unblocked at the posture layer when Q-005 (ADR 0032 v2) + Q-006 (b)-(g) (ADR 0033 v2) settled earlier 2026-05-03. (b) QualityGate timing: defer to standalone follow-up ADR (Q-007(g)). ADR 0034 does NOT define QualityGate; ADR 0022 §Future amendments deferral satisfied. No posture vacuum during deferral: gate-consuming operations gate on `BoundaryObservation` evidence_refs in interim via the §Sub-decision (d) three-state matrix; QualityGate is aggregation/identity layer, NOT new threshold layer. (c) Boundary evidence composition: linked-observations pattern via `evidence_refs` (NOT nested fields). Two new evidence subtypes (Q-011 bucket 1; `evidenceSchema`-direct typed payload): `GitIdentityBinding` (per-(workspace_id, surface_id) grain; payload includes Git user.name/user.email scrubber-eligible, `git_signing_key_id` typed FK to `CredentialSource` (NOT raw string), `git_signing_format_kind` discriminator, `credential_source_evidence_ref`; subtype-level redaction floor `redaction_mode != none` required at Layer 1 mint API per inv. 5; cross-surface rejection at Layer 1 with `boundary_evidence_missing`); `ToolProvenance` (per-(`tool_or_provider_ref`, `execution_context_id`) grain; payload subsumes shim chain + `install_source_kind` (8-value enum) + `version_drift_kind` (5-value enum) discriminators; flat-payload composition not Sub-rule 5 sibling-array; §Path canonicalization rule canonicalizes `installed_path` + `shim_chain` paths to placeholder form `${HOME}` / `${TMPDIR}` / `${XDG_CACHE_HOME}` / `${XDG_DATA_HOME}` / `${XDG_CONFIG_HOME}` / `${USERS_SHARED}` at Layer 1 mint API; raw paths with username/TMPDIR-seed strings rejected with `secret_resolution_in_chunk` per inv. 5). Plus four new `boundary_dimension` registry candidates queued: `execution_context_boundary`, `credential_source_boundary`, `git_identity_boundary`, `tool_provenance_boundary`. Both direct-typed-payload (subtypes) and `BoundaryObservation` envelope shapes coexist; consumers choose at composition time; mint-time direct-subtype consumers cannot re-project to envelope payloads without fresh observation (preserves inv. 19 candidate "no lateral substitution"). (d) Three-state matrix mirroring ADR 0029 v2 anomalous-capture matrix. States: `block | approval_required | warn`. Anomaly classes (rows): stale (`valid_until` expired) | missing (required dimension absent) | contradictory (linked observations diverge). Six operation classes (from ADR 0029 v2). Cells: stale × destructive_git/external_control_plane_mutation/worktree_mutation/merge_or_push = approval_required; missing or contradictory × destructive classes = block; read_only_diagnostic + agent_internal_state = warn. Applies at all three Ring 1 layers. `unknown` evidence_state evaluates as `missing` for matrix purposes (NOT a fourth row; ADR 0022 seven-state observer-side enum and matrix gateway-side rows remain distinct; avoids permissive interpretation of unknown for destructive operations). Per-`boundary_dimension` `valid_until` windows policy-set in `tiers.yaml` at Milestone 2; matrix is universal posture. Three new Decision.reason_kind reservations (posture; Decision-level per ADR 0029 v2): `boundary_evidence_stale`, `boundary_evidence_missing`, `boundary_evidence_contradictory` (latter co-records `divergent_evidence_ref_pair` audit-chain attribution). Three new Decision.required_grant_kind reservations: `boundary_evidence_freshness_override`, `boundary_evidence_contradiction_acknowledgment`, `boundary_evidence_absence_acceptance` (single-use per operation_id, mirrors ADR 0030 v2 / ADR 0031 v1 grant patterns). ApprovalGrant.scope per-class extension binds `boundary_observation_evidence_refs` + `execution_context_id`; **Layer 3 evidence-refs binding integrity rule**: operation's consumed evidence_refs MUST be subset of grant's bound boundary_observation_evidence_refs; set-mismatch rejects with `boundary_evidence_missing` (closes intra-class evidence-ref substitution gap). Scope-key disjointness preserved per ADR 0019 v3 / ADR 0031 v1 / ADR 0033 v2: boundary-evidence grants do NOT overlap with `worktree_mutation`, `destructive_git`, `merge_or_push`, `external_control_plane_mutation`, `runner_registration` / `runner_deregistration` per-class extensions. **Sandbox-promotion rejection (charter inv. 8) tightened**: evidence-side rule (Evidence.authority sandbox-observation/self-asserted blocks promotion to host-authoritative gate) + grant-side rule (Layer 1 rejects boundary_evidence_* grants whose cited evidence_refs carry sandbox/self-asserted authority) + contradiction-asymmetry rule (sandbox observation cannot stand against host-observation as contradicting side; cross-authority pairs are NOT contradictory but expected divergence with host governing). **Charter inv. 17/18-candidate/19-candidate freshness-precedence rule**: when CoordinationFact (inv. 18) cites BoundaryObservation (inv. 19), the underlying BoundaryObservation.valid_until is binding; CoordinationFact.valid_until cannot extend beyond. Layer 2/3 enforcement explicit. Prevents laundering stale boundary observations through fresh coordination facts. (e) Six dashboard views named in view-shape commitment (posture-only; React implementation deferred to separate dashboard ADR per ADR 0019 v3 precedent): `/quality-gates`, `/boundary-observations`, `/quality-decisions`, `/credential-sources`, `/tool-provenance`, `/mutation-queue`. Display vocabulary uses ADR 0022 seven-state enum. `/mutation-queue` consumes `boundary_evidence_*` and `anomalous_capture_*` rejection classes uniformly via `Decision.reason_kind` enum (NOT re-implementing matrix logic at view layer). `version_observed` aggregation across long windows in `/tool-provenance` is a cohort-fingerprinting side-channel; dashboard ADR commits aggregation boundaries. (f) Charter inv. 19 candidate text drafted: "Boundary claims are freshness-bound and execution-context-bound. Every BoundaryObservation and related evidence subtype must carry valid_until and an execution-context binding (execution_context_id, surface_id, workspace_id, or credential_source_id). HCS must model contradictory, missing, and stale boundary evidence as distinct states, not promote them to false negatives or unknown-as-false. Boundary inference cannot cross macOS app, shell, package-manager, Git/GitHub, or MCP surfaces without a matching observed context. Linked BoundaryObservation records sharing target references may represent multi-surface facts; lateral context reuse requires fresh observation, not substitution." Composes with inv. 6/7/8/17/18-candidate. Amendment lands per change-policy in separate charter PR after this ADR's acceptance; target charter v1.4.0; may land alongside inv. 18 in a single wave or stand alone. **Q-007(g) accepted via ADR 0035 (revision 2, accepted 2026-05-04)** after a two-revision review cycle (v1 returned 3 blocking findings — all security; laundering-attack vectors S-B1 sandbox-laundering via gate-citing-gate, S-B2 acknowledgment-flooding, S-B3 proven-expired-re-mint laundering loop; v2 closed all 3 with no new blockers; all four reviewers returned READY-FOR-ACCEPTANCE on v2; 14 non-blocking observations folded; 3 mechanical tweaks at acceptance — meta-gate Ring 1 explicit sentence, denial-rate forward-look, Layer 1 atomicity note). QualityGate is a Q-011 bucket 2 standalone Ring 0 entity (durable identity + lifecycle); ADR 0035 is the follow-up to ADR 0034 v2 §Sub-decision (b) deferral. Five major decisions settled per pre-draft research-grounded human review: (1) Gate identity triple `(gate_id UUID, gate_kind closed-enum discriminator, target_subject_ref polymorphic FK)`. Six gate_kind reserved values: `identity_binding`, `credential_shadow`, `signing_identity`, `filesystem_trust`, `tool_provenance`, `mutation_class`. Per-kind target_subject_ref resolution table committed. (2) Four-state lifecycle (`provisional | proven | expired | denied`) mirroring ADR 0031 v1 Lease pattern. NO allowed_for_gate boolean (gates are aggregations of typed evidence, not authored claims). Transitions emit typed Decision events per registry v0.3.1. (3) Single polymorphic evidence_refs array per evidenceRefSchema (NOT per-kind discriminator-and-array). Per-kind composition rules documented narratively. (4) Parallel-layer composition with ADR 0034 v2 three-state matrix (NOT sequential). Matrix operates per-invocation on individual BoundaryObservation evidence; QualityGate aggregates across multiple observations into durable typed entity. Both produce typed Decisions; Layer 3 gateway enforces more restrictive when both apply. (5) Per-gate_kind evidence sketches: `identity_binding` requires GitIdentityBinding + RepositoryIdentityReconciliation; `credential_shadow` requires CredentialSource + MCPCredentialAudienceObservation + credential_source_boundary; `signing_identity` requires GitIdentityBinding.git_signing_format_kind + signing-key CredentialSource + Q-006 stage-3 CommitSigningReceipt (gate stays provisional until that ADR lands); `filesystem_trust` requires filesystem_authority + GitDirtyStateObservation + execution_context_boundary; `tool_provenance` requires ToolProvenance for critical tools (git/gh/ssh/op); `mutation_class` is meta-gate aggregating identity_binding + signing_identity + tool_provenance + filesystem_trust (all four sub-gates must be proven for meta-gate to be proven). Three security blockers closed: S-B1 universalized cross-authority composition rule (any consumer — gate or operation — MUST reject evidence_refs whose transitive chain touches Evidence.authority in {sandbox-observation, self-asserted}); S-B2 duplicate-target rule (one proven OR provisional gate per (target_subject_ref, gate_kind, execution_context_id) triple at Layer 1 mint API; new gate_target_already_active reason_kind); S-B3 re-mint evidence-rotation rule (subset-only re-mint rejects with new gate_evidence_stale_reuse reason_kind; forces evidence rotation). Sandbox-promotion rejection rule (charter inv. 8) inherited and universalized: evidence-side rule + grant-side rule + cross-authority composition rule (now applies to ALL gate_kinds, not just mutation_class) + transitive-walk rule. Three-lifecycle separation made explicit: Lease (temporal-extent ownership) / CoordinationFact (authored-claim verification) / QualityGate (evidence-aggregated typed gates) — orthogonal concerns. Meta-gate aggregation explicit Ring 1 kernel-set computation; Ring 0 stores resulting state + evidence_refs chain. Six new Decision.reason_kind reservations (posture; Decision-level per ADR 0029 v2): gate_provisional, gate_denied, gate_expired, gate_evidence_insufficient, gate_target_already_active (NEW S-B2), gate_evidence_stale_reuse (NEW S-B3). One new Decision.required_grant_kind reservation: gate_evidence_acknowledgment (single-use per operation_id; scope binds gate_id + execution_context_id; NOT applicable to expired state — re-mint with fresh evidence is canonical). One new evidence_subject_kind enum value reserved: quality_gate (allows entities to cite QualityGate records via evidenceRefSchema, e.g., mutation_class meta-gate citing sub-gates). Decision transition record fields enumerated explicitly: gate_id, prior_state, new_state, transition_reason (closed-enum: evidence_satisfied / evidence_failed / valid_until_passed / target_already_active_rejection / evidence_stale_reuse_rejection / cross_authority_composition_rejected), evidence_refs_at_transition, transition_timestamp, failing_sub_gate_id (present iff mutation_class meta-gate denied), agent_client_id + session_id per registry v0.3.1 canonical attribution. Layer 1 atomicity for duplicate-target rule explicit (SQLite WAL transaction + unique constraint on triple where gate_state IN ('provisional', 'proven')). Six forward-looking concerns deferred: schema PR for QualityGate + six Decision.reason_kind reservations + one Decision.required_grant_kind reservation + ApprovalGrant.scope per-class extension binding gate_id + execution_context_id; registry update PR for gate_kind (six values) + gate_state (four values) + quality_gate evidence_subject_kind enum extensions; canonical policy YAML at Milestone 2 (per-(gate_kind, operation_class) composition rules; per-gate_kind valid_until window maxima; verifier-class privileges; matrix entries for mutation_class meta-gate; per-(producer, target_subject_ref, gate_kind) denial-rate ceiling per Policy P-N1; per-gate_kind evidence-rotation materiality rule per Policy P-N2; cross-authority composition as explicit predicate); Q-006 stage-3 CommitSigningReceipt ADR (signing_identity gate stays provisional until that lands); separate dashboard ADR commits /quality-gates view React implementation; charter v1.4.0 amendment PR for inv. 19 text. **Q-007 is now FULLY ACCEPTED ((a) ADR 0022, (b)-(f) ADR 0034 v2, (g) ADR 0035 v2)**. Six forward-looking concerns deferred to schema PR / Milestone 2 / follow-up ADRs: schema PR per `.agents/skills/hcs-schema-change` for the two new evidence subtypes + six Decision.reason_kind / required_grant_kind reservations + ApprovalGrant.scope per-class extension; registry update PR for four new boundary_dimension candidates + two new evidence_subject_kind enum values + provider_observed_via enum extensions; charter v1.4.0 amendment PR for inv. 19 text; separate dashboard ADR (React component implementations + version_observed aggregation boundaries); canonical policy YAML at Milestone 2 (per-boundary_dimension freshness windows, verifier-class privileges, system-config dual-authority rule); Q-007(g) QualityGate ADR. ADR 0022 acceptance commits envelope shape only — no `BoundaryObservation` schema, policy tier, dashboard route React component, regression-trap, GitHub, macOS, SSH, package-manager, runtime behavior, or mutation operation is accepted by ADRs 0022 or 0034 alone. |
| Q-008 | Agent execution reality and destructive Git hygiene model. The 2026-04-30 ScopeCam exchange synthesis (`docs/host-capability-substrate/research/local/2026-04-30-codex-scopecam-exchange-synthesis.md`) shows a distinct failure family: an agent converted no-output/tool symptoms into environment diagnoses before proving execution state, mixed normal and escalated execution evidence, used weak auth probes, treated remote-gone branches as cleanup candidates without ancestry or patch-equivalence proof, and let worktree/branch-flow topology remain implicit. Sub-decisions: **(a)** should `ToolInvocationReceipt`, `CommandCaptureReceipt`, or `ExecutionModeObservation` become Ring-0 concepts or `Evidence` / `Run` / `Artifact` subtypes? **(b)** when does anomalous command capture block implementation, cleanup, merge, push, or worktree mutation? **(c)** what proof is required before branch deletion (`BranchDeletionProof`, worktree attachment check, ancestry proof, zero-diff/patch-equivalence proof), and should force deletion be non-escalable or approval-only? **(d)** how should worktree ownership compose with `WorkspaceContext`, `Lease`, and Q-003 coordination facts? **(e)** should PR body creation through inline shell strings be forbidden in favor of `--body-file` in GitHub operation renderers and agent guidance? | user | Phase 1 execution-evidence / Git hygiene synthesis | Q-008(e) accepted (2026-05-02): GitHub body-bearing operations (`gh pr create`, `gh issue create`, `gh {pr,issue} comment`, `gh {pr,issue} edit`) must use `--body-file <path>` or `--body-file -` with stdin for multi-line bodies; inline `--body "..."` is reserved for single-line content. Recorded in `AGENTS.md` Hard-boundaries; applies to HCS Ring 2 adapter renderers and to all agent guidance. Q-008(c) accepted via ADR 0025 (revision 2, accepted 2026-05-02) after `hcs-architect` / `hcs-ontology-reviewer` / `hcs-security-reviewer` returned no blocking objections on the v2 re-review; four mechanical tweaks applied at acceptance (renamed `approval_grant_ref` to `approval_grant_id` per registry naming rule; dropped `proof_schema_version` per registry v0.2.1 §Version-field naming; annotated `evidence_schema_version` semantics; added layer-3 mint-time-and-execution-time re-check sentence). `BranchDeletionProof` is a Ring 0 proof composite (Q-011 bucket 3) **minted by a Ring 1 kernel service** (agents request, Ring 1 authors). Two principal fields: `authoring_service_id` (kernel-set) and `requesting_principal_id` (gateway-set). `repository_id` resolved by minter from `WorkspaceContext`; agent-supplied `repository_id` rejected at mint API. Five-layer defense-in-depth enforcement: (1) Ring 1 mint API refuses force-protected combination, (2) Zod schema structural guard, (3) gateway/policy authoritative non-escalable per inv. 6, re-checked at execution time, (4) `hcs-hook` substrate interception of direct git deletion commands against literal-protected-list (`main`, `master`, `HEAD`); non-literal protection classification queued as ADR 0026 once `BranchProtectionObservation` exists, (5) broker FSM force-flag binding + single-use consumption + contradictory-evidence rejection. Composite shape uses `<thing>_evidence_refs` arrays per ontology-registry v0.2.1; `merge_proof_kind` discriminator (`ancestry`/`patch_equivalence`/`vacuous`) and `pr_state_kind` discriminator (`absent`/`open`/`closed_unmerged`/`merged`) replace v1's collapsed-OR fields; positive-absence `PullRequestAbsenceReceipt` required for `pr_state_kind = absent`; positive empty-branch evidence required for vacuous merge. Multi-worktree branches require `worktree_inventory_evidence_refs`. Composite participates in audit hash chain. Schema implementation gated on Q-006 component evidence subtypes (`GitRepositoryObservation`, `GitWorktreeObservation`, `GitWorktreeInventoryObservation`, `GitBranchAncestryObservation`, `GitRemoteObservation`, `GitDirtyStateObservation`, `PullRequestReceipt`, `PullRequestAbsenceReceipt`) landing first. **Q-008(a) accepted via ADR 0028 (revision 4, accepted 2026-05-03)** after a four-revision review cycle (v1 returned 11 blocking findings; v2 closed them; v3 closed five v2-re-review gaps; v4 closed three v3-re-review gaps and three mechanical tweaks at acceptance). `ToolInvocationReceipt`, `CommandCaptureReceipt`, and `ExecutionModeObservation` all use `evidenceSchema` directly as typed payloads (Q-011 bucket 1; no new envelopes). `evidence_kind` is `receipt` for the first two; `observation` (telemetry-backed) or `derived` (computed) for the third. `CommandCaptureReceipt.capture_status: empty` is the typed positive empty-capture receipt closing the ScopeCam motivating failure at the receipt-shape layer (empty stdout is a positive observation about capture, not an implicit signal about the world). Authority-class fields (`captured_by`, `observed_via`) are removed from producer payloads per registry v0.3.2 §Producer-vs-kernel-set authority fields; the kernel/mint API sets them from execution context. `Evidence.producer` is kernel-set when its value names a kernel-trusted producer class (allowlist: `kernel_broker`, `kernel_telemetry`, `mint_api`); producer-supplied values naming an allowlist class are rejected at the mint API. The new `self-asserted` authority class (registry v0.3.0) is the canonical class for unverified producer claims; cannot be promoted. Producer-crash kernel watchdog: when a broker-mediated producer exits without a closing `ToolInvocationReceipt`, the broker FSM emits a synthetic receipt via `kqueue EVFILT_PROC NOTE_EXIT` (macOS) with `Evidence.authority: host-observation`, `Evidence.producer: kernel_broker`. Watchdog scope covers broker-spawned/supervised/MCP-via-broker exec paths; agent-harness-spawned tools rely on agent-side closing-receipt + mint-API session-close pairing; daemon-spawned and double-fork-detached children are out-of-scope for Phase 1. `argv_capture_mode` (renamed from v1's `argv_redaction_mode` per registry §Redaction posture) replaces redundant redaction enum vocabulary; the field-level scrubber covers `argv`, `argv_redacted`, `argv_summary`, `stdout_redacted_excerpt`, `stderr_redacted_excerpt`. Excerpt byte cap committed at `<= 1024 bytes` per excerpt. Capture-status × redaction-mode permitted-combination matrix is explicit (promoted to registry v0.3.1 sub-rule). `mode` enum (renamed `sandbox_observation` → `sandboxed` to break collision with `sandbox-observation` authority class): `normal | sandboxed | escalated | isolated_clean_room | unknown`; `unknown` BLOCKS at gateway. Mode-agnostic operations are policy-declared in canonical policy, never producer-asserted; mode-agnostic remains subject to inv. 7 mutation_scope rules. `parent_invocation_id` lives in `subject_refs[subject_kind="tool_invocation", relation="parent"]` per registry v0.2.1 §Field-name suffixes. Cross-context parent linkage requires `ContextInheritanceReceipt` (queued for stage-2 ontology) carrying `host-observation` authority; rejection at all three Ring 1 layers. Schema implementation gated on Q-008(b) blocking thresholds (still pending), the `tool_invocation` evidence-subject enum extension, the `Evidence.producer` schema-side tightening (free-form string → kind-tagged shape), and the `self-asserted` authority class enum extension. **Q-008(b) accepted via ADR 0029 (revision 2, accepted 2026-05-03)** after a two-revision review cycle (v1 returned 16 blocking findings across all four reviewer subagents — 4 architect, 4 ontology, 4 policy, 4 security; v2 closed all 16 with no new blockers; all four reviewers returned READY-FOR-ACCEPTANCE on v2). Cross-cutting registry codifications landed in ontology-registry v0.3.3 (commit `0b24b95`) as a precondition: §Naming suffix discipline §Sub-rule 6 amended to forbid `_code` discriminator suffix in addition to existing `_class` rejection (`Decision.reason_kind` and `ApprovalGrant.required_grant_kind` are canonical names; `_code` / `_class` discriminator suffixes fail ontology review); §Sub-rule 9 codifies enum-value casing (stable enum values use `lower_snake_case`; existing `evidenceAuthoritySchema` `kebab-case` grandfathered for that enum only). Anomalous-command-capture blocking thresholds are expressed as a three-state matrix (`block` | `approval_required` | `warn`) crossed with eight anomalous-capture combinations (A: `empty_apparent_success`, B: `capture_failure`, C: `abnormal_termination`, D: `authority_self_asserted`, E: `mode_unknown`, F: `capture_truncated_at_cap`, G: `producer_class_forgery_attempt`, H: `cross_receipt_inconsistency` — H added in v2 and detected at Layer 2 broker FSM, the first layer with both receipts in hand) and six operation classes (`read_only_diagnostic`, `agent_internal_state`, `destructive_git`, `external_control_plane_mutation`, `worktree_mutation`, `merge_or_push`). `worktree_mutation` row carries default thresholds; final cell content composes with `WorkspaceContext` / `Lease` / Q-003 coordination facts under Q-008(d) once Q-003 settles. `block` cells are non-escalable per inv. 6 and **Decision-level / tier-orthogonal** (do not promote operation to forbidden tier; an `allowed_with_approval` tier operation whose invocation hits a `block` cell rejects without re-tiering); `approval_required` cells consume `ApprovalGrant`; `warn` cells produce typed `Decision` records in the audit chain. Combination G (`producer_class_forgery_attempt`) is enforced as defense-in-depth across all three Ring 1 layers: Layer 1 mint API rejection per registry v0.3.2 (primary), Layer 2 broker FSM re-check, Layer 3 gateway re-derive — bypass attempts that skip Layer 1 still fail at Layers 2/3 with audit events emitted at every layer per registry v0.3.1 §Audit-chain coverage of rejections. Combination E (`mode_unknown`) inherits ADR 0028 v4's gateway-BLOCK rule with explicit Layer 1/2/3 re-check mapping. Operation-class classification authority: owning Ring 1 service registers operation class at registration time; broker FSM enforces at execution time; cross-class promotion at invocation time forbidden and itself a typed Decision. Closed-list fail-mode: tightening defaults for unregistered combinations (`block` for destructive_git / external_control_plane_mutation / worktree_mutation / merge_or_push; `warn` for read_only_diagnostic / agent_internal_state); unregistered operation class rejected with `Decision.reason_kind: operation_class_unregistered`. `ApprovalGrant.scope` shape sketch (posture-only): grants must bind `operation_class` + `anomalous_capture_combinations` + `execution_context_id` + `target_ref`; per-class extensions for `destructive_git` (commit SHA + first-commit-SHA `repository_id`, matching ADR 0025 v2 BranchDeletionProof scope), `merge_or_push` (source ref + target ref + commit SHA), `external_control_plane_mutation` (provider_id + provider-side target_id, matching inv. 16); `worktree_mutation` deferred to Q-008(d). Approval × self-asserted authority composition: standard `ApprovalGrant` does NOT clear `approval_required` cells when consuming evidence carries `Evidence.authority: self-asserted`; a typed self-assertion-acknowledgment grant with `acknowledges_self_asserted_authority: true` bound to specific `evidence_id` is required; non-transferable across same-kind different-record evidence. Proof composites (e.g., ADR 0025 v2's `BranchDeletionProof`) consume the matrix at composition time. The matrix applies at all three Ring 1 layers (mint API + broker FSM re-check + gateway re-derive) per registry v0.3.2 §Cross-context enforcement layer; combinations A and H surface at Layer 2 (FSM is first layer with both receipts in hand). Doc-only / posture-only ADR; canonical numeric thresholds (rate windows, session caps, freshness deadlines) land in `system-config/policies/host-capability-substrate/tiers.yaml` once HCS Milestone 2 ships. Three forward-looking acceptance notes deferred to downstream work (no ADR-level mechanical tweaks were applied at acceptance): (i) first-commit-SHA `repository_id` idiom from `ApprovalGrant.scope` `destructive_git` extension needs ontology-registry entry before schema PR using it lands; (ii) typed self-assertion-acknowledgment grant binding behavior should be cross-referenced from canonical policy YAML rather than re-derived; (iii) schema PR enumerating `Decision.reason_kind` must list each named value introduced (`producer_class_forgery_attempt`, `cross_receipt_inconsistency`, `operation_class_unregistered`, plus the eight anomalous-capture combination names). **Q-008(d) accepted via ADR 0031 (revision 1, accepted 2026-05-03)** after a one-revision review cycle (v1 returned 0 blocking findings across all four reviewer subagents — the cleanest first-round outcome of Phase 1 ADRs; 12 mechanical tweaks folded at acceptance). With Q-003 settled (ADR 0019 v3 accepted 2026-05-03), Q-008(d) was unblocked at the posture layer. Five sub-decisions settled per pre-draft human review: (A) `WorkspaceContext` ↔ worktree cardinality is one-to-one for Phase 1; (B) `Lease` shape is per-(session, repository_id, worktree_path) grain matching `GitWorktreeObservation` per-worktree grain; (C) `CoordinationFact` shape for worktree ownership: `subject_kind: "worktree"` + `subject_ref: { repository_id, worktree_path }` + `predicate_kind: "leased_to"` + `object_kind: "scoped_assertion"` + `object: { session_id, lease_id, valid_until, lease_acquired_at }`; (D) `worktree_mutation` matrix cells: keep ADR 0029 v2 defaults; Q-008(d) introduces worktree-specific rejection classes that compose with the matrix; (E) Conflict resolution patterns: typed Decision events with Layer 1/2/3 enforcement. Lease entity gains worktree-ownership composition fields (posture-only): `lease_kind: "worktree" | "credential_audience" | "external_target"` discriminator; kernel-set `held_by_session_id` + `held_by_agent_client_id` per registry v0.3.1 canonical attribution; `acquired_at` / `valid_until` / `released_at` / `force_break_grant_id` timestamps + grant FK; `lease_state: "active" | "expired" | "released" | "force_broken"` discriminator (bare-noun central concept per registry Sub-rule 8); worktree-specific `repository_id` / `worktree_path` / `workspace_context_id` (when `lease_kind: "worktree"`). WorkspaceContext gains `worktree_path` + `repository_id` field-shape; legacy contexts without the field default-block worktree_mutation per Closed-list fail-mode discipline. Twelve mechanical tweaks applied at acceptance: (1) force-break separation of duties (minter ≠ holder, mirrors ADR 0019 v3 producer-equals-verifier prohibition) + Phase 1 dashboard-only interim posture; (2) `worktree_path` canonicalization at L1 mint API (resolves `..` traversal + symlinks before uniqueness check); (3) sandbox-derived lease rejection at L1 (charter inv. 8 — sandbox-execution session cannot acquire `lease_kind: "worktree"`); (4) release authority L1 check (only holder can release); (5) `valid_until` 24-hour interim cap until M2 canonical policy commits per-`lease_kind` windows; (6) TOCTOU atomic insert with unique constraint on `(repository_id, canonical_worktree_path, lease_state == "active")`; (7) audit lineage preservation via Decision `evidence_refs` to prior acquire Decision (force-break event preserves holder lineage); (8) cross-context "consistency" tightened to explicit equality rule (`WorkspaceContext.execution_context_id == Session.execution_context_id`); (9) backward-compat WorkspaceContext default-block for legacy contexts without `worktree_path`; (10) `coordination_fact_worktree_drift` body definition (fires at L2/L3 when `Lease` transitions to expired/released/force_broken between CoordinationFact mint and gate consumption, OR when cited GitWorktreeObservation's lease_id no longer matches CoordinationFact.object.lease_id); (11) cardinality-not-duplicated forward-look (kernel mint-API invariant, not policy YAML; same posture for TOCTOU + canonicalization rules); (12) `lease_kind` scope-key disjointness forward-look (cross-class grant reuse rejected; future `credential_audience_mutation` / `external_target_mutation` operation classes binding `lease_id` must reference `lease_kind` to disambiguate). ApprovalGrant.scope per-class extension for worktree_mutation committed (closes ADR 0029 v2 deferred row): operation_class + target_ref: {repository_id, worktree_path} + required lease_id + execution_context_id; grants without lease_id rejected at L1. Five new Decision.reason_kind reservations (posture-only): worktree_lease_unavailable, worktree_lease_held_by_other_session, worktree_lease_expired_during_mutation, worktree_not_in_workspace_context, coordination_fact_worktree_drift. One new Decision.required_grant_kind reservation: worktree_lease_force_break_acknowledgment. Three Predicate-kind vocabulary candidates confirmed for the registry follow-up (per ADR 0019 v3 §Predicate-kind registry reservation): leased_to (primary, committed by this ADR), attached_to (reserved for branch-worktree binding), held_by (reserved for generic ownership). Lease lifecycle: four typed Decision events (acquire / release / expiry / force-break) participating in audit hash chain per inv. 4 + registry v0.3.1 (extended by inheritance). Eight forward-looking concerns deferred to schema PR / Milestone 2 / regression corpus / future ADRs (no further ADR-level mechanical tweaks): Q-008(d)-followup ADR (WorkspaceContext switch mid-session — Phase 1 default explicit release required); schema PR per `.agents/skills/hcs-schema-change` for Lease field additions / WorkspaceContext.worktree_path / CoordinationFact subject_kind: "worktree" shape; §Predicate-kind vocabulary registry update PR (precondition for CoordinationFact schema implementation); canonical policy at Milestone 2 (per-lease_kind windows, per-class force-break authority, verifier-class privileges, backward-compat tightening); charter v1.4.0 candidate codifying "worktree mutations require typed lease + dirty-state + matrix-pass" as forbidden-pattern violation; regression corpus expansion with 8 suggested traps from policy + security reviews; Lease force-break audit chain reconstruction test; multi-worktree WorkspaceContext revisit when an incident motivates. Six seed-level traps (#39-#44) queued; #44 (inline-pr-body-shell-expansion) is now eligible for scaffold promotion. Three additional regression-trap candidates surfaced during ontology v2 review (Layer-2 G defense-in-depth verification, Layer-2 H detection on contradictory receipt pair, self-assertion-acknowledgment grant non-transfer); five additional security-suggested tests (G-bypass at Layer 2, novel-combination + standing grant, cross-class escalation attempt, self-assertion grant transfer, grant-scope under-binding); five additional policy-suggested tests. No `ToolInvocationReceipt` / `CommandCaptureReceipt` / `ExecutionModeObservation` schema, `BranchDeletionProof` Zod source, `Decision.reason_kind` / `Decision.required_grant_kind` / `ApprovalGrant.scope` schema source, branch-rule, hook architecture beyond the literal-protected-list (queued as ADR 0026), or runtime behavior is accepted by this row. |
| Q-009 | HCS diagnostic surface and workspace manifest model. The 2026-04-30 HCS evidence/planning synthesis (`docs/host-capability-substrate/research/local/2026-04-30-hcs-evidence-planning-synthesis.md`) consolidates ScopeCam and Budget Triage planning evidence into a normalized HCS vocabulary: candidate runtime diagnostics, D-028 secret-contract hardening, Git/GitHub diagnostics, `WorkspaceContext`/workspace-profile inputs, safe process inspection, docs/reference classification, cleanup planning, and claim reconciliation. Sub-decisions: **(a)** which candidate surfaces become HCS versioned operations (`system.runtime.diagnose.v1`, `system.git.diagnose.v1`, `system.workspace.diagnose.v1`, `system.process.inspect_safe.v1`, `system.docs.diagnose.v1`, `system.cleanup.plan.v1`, `system.claims.reconcile.v1`) versus CLI compatibility wrappers? **(b)** is a target-repo workspace manifest a source of truth, generated view, or host-level registry entry? The Q-011 dedupe plan recommends generated view from `WorkspaceContext` and facts, not a second source of truth. **(c)** how do protected paths, search exclusions, canonical MCP config, verify commands, and docs taxonomy compose with `WorkspaceContext`, `Evidence`, `Lease`, and Q-003 coordination facts? **(d)** should nested-worktree search contamination, duplicate MCP config canonicality, and docs/planning-index projection drift become regression traps? **(e)** how does claim reconciliation avoid becoming untyped agent memory? | user | Phase 1 diagnostic surface / workspace-context synthesis | **Q-009 accepted via ADR 0036 (revision 2, accepted 2026-05-04)** after a two-revision review cycle (v1 returned 19 blocking findings — 3 architect, 8 ontology, 0 policy, 8 security; v2 closed all 19 with structural fixes including (i) splitting `boundary_dimension: filesystem_authority` into three separate dimensions per registry Sub-rule 1, (ii) introducing `security_label: "secret_pointer"` for `op://` literals to keep audit-profile chunks embedding-eligible while still rejecting resolved secrets via universal scrubber, (iii) reclassifying `VerificationCommand` as producer-asserted `VerificationCommandSpec`, (iv) adding Layer 1 host-observation grounding requirement for promotion of `subject_kind: workspace_context \| audit_profile_snapshot` CoordinationFacts and for cleanup ops citing `deletion_authority_kind: coordination_fact`, (v) cross-enum value-collision renames including `subject_kind` → `workspace_context`/`audit_profile_snapshot`, `boundary_dimension` → `mcp_canonical_authority`, `source_kind` → `audit_profile_yaml`, and reason_kind verb-past → `<subject>_<state>` form, (vi) introducing kernel-trusted producer class `kernel_workspace_diagnose` to replace v1's incorrect assertion that `mint_api` was a producer-class enum value; v2 re-review returned zero blockers across all four reviewers, accepted with five mechanical tweaks). The user's structural reframe during deliberation recognized that the audit framework's `project_profile.yaml` is structurally HCS-shaped already (claims/confirmations, dated snapshots, drift detection, attention flags) and slots in as a typed Layer 2 `KnowledgeSource` input rather than competing with new manifest entities. **Sub-decisions settled**: **(a)** ONLY `system.workspace.diagnose.v1` (Ring 1 read-only, `operation_class: read_only_diagnostic`, `mutation_scope: none`) lands in Phase 1; four other candidate operations defer until evidence dependencies clear (Q-006 stage-3, Q-008 typed inputs, Milestone 2 canonical policy); diagnose op outputs `DerivedSummary` minted via new `kernel_workspace_diagnose` producer class; chain-promotion rule + `allowed_for_gate: false` default both block gate-promotion when audit-profile `KnowledgeChunk`s are cited. **(b)** Workspace manifest is a Layer 3 retrieval projection (NOT a file, NOT a Ring 0 entity); three explicit input layers (Layer 1 operational truth, Layer 2 audit-profile `KnowledgeSource`, Layer 3 derived projection per ADR 0019 v3); `system.workspace.manifest.v1` Ring 2 read-only view operation. **(c)** Five governance elements compose via three new `boundary_dimension` values for filesystem authority (`filesystem_inheritance`, `filesystem_protected_paths`, `filesystem_path_authority_check` reserved-only stage-2), one new `boundary_dimension` for MCP config (`mcp_canonical_authority` with `redaction_mode: reference_only`), one new producer-asserted spec entity (`VerificationCommandSpec` with kernel-set author-identity fields + argv-env scrubber), `WorkspaceContext` field additions for `manifest_valid_until` / `verify_operations` / search-lint-docs exclusions / docs-taxonomy-evidence-refs, `KnowledgeSource.source_kind` extensions (`audit_profile_yaml`, `cycle_history`), new `KnowledgeSource.security_label: "secret_pointer"` value with universal-scrubber-on-chunk-mint rule, polymorphic `OperationShape.deletion_authority_source_ref` + `deletion_authority_kind` closed enum (4 values per D-025) with per-target-kind validation, and `cycle-history.md` ↔ CoordinationFact lifecycle binding mapping audit-framework's audit-proposes/operator-promotes workflow to Q-003 verifier promotion pattern. **(d)** Three regression traps (#26 nested-worktree-search-contamination, #27 duplicate-mcp-config-canonicality, #28 docs-planning-index-projection-drift) accepted as design intent; fixtures land when evidence dependencies clear. **(e)** Claim reconciliation already settled by ADR 0019 v3 (`DerivedSummary` + Q-003 promotion workflow + new Layer 1 host-observation grounding requirement); NO new `ClaimReconciliationReceipt` entity. Subject-kind reservations (`workspace_context`, `audit_profile_snapshot`); predicate-kind reservations (`claimed_to_contain`, `confirmed_to_contain`, `claim_superseded_by_snapshot`); six new `Decision.reason_kind` reservations (`mcp_canonical_authority_duplicate`, `verification_command_spec_unmet`, `derived_summary_secret_shape_in_text`, `coordination_fact_insufficient_grounding`, `coordination_promotion_no_layer1_grounding`, `deletion_authority_kind_ref_mismatch`); `workspace_verify` operation_class matrix row committed in posture form (warn/approval_required/block for stale/missing/contradictory); `mutation_scope: verify_workspace` extension. Schema PR + registry update PR + trap fixtures + canonical policy YAML at Milestone 2 + four deferred diagnostic operations + future Q-row for commit-signature-to-principal mapping + future ADR for `system.cleanup.plan.v1` composition + Layer 1 grounding rule extensibility principle for future derived-content subject_kinds all queued in §Future amendments. Q-010 remains the only Q-row in the Phase 1 inventory after Q-009 settles. |
| Q-010 | Cross-agent isolation and compatibility taxonomy. The 2026-05-01 agentic coding tool isolation synthesis (`docs/host-capability-substrate/research/local/2026-05-01-agentic-tool-isolation-synthesis.md`) ingests a broad report on Claude Code, Codex, Cursor, Copilot, Devin, Windsurf, Augment/Intent, Amp, OpenCode, Warp, and VS Code. Core lesson: permission gating, worktree/file isolation, local kernel sandboxing, container/VM isolation, and remote cloud execution are separate evidence dimensions. Sub-decisions: **(a)** should containment posture live directly on `ExecutionContext`, as an `Evidence` subtype, or through future `BoundaryObservation`? ADR 0022 partially constrains this by making containment reachable as a `BoundaryObservation` / `ContainmentObservation` payload, while leaving `AgentClient` field shape open. **(b)** how should `AgentClient` distinguish product family, local/app/IDE/cloud surface, app build, dependency bundle, permission mode, and containment mechanism? **(c)** what remote-agent environment receipt is required before HCS trusts cloud test or PR evidence? **(d)** which adjacent products become first-class HCS target surfaces versus compatibility-only tooling-matrix entries? **(e)** how do daily tool updates invalidate sandbox/containment evidence and trigger re-baseline? | user | Phase 1 schema / compatibility synthesis | **Q-010 accepted via ADR 0037 (revision 2, accepted 2026-05-04)** after a two-revision review cycle (v1 returned 5 blocking findings — 2 architect, 3 ontology, 0 policy, 0 security — primarily naming-discipline + cache-field-ambiguity items; v2 closed all 5 with structural fixes including (i) renaming `ExecutionContext.sandbox_kind` cache field → `kernel_sandbox_kind` to disambiguate "no kernel sandbox in use" from "non-kernel-class containment" with explicit consumer-direction note that consumers needing the broader containment classification MUST read the resolved `BoundaryObservation` payload's `containment_kind` directly, (ii) renaming `containment_runtime_exceeds_capability` reason_kind → `containment_runtime_capability_exceeded` to match the `<subject>_<state>` past-participle precedent from ADR 0036 v2, (iii) normalizing `AgentClient.containment_mechanism` capability-class enum to all-`_capable` form (`terminal_no_isolation_capable`, `ide_host_isolation_capable`, `app_managed_bundle_capable`, `remote_cloud_managed_capable` joining existing `kernel_sandbox_capable`, `container_capable`, `vm_capable`) to eliminate cross-enum value collision with `containment_kind` runtime-class enum, (iv) documenting the ontological distinction between `none` (observation-state — what is observed) and `none_required` (capability-state — why no mechanism applies) in §Authority discipline §Absence-naming convention; v2 re-review returned a single editorial blocker — incomplete `sandbox_kind` → `kernel_sandbox_kind` rename with two stragglers in §Authority discipline kernel-set fields list and §Decision.reason_kind reservations — accepted with four mechanical tweaks closing the stragglers + revision-history self-rename typo + closed-enum count "5" → "6"). **Sub-decisions settled**: **(a)** A3-pointer: `ExecutionContext.kernel_sandbox_kind` cache field becomes kernel-set denormalized cache from `latest_containment_evidence_ref` pointer to `BoundaryObservation` of `boundary_dimension: containment_class`; authority class flips producer-asserted → kernel-set per registry v0.3.2 §Producer-vs-kernel-set; tiebreaker rule = latest-by-kernel-mint-time among non-stale observations (NOT latest-by-`observed_at`; producers can backdate); inv. 17 compliance argument committed inline (cache is projection from typed evidence bound to same `execution_context_id`, not inference); closes Q-011(i) deferred question; commits the `ContainmentObservation` payload shape on `BoundaryObservation` (closes ADR 0022 open dependency without amendment). **(b)** B3-reference: `AgentClient` becomes Q-011 bucket-2 no-suffix Ring 0 entity; identity grain per-(`product_family`, `surface`, `app_build`) per D-029; `agent_client_id` never mutates in place; new `app_build` mints NEW AgentClient; retired AgentClients stay queryable for audit-chain reconstruction; lifecycle `agent_client_state: active \| retired` per registry Sub-rule 8 bare-noun pattern (mirrors `lease_state`); six axes with per-axis authority matrix (5 kernel-set + 1 producer-asserted-kernel-verifiable); composition with `ExecutionContext.kernel_sandbox_kind` via narrower-wins rule (capability-class ↔ runtime-class) at Layer 3 gateway re-derive; new kernel-trusted producer class `kernel_agent_client_resolver`; new `audit_chain_link_hash` field mirroring Lease pattern per ADR 0031 v1. **(c)** C2-split: three Evidence subtypes mirror Q-005 multi-receipt pattern (`RemoteAgentBaseImageObservation`, `RemoteAgentSetupReceipt` with `secret_injection_kind` discriminator (5 values), `RemoteAgentNetworkPostureObservation`); source-app identity composes via existing `StatusCheckSourceObservation` (ADR 0033 v2); checkout commit composes via `evidence_ref` to `GitRepositoryObservation` (ADR 0027 v2) — NOT as payload field (drift class closed by ADR 0030 v2); non-PR binding via `(execution_context_id, observed_at_window)` for Phase 1 with `RemoteAgentInvocationReceipt` aggregator flagged for future amendment; defense-in-depth rationale committed (binding is bounded by kernel-resolved `execution_context_id` AND freshness window). **(d)** D2: surface enum gains ONE new umbrella value `remote_cloud_agent` (12 → 13 closed enum extension); specific cloud-agent products (Cursor, Copilot, Devin, Windsurf, Augment, Amp, OpenCode, Warp Oz, VS Code local agents) stay matrix-only entries in `tooling-surface-matrix.md`. **(e)** E-ratify: containment evidence carries `valid_until` per ADR 0022 envelope; re-baseline trigger = `observed_runtime` change OR `matching_changelog` material change per D-026 authority hierarchy; NO new `boundary_dimension: version_drift` (per ADR 0022 registry rule 5 — version drift is freshness signal, not dimension); canonical policy at Milestone 2 commits per-`boundary_dimension` freshness windows (containment dimension needs hours-to-day order). Cross-cutting Composition rule: remote-agent-produced evidence carries `Evidence.authority: "derived"` against host-authority ladder by default; promotion to stronger authority requires linked-observation chain through host-observation evidence; pre-empts `AgentClient.permission_mode` escalation surface (an `approve_all` claim on a Devin session reads as `derived` evidence about the remote agent's claimed permission mode, NOT host-grade authority); composes with charter inv. 16 (external-control-plane evidence-first) and inv. 8 (sandbox-promotion-rejection extended to remote-agent authority chains). Six new `Decision.reason_kind` reservations (`containment_evidence_absent`, `containment_evidence_producer_supplied`, `containment_runtime_capability_exceeded`, `agent_client_axis_self_asserted`, `remote_agent_evidence_authority_overreach`, `non_pr_remote_agent_binding_partial`). Three regression traps queued (#29 narrower-wins composition, #30 agent-client-axis self-asserted rejection, #31 remote-agent authority overreach) + five additional candidate traps queued in §Future amendments per security reviewer recommendation; final fixture numbering deferred to fixture-landing PR for deconfliction with coordination-store brief candidate range (#31-#35 per `MEMORY.md`). Schema PR + registry update PR + canonical policy YAML at Milestone 2 + future Q-row for first-class per-product surface enum entries + future ADR for `RemoteAgentInvocationReceipt` aggregator + future ADR for cross-cutting AgentClient × WorkspaceContext cardinality all queued in §Future amendments. ADR 0022 confirmed correct as accepted (no amendment required). **Q-010 acceptance closes the Phase 1 synthesis-window Q-row inventory** — schema PR + registry update PR sequencing begins next. |
| Q-011 | Ontology promotion and receipt dedupe rule. The 2026-05-01 planning doc (`docs/host-capability-substrate/research/local/2026-05-01-ontology-promotion-receipt-dedupe-plan.md`) inventories candidate names from Q-003/Q-005/Q-006/Q-007/Q-008/Q-009/Q-010 and ADR 0015/0016/0017/0018/0020. Sub-decisions: **(a)** approve, amend, or reject the three-bucket review rule: evidence subtype, standalone entity, and composite/authored decision artifact; **(b)** decide whether proof composites such as `BranchDeletionProof`, `CleanRoomSmokeReceipt`, and `PolicyPlanReceipt` derive from or are consumed by `ApprovalGrant`; **(c)** decide whether Q-003 authored facts (`CoordinationFact`, `DerivedSummary`) are peer concepts or specializations of existing `Evidence`; **(d)** approve naming suffix discipline for `*Observation`, `*Receipt`, `*Proof`, and no-suffix entities; **(e)** accept duplicate dispositions such as `GitHubRepositorySettingsObservation` over `GitHubRepoSettingsObservation`, `GitWorktreeObservation` over `WorktreeStateObservation`, and one canonical `BranchDeletionProof`; **(f)** approve dependency order before ADR 0019/0020 acceptance and Milestone 1 schema expansion; **(g)** decide the `boundary_dimension` registry artifact, singular-versus-multiple-dimension rule, primary-target binding convention, and whether version/build drift remains freshness invalidation only or becomes a narrower approved dimension; **(h)** define the full `Evidence` base entity or explicit canonical substitute before any `Evidence` subtype envelope acceptance; **(i)** decide how current `ExecutionContext.sandbox` fields migrate or coexist with future `BoundaryObservation` sandbox evidence. | user | Before ADR 0019/0020 acceptance and Phase 1 schema synthesis | Approved — human owner accepted the nine sub-decisions in `docs/host-capability-substrate/human-decision-report-2026-05-01.md`. This resolves the review grammar and sequencing rule only; the Evidence base schema landed separately through ADR 0023 and the schema-change workflow. No ontology-registry artifact, policy tier, hook, adapter, dashboard route, GitHub setting, runtime probe, or mutation operation is accepted by this row. |
| Q-012 | Charter v1.3.0 wave-1 amendment. Proposed ADR 0021 (`docs/host-capability-substrate/adr/0021-charter-v1-3-wave-1.md`) narrows the first charter amendment wave to invariant 16, external-control-plane evidence-first, and invariant 17, execution-context declared-not-inferred. Sub-decisions: **(a)** approve invariant 16 wording independently; **(b)** approve invariant 17 wording independently, including the carve-out for intentional typed inheritance operators such as Codex `inherit` / `include_only`; **(c)** confirm invariants 18-20 stay queued until Q-003/Q-007/Q-008 settle; **(d)** confirm the charter-edit PR changes only the charter and related bookkeeping, not schemas, policy, hooks, adapters, dashboard routes, or mutation operations; **(e)** decide whether the amendment lands as v1.3.0 or another version if additional charter edits intervene; **(f)** confirm whether `ExecutionContext` is promoted into the canonical Milestone 1 entity list or replaced by an equivalent canonical entity before invariant 17 can become satisfiable. Reviewer scopes: `hcs-architect` owns invariant numbering, cross-ADR consistency, version sequencing, and the `ExecutionContext` forward-binding gap; `hcs-policy-reviewer` owns policy-tier duplication risk and carve-out classification ambiguity; `hcs-security-reviewer` owns whether inheritance carve-outs could allow secret-bearing env, app permission, or sandbox-scope leakage into an unvalidated target context. | user | Before charter v1.3.0 is drafted; target Phase 1 synthesis closeout, 2026-05-08 | Approved — ADR 0021 accepted after reviewer blockers were addressed. The accepted text adds invariant 16 and invariant 17 only, keeps invariants 18-20 queued, narrows `inherit/include_only` to environment-materialization evidence only, and requires the future charter-edit PR to remain charter/bookkeeping-only. Charter v1.3.0 landed 2026-05-02 (wave-1: invariant text); v1.3.1 added enforcement plumbing (wave-2); v1.3.2 closed the security-reviewer-flagged gaps (wave-3). ADR 0024 records the authority for waves 2 and 3 and closes the change-policy compliance question for the v1.3 amendment cycle. Invariant text remains unchanged across all three waves. |
| Q-013 | Credential plane integration: 1Password authority surface, SDK-backed reconciler posture, brokered runtime injection, OpenTofu consumption boundaries, and machine-identity compliance. The 2026-05-05 external intake (`docs/host-capability-substrate/research/external/2026-05-05-1password-credential-plane-research.md`) plus local synthesis (`docs/host-capability-substrate/research/local/2026-05-05-credential-plane-integration-synthesis.md`) propose treating 1Password as a credential authority surface HCS models, not a secret manager HCS owns. HCS would model abstract credential-source posture, typed receipts, brokered injection, and forbidden patterns while `system-config` and Citadel-owned implementation docs own organization-specific vault inventory, aliases, manifests, and rollout docs. Sub-decisions: **(a)** does this land as Phase 2 Wave-2 / Phase 2.7 or require an ADR 0038 sequencing amendment? **(b)** is any future `CredentialSource.source_type` enum extension needed, or are existing coarse values sufficient? **(c)** is `credential_plane_mutation` a distinct operation class or sub-discriminator under `external_control_plane_mutation`? **(d)** where does reconciler manifest authority live (`system-config` or Citadel-owned implementation docs), excluding HCS core ontology? **(e)** is OpenTofu credential-plane policy part of Q-013 or a separate Q-row? **(f)** are existing charter invariants 5, 7, 8, 10, 14, 16, 17, 18, and 19 sufficient, or is a new credential-plane invariant needed? **(g)** how does 1Password bootstrap/workstation custody compose with Infisical or future machine-identity authorities? | user | Before any credential-plane schema, policy YAML, reconciler, service-account, vault inventory, OpenTofu, or runtime changes | **Q-013 accepted via ADR 0040** (proposed 2026-05-05, accepted 2026-05-05) after a four-reviewer pass with Ontology wording patches re-reviewed clean. Three-layer credential-plane pattern accepted at posture level only: authority of record (1Password or another credential authority), control-plane reconciliation (SDK-backed specification reconciler with version-pinned SDK per inv. 14), and runtime injection (`op run` / `op inject` / SSH Agent / Environments / future `$HCS_BROKER_SOCKET` per ADR 0012). HCS owns generic typed evidence, `CredentialSource` refinement, broker contracts, mutation receipts, and policy-gate posture; canonical policy YAML remains owned by `system-config`; Citadel-owned implementation docs own org-specific vault hierarchy, aliases, service-account wiring, and reconciler manifests; Jefahnierocks adopts the pattern under local authority. `CredentialSource.source_type` field name is preserved; coarse existing values (`onepassword`, `service_account`, `brokered_secret_reference`, `infisical`, `vault`) are the starting point, with no new enum values accepted by ADR 0040. Credential-plane mutations compose through ADR 0015 / ADR 0029 v2 `external_control_plane_mutation` posture; no `credential_plane_mutation` operation class is accepted. A 1Password service account is a scoped exception requiring expiry/rotation evidence, scope evidence, health evidence, auditability, and approval gates; it is not blanket permission for long-lived bearer-token automation and is not the preferred unattended machine identity when GitHub Apps, OIDC, or platform-native short-lived credentials are available. Human SSH Agent use remains a human interactive identity surface, not a machine-identity platform for unattended CI, cron, servers, or shared automation. OpenTofu may consume secret references and ephemeral reads under future accepted policy; future policy must reject ownership of SSH key lifecycle, service-account token lifecycle, and any path persisting resolved secret material in state. ADR 0038 is not amended; credential-plane implementation is a Phase 2.7 / Wave-2 candidate unless a separate sequencing amendment is accepted. No `credential_source_kind` rename, charter invariant addition, or Nash/Covenant/Citadel/subsidiary names in HCS ontology are accepted. Covenant per-entity vault isolation, scoped-token enforcement, and entity-specific audit routing remain validation posture, not live HCS controls, until issuance, audit logs, workflow rewrites, and Litecky migration proof exist. ADR 0043 later accepted and landed the first Q-013 schema/evidence slice for `CredentialAuthorityObservation`, `MachineIdentityBindingObservation`, and required subject/ref vocabulary. Further Q-013 policy, reconciler, service-account, vault, OpenTofu, runtime, broker, provider-mutation, credential-issuance, and operation-registration work remains blocked pending a follow-on accepted ADR or approved sequencing amendment. |
| Q-014 | Project-substrate contract compatibility and admission-validation model. Citadel PR #37 (`The-Nash-Group/citadel-config` commit `46c55857427af4b887194277bac2218c20b595b6`) defines a project-owned substrate contract/admission standard for CI execution and project infrastructure use of the shared substrate. The 2026-05-06 external source note (`docs/host-capability-substrate/research/external/2026-05-06-citadel-project-substrate-standard.md`) plus local synthesis (`docs/host-capability-substrate/research/local/2026-05-06-project-substrate-compatibility-synthesis.md`) map the standard to HCS concepts: `Evidence`, `CredentialSource`, `ResourceBudget`, `BoundaryObservation`, runner/check receipts, `StatusCheckSourceObservation`, machine identity, operation gating, freshness, and no-secrets invariants. ADR 0041 (`docs/host-capability-substrate/adr/0041-q-014-project-substrate-contract-validation.md`) accepted on 2026-05-06 narrows the v1 ADR: **(a)** compose with ADR 0036 by adding candidate `KnowledgeSource.source_kind: "project_substrate_contract"` plus typed validation receipts, not a parallel intake model; **(b)** treat contract lifecycle status as evidence input to a future `QualityGate.gate_kind: "project_substrate_admission"`, never gate authority alone; **(c)** treat `guardian_approval` as admission-authority evidence, not HCS `ApprovalGrant`; **(d)** group evidence shapes so v1 commits only posture for `ProjectSubstrateContractValidationReceipt`, `ProjectSubstrateAdmissionObservation`, `ProjectTeardownPlanReceipt`, and `ProjectTeardownCompletionReceipt`, while cohort 1 composes with existing ADRs and cohort 3 defers; **(e)** cite charter/ADR authority for each stop rule; **(f)** sequence implementation as Phase 2.7 / Wave-2 jointly with Q-013 unless a separate ADR 0038 amendment is accepted. | user | Before any project-substrate contract schema, validator, canonical policy YAML, adapter, dashboard, hook, runner, Proxmox, OpenTofu, machine-identity, project workload, or runtime changes | Q-014 accepted via ADR 0041 on 2026-05-06 after required reviewer pass completed with no remaining blockers. Component facts fit existing accepted lanes: Q-005 runner/check evidence, Q-006 check-source identity, Q-007 BoundaryObservation/QualityGate posture, Q-009 workspace-manifest projection, and Q-013 credential-plane posture. ADR 0044 later accepted and landed the first Q-014 schema/evidence slice for `project_substrate_contract`, `project_admission_authority`, `ProjectSubstrateContractValidationReceipt`, `ProjectSubstrateAdmissionObservation`, `ProjectTeardownPlanReceipt`, and `ProjectTeardownCompletionReceipt`. Conservative posture remains: treat project-owned contract YAML as ADR 0036 Layer 2 input and mint typed validation evidence that cites both contract source and Layer 1 operational evidence, not as HCS operating authority. No Q-014 policy, runner, Proxmox, OpenTofu, identity issuance, adapter, dashboard, hook, project provisioning, provider mutation, gate-kind behavior, or runtime behavior is authorized by this row. |
| Q-015 | Backup-readiness substrate-contract posture: typed evidence and authority pattern for upstream backup/restore surfaces (`runner-substrate` Proxmox/Synology, `hetzner` VPS restic/Storage Box, project repos) without HCS taking over live execution. The 2026-05-06 advisor directive (intake captured at `docs/host-capability-substrate/research/local/2026-05-06-q-015-backup-readiness-intake.md`, verbatim payload preserved in §Advisor directive (verbatim)) proposes (a) a `pending → configured → usable → ready` storage-class readiness lifecycle with optional `expired` and restore-drill-with-boot/service-verification as the promotion gate from `usable` to `ready`; (b) candidate entities `BackupReadinessEvidence`, `StorageClassReadiness`, `RestoreDrillEvidence`, `BackupLayerThreatModel`, `BackupCredentialCustody`, `BackupMonitoringRequirement`, `ProjectSubstrateBackupRequirement`, with Q-011 naming-suffix renames expected at ADR review (e.g., `*Evidence` → `*Receipt`/`*Observation`, standalone entities omit the suffix); (c) explicit source-ownership split keeping `runner-substrate`, `hetzner`, Citadel, `HomeNetOps`, `system-config`, and project repos owning their respective surfaces while HCS is typed evidence/contract consumer only; (d) vocabulary discipline that reachable ≠ usable (it is `configured`), succeeding ≠ ready (it is `usable`), Hetzner native Backups are rollback insurance not durable backup, Hetzner Storage Box/restic ≠ Proxmox Synology readiness, Synology NFS for `runner-substrate` is backup-only storage not VM disk storage, and backup readiness does not imply runner or project workload admission; (e) project-contract composition declaring persistent-data presence, required backup class, required restore evidence before active use, RPO/RTO expectation, data-minimization posture, teardown/retention expectations, and evidence reference/expiration; (f) composition with ADR 0036 (workspace manifest projection; backup contract YAML and threat-model docs as Layer 2 `KnowledgeSource` inputs with typed validation evidence), ADR 0040 (credential plane; `BackupCredentialCustody` composes with `CredentialSource` and broker, never resolved secret material), ADR 0041 (project-substrate contract; `ProjectSubstrateBackupRequirement` is the backup-shaped slice of admission). Sub-decisions: **(a)** does this displace ADR 0038 Phase 2.1–2.6 sequencing or land as Phase 2.7 / Wave-2 behind Q-013 and Q-014? **(b)** are advisor names retained or replaced under Q-011 naming-suffix discipline? **(c)** is `BackupReadinessObservation` (already named in the Phase 2.7 deferred-lane sequencing plan Lane Dependency Matrix) the canonical entity, or are advisor names additive? **(d)** does `StorageClassReadiness` land as standalone Ring 0 entity, evidence subtype, or as a payload on `BoundaryObservation`? **(e)** is a new `boundary_dimension: "backup_readiness"` registry entry needed, or do typed evidence subtypes suffice? **(f)** does `ProjectSubstrateBackupRequirement` extend Q-014 contract validation or stand alone? **(g)** is a new `gate_kind: "backup_readiness"` accepted, and how does it compose with `gate_kind: "project_substrate_admission"` from Q-014? **(h)** what freshness window is appropriate for restore-drill receipts and threat-model snapshots, and how does charter inv. 19 (boundary claims freshness/execution-context-bound) apply? | user | Before any backup/restore schema, registry, validator, canonical policy YAML, adapter, dashboard, hook, runner, Proxmox, Hetzner, OpenTofu, machine-identity, project workload, or runtime changes | **Q-015 accepted via ADR 0042** on 2026-05-07 by human approval. Posture accepted first: HCS models backup/readiness as a typed evidence and contract-consumption lane while preserving upstream ownership. ADR 0045 later accepted and landed the first Q-015 schema/evidence slice for `BackupReadinessObservation`, `RestoreDrillReceipt`, `BackupCredentialCustodyObservation`, `ProjectSubstrateBackupRequirementObservation`, generic `KnowledgeSource.source_kind: "threat_model"`, and `KnowledgeSource.schema_version` `0.2.0`. Post-landing hardening in commit `ab38dc8` tightened proof-bearing nested evidence refs and generated JSON Schema guards; regression scaffold #58 landed in commit `1ae3bac`. No Q-015 policy, validator, runner, Proxmox, Hetzner, OpenTofu, machine-identity issuance, project workload, backup/restore execution, provider mutation, gate-kind behavior, adapter, dashboard, hook, or runtime change is authorized by this row; no upstream repo edits are authorized; no live infrastructure or secret actions are performed by this acceptance. Follow-on implementation reviewer dispatch remains hcs-architect, hcs-ontology-reviewer, hcs-policy-reviewer, hcs-security-reviewer, matching the Q-013/Q-014 pattern. |

Implementation ADR note: ADR 0043
(`docs/host-capability-substrate/adr/0043-q-013-credential-plane-implementation.md`)
was accepted on 2026-05-07 as the first Phase 2.7 Q-013 implementation slice
after reviewer pass and human approval. It authorizes schema/evidence work
only for `CredentialAuthorityObservation`, `MachineIdentityBindingObservation`,
and the minimum subject/ref vocabulary those records require. The v1 schema
slice landed on 2026-05-07, including `Evidence.schema_version` `0.9.0` and
`subject_kind: "machine_identity"`. It does not authorize canonical policy
YAML, broker/runtime behavior, reconciler code, service-account creation,
vault inventory mutation, OpenTofu changes, provider mutation, operation
registration, `CredentialRuntimeInjectionReceipt`,
`CredentialReconcilerReceipt`, `CredentialIssuanceReceipt`, or
`RemoteMutationReceipt`.

Implementation ADR note: ADR 0044
(`docs/host-capability-substrate/adr/0044-q-014-project-substrate-implementation.md`)
was accepted on 2026-05-07 as the first Phase 2.7 Q-014 implementation slice
after four-reviewer pass and human approval. It authorizes only the narrow
schema/evidence slice for `project_substrate_contract`,
`project_admission_authority`, `ProjectSubstrateContractValidationReceipt`,
`ProjectSubstrateAdmissionObservation`, `ProjectTeardownPlanReceipt`,
`ProjectTeardownCompletionReceipt`, and required subject/ref vocabulary. It
landed on 2026-05-07 as schema source, generated JSON Schema,
ontology/registry docs, and focused tests. It does not authorize canonical
policy YAML, runtime/live validators, adapters,
dashboard routes, hooks, runner registration, GitHub runner-group mutation,
Proxmox changes, OpenTofu changes, machine-identity issuance, provider
mutation, operation registration, project workload provisioning, backup
readiness, `QualityGate.gate_kind: "project_substrate_admission"`,
`ApprovalGrant.scope`, `allowed_for_gate`, or runtime behavior.

Implementation ADR note: ADR 0045
(`docs/host-capability-substrate/adr/0045-q-015-backup-readiness-implementation.md`)
was accepted on 2026-05-07 after four-reviewer pass and human approval. It
authorizes only the narrow schema/evidence slice for
`BackupReadinessObservation`, `RestoreDrillReceipt`,
`BackupCredentialCustodyObservation`,
`ProjectSubstrateBackupRequirementObservation`, and generic
`KnowledgeSource.source_kind: "threat_model"`. That slice landed on
2026-05-07 as schema source, generated JSON Schema, ontology/registry docs,
and focused tests. It reuses existing `Evidence.subject_kind` values and does
not bump `Evidence.schema_version`; it does bump `KnowledgeSource` to
`schema_version: "0.2.0"` for the `threat_model` source-kind extension. The
post-landing reviewer cleanup in commit `ab38dc8` tightened Q-015
proof-bearing nested evidence refs, preserved generated JSON Schema
conditional guards, and added negative tests for sandbox/missing-freshness
restore refs. Regression scaffold #58
(`packages/evals/regression/backup-readiness-generic-restore-ref-promotion.md`)
landed in commit `1ae3bac`. It does not authorize backup/restore execution,
runtime validators, canonical policy YAML, adapters, dashboard routes, hooks,
runner registration, Proxmox changes, Hetzner changes, OpenTofu changes,
machine-identity issuance, project workload provisioning, provider mutation,
gate-kind behavior, `ApprovalGrant.scope`, `allowed_for_gate`, or runtime
behavior.

Compatibility intake note: Citadel follow-up guidance delivered 2026-05-07 is
recorded at
`docs/host-capability-substrate/research/local/2026-05-07-citadel-readiness-compatibility-followup.md`.
It is future-policy/schema input only: runner readiness remains pending
structured evidence, alert delivery is a future readiness-evidence input,
file-level/meta-backup remains distinct from VM/CT image backup, and
cross-repo receipts remain generic source/evidence references. This note does
not authorize live readiness claims, schema/ontology additions, host-local
policy, provider mutation, upstream repo edits, or runtime behavior.

## Accepted

| ID | Decision | Rationale | ADR | Date |
|----|----------|-----------|-----|------|
| D-001 | HCS is a horizontal substrate with kernel + protocol adapters; MCP is one adapter, not the architecture. | Protocol-agnostic kernel ages better as GPT/Claude/Gemini/MCP/A2A evolve. | ADR 0001 | 2026-04-22 |
| D-002 | Runtime: Node LTS first. Bun only with measured evidence. | Always-on service prioritizes ecosystem maturity, OTEL, SQLite library stability. | ADR 0002 | 2026-04-22 |
| D-003 | Storage: SQLite with WAL; single writer, snapshot-isolation readers. | Matches workload shape. | ADR 0004 | 2026-04-22 |
| D-004 | Policy canonical location: `system-config/policies/host-capability-substrate/`. HCS repo vendors a snapshot for tests. | Governance-adjacent; consistent across hosts; review under existing `policies/` process. | ADR 0006 | 2026-04-22 |
| D-005 | Hook call pattern: blocking local RPC, 50ms target, cache fallback. Writes fail-closed when classifiable as mutating/destructive; reads warn-and-allow on timeout. | Blocking is simplest; cache prevents SPOF; 50ms is the perceptible-latency threshold. | ADR 0007 | 2026-04-22 |
| D-006 | Claude hooks: command hook for hard decisions (fail-closed), HTTP hook for advisory/telemetry. Bodies stay thin. | Command hooks run with user permissions; HTTP hook failures are non-blocking per Claude Code docs. | ADR 0007 | 2026-04-22 |
| D-007 | Codex hooks: advisory guardrails, not the hard enforcement boundary (Bash-only coverage). | Current Codex hook coverage is incomplete; substrate-side policy is the real boundary. | ADR 0007 | 2026-04-22 |
| D-008 | OPA adoption trigger: migrate when ≥2 rules need boolean composition across principal + host + workspace + operation + target + time + prior grants. | Concrete + observable; avoids premature engine adoption. | (pending ADR) | 2026-04-22 |
| D-009 | GPT-5.4 remote MCP hosting: localhost-only in Phase 0/1 (stdio + Streamable HTTP bound to 127.0.0.1). Remote tunnel is a separate ADR. | MCP Streamable HTTP guidance says local servers bind localhost with auth; build posture before exposing. | ADR 0003 | 2026-04-22 |
| D-010 | Audit access: both direct read-only SQLite/DuckDB AND server-side query endpoints. | SQL is right for post-hoc investigation; agents should not discover audit schema ad hoc. | (pending ADR) | 2026-04-22 |
| D-011 | Tier file owner-of-record: human (user) owns; agents may draft; `hcs-policy-reviewer` subagent must file objections before merge. | Tier classification is judgment work; human loop kept light by the reviewer subagent. | (pending ADR) | 2026-04-22 |
| D-012 | MCP primitives: tools for live calls; resources for cached snapshots/evidence/policy/audit views; prompts for user-invoked workflows. | Matches MCP's primitive hierarchy; keeps surface idiomatic for every host. | ADR 0010 | 2026-04-22 |
| D-013 | A2A facade: deferred to Month 6+ unless a concrete cross-agent delegation flow that cannot be represented as MCP tools/resources/prompts emerges. | Complementary to MCP; HCS must first be a reliable tool/context substrate. | (pending ADR) | 2026-04-22 |
| D-014 | Repo: dedicated target repo (name pending user choice). | Substrate cadence differs from project code. | ADR 0001 | 2026-04-22 |
| D-015 | Dashboard is part of first slice (read-only). View-model contracts defined before kernel internals. | Human visibility is first-class; kernel output must be dashboard-renderable from day 1. | ADR 0008 | 2026-04-22 |
| D-016 | Execute lane ships only after approval grants + audit hash chain + dashboard review + lease manager all exist. | Defer the dangerous surface until the full safety stack is live. | (pending ADR) | 2026-04-22 |
| D-018 | Public source, private deployment boundary. Repo contains source/schemas/fixtures/docs; live policy, runtime state, audit archives, and tokens live outside the repo. | Transparency of structure without leaking authority over live host. | ADR 0011 | 2026-04-22 |
| D-019 | Skills canonical location is `.agents/skills/` (cross-tool). `.claude/skills/` is Claude-specific wrappers only, empty at Phase 0a. | Windsurf and Codex honor `.agents/skills/`; Claude wrappers added only when needed. Avoids skill drift. | (pending ADR) | 2026-04-22 |
| D-020 | Runtime state at `~/Library/Application Support/host-capability-substrate/`; logs at `~/Library/Logs/host-capability-substrate/`; LaunchAgent label `com.jefahnierocks.host-capability-substrate`. | macOS-native; matches OS conventions; reverse-DNS chosen for uniqueness. | ADR 0005 | 2026-04-22 |
| D-021 | Six project-scoped subagents including `hcs-ontology-reviewer`. All default Opus 4.7. No Bash in any review subagent's tool list. | Schema/ontology drift is load-bearing; independent reviewer is warranted. | (pending ADR) | 2026-04-22 |
| D-022 | Original early-phase tool baseline recorded internal/build-shaped identifiers for Claude Code and Codex. This row is amended by D-029, which makes public CLI semver the authoritative baseline and tracks app build identifiers separately. | Stabilize behavior during scaffolding + first slice; preserve the original observation while correcting the version-string authority. | (amended by D-029) | 2026-04-22 |
| D-023 | No `WARP.md` in Phase 0a. Warp prioritizes `WARP.md` over `AGENTS.md`; adding early risks policy fork. | Let Warp consume `AGENTS.md` first; add pointer-only `WARP.md` post-Phase-0b only if measurement shows a gap. | (pending ADR) | 2026-04-22 |
| D-024 | Current repo-side Phase 0b soak window is April 23, 2026 through April 25, 2026, with closeout on April 26, 2026. If the window does not produce a clean go/no-go, extend it rather than weakening the gate. | Keeps the evidence loop tight while preserving honest failure states. This is an execution-cadence decision, not a charter or ontology change. | (plan-only) | 2026-04-23 |
| D-025 | Substrate cleanup classifier must distinguish derivable-from-source from load-bearing state. Gitignore state is not sufficient deletion authority. `OperationShape` for cleanup carries a `deletion_authority_source` field that must resolve to a non-gitignore authority. | Day-1 Codex p5 incident: `rm -rf .logs` against active 28MB soak partition because `.logs/` is gitignored. Sandbox held 498s; user aborted. A host-scoped substrate pre-loading `.logs/` as load-bearing soak state would have prevented this without depending on agent vigilance. | (pending ADR → charter v1.2.0 invariant 13) | 2026-04-23 |
| D-026 | Config-spec authority hierarchy for runtime-config claims: `observed_runtime + matching_changelog > static_docs > published_schema`. Config-spec assertions must carry `{source_url, observed_at, installed_version, authority_order}` provenance. Generalizes charter invariant 2 from command syntax to config schema. | 2026-04-23 harness drift incident: Claude Code 2.1.119 refused startup on `"verbose": "true"` (string). Anthropic's settings page, changelog, and SchemaStore disagreed on where the key lives and what type it takes; the changelog + installed runtime matched, the docs page did not. | (pending ADR → charter v1.2.0 invariant 14) | 2026-04-23 |
| D-027 | Pre-launch host-config type validation (e.g., JSON-boolean checks on `~/.claude/settings.json`, `~/.claude.json`) lives at the chezmoi / host-hygiene layer, not inside HCS rings. HCS does not own harness-startup lint. | Ring-boundary decision. The failure class is bootstrap-level — if the harness cannot start, nothing below it can help. Hygiene lives adjacent to `system-update`; HCS focuses on post-startup substrate behavior. | (plan-only) | 2026-04-23 |
| D-028 | Phase 0b proximate credential plane defines the `host_secret_*` shell contract (`host_secret_read`, `host_secret_export`, `use_host_secrets`, `host_secret_diag`) and the `HCS_SECRET_*` env-var namespace (`HCS_SECRET_ACCOUNT`, `HCS_SECRET_TIMEOUT`, `HCS_BROKER_SOCKET`) that the eventual substrate broker must implement. Backend today is direct `timeout "$HCS_SECRET_TIMEOUT" op read`; backend post-broker is the same callers speaking to `$HCS_BROKER_SOCKET`. System-config hardening only; no HCS code change at this decision. Ships in `~/.config/direnv/direnvrc` and five `~/.local/bin/mcp-*-server` wrappers. | 1Password IPC queue deadlock (field report `2026-04-23-op-ipc-queue-deadlock.md`) reproduced within 24 hours of first documentation. `op` 2.32.1 has no client-side timeout flag or env var; wrapper-side `timeout` is the only option for consumers that need bounded wall-clock. A stable interface shipped now is the migration path for broker adoption without caller churn. The substrate's Requirements 1 (centralized broker), 2 (consumer timeouts), and 3 (diagnostic surface) from the field report are met by name/contract today and by implementation when the broker lands. | (pending ADR; will be superseded when broker ADR for `HCS_BROKER_SOCKET` is accepted) | 2026-04-24 |
| D-029 | Tool baseline authority uses public CLI semver strings; app build identifiers are separate facts. Current closeout baseline: Claude Code CLI `2.1.120`, Claude macOS app `1.4758.0 (fb266c)` dated `2026-04-24T20:22:30.000Z`, Codex CLI `0.125.0`, Codex macOS app `26.422.30944 (2080)`, model posture GPT-5.5/GPT-5.4-compatible HCS profiles + Opus 4.7. | Phase 0b proved the prior D-022 identifiers mixed internal/app build shape with CLI `--version` output. Tool and config claims now follow charter v1.2.0 invariant 14's authority order. | charter v1.2.0 | 2026-04-26 |
| D-030 | HTTP MCP servers should prefer OAuth/resource-metadata flows when available, but HCS-managed HTTP MCP entries remain `enabled = false` by default and require explicit opt-in. Profile selection does not gate or imply HTTP MCP availability. | OAuth/resource metadata is the right direction for HTTP MCP, but enabling remote/authenticated control-plane access by default would exceed Phase 0/1 posture. Explicit opt-in keeps the live surface bounded. | ADR 0015 | 2026-04-26 |
| D-031 | Codex profiles are CLI-only opt-ins for HCS planning/implementation/review posture. HCS does not treat `[profiles.hcs-*]` as a cross-surface identity, auth, policy, or app/IDE configuration mechanism. | Codex app and IDE behavior diverge from CLI behavior, and documentation/research show profiles are not a portable cross-surface contract. Direct tests remain useful, but HCS architecture must not depend on profiles applying outside CLI invocation. | charter v1.2.0 | 2026-04-26 |
| D-032 | HCS treats external APIs as typed, evidence-producing control planes, not shell-string targets. Provider object IDs, public client IDs, secret material, Access app AUDs, tunnel audience allowlists, origin reachability receipts, policy selector values, and `SecretReference` values are distinct schema concepts. `HTTP 429` and recent MCP fan-out are cooldown evidence, not retry prompts. Cloudflare Access auth success and tunnel/origin validation success are separate authority layers; ADR 0015 is the master decision. | Cloudflare Stage 3a, tunnel-audience, and MCP fan-out incidents showed that external mutations need minimal-request plans, budget-gated optional checks, one-time-secret capture-at-source, provider-specific shape validation, and origin/tunnel evidence before more provider-side policy mutation. | ADR 0015 | 2026-04-26 |
| D-033 | HCS shared memory is typed evidence + coordination state + derived retrieval index, not agent memory. Canonical names: "evidence and coordination store," "shared state," "knowledge index," "coordination fact," "derived summary." Forbidden phrasings: "memory," "agent memory," "shared memory," "LLM memory," "persistent memory," "long-term memory," "context memory." | Naming discipline accepted as part of Q-003 / ADR 0019 v3 on 2026-05-03 and reserved by that row's acceptance text for a "next decisions-sweep PR (NOT the W3 closeout batch)." Calling the layered store "memory" mis-frames the safety contract: gates consume typed evidence (`Evidence`, `CoordinationFact` with `allowed_for_gate: true`), never `KnowledgeChunk` retrieval results or `DerivedSummary` records. | ADR 0019 | 2026-05-07 |
| D-034 | Repo GitHub slug: `jefahnierocks/host-capability-substrate`. Repository name (`host-capability-substrate`), local path (`~/Organizations/jefahnierocks/host-capability-substrate/`), workspace boundary, ring boundaries, and `README.md`-declared "Owned by **jefahnierocks**" ownership semantics are unchanged; only the GitHub-side slug moves. Schema test fixtures at `packages/schemas/tests/source-control-receipts.test.ts:96,123,514` already canonicalize the new slug; no test changes required. Replaces the GitHub-slug portion of D-017 (moved to **Reversed**). | GitHub UI ownership transfer from personal `verlyn13` account to a dedicated `jefahnierocks` GitHub organization completed 2026-05-08. Aligns GitHub-side ownership with the long-standing in-repo `README.md` §Ownership declaration; matches sibling `system-config` migration trajectory (handled in that repo's own scope). Frozen evidence (`packages/fixtures/provenance-snapshot-2026-04-30.json`, `.logs/phase-0/`, dated `docs/host-capability-substrate/research/` notes, archived Codex/Claude transcripts) intentionally retains the legacy `verlyn13/...` slug as observed-at-date provenance per AGENTS.md §Hard boundaries `observed_at` rule and charter v1.4.0 inv. 10 + inv. 19. GitHub-side bypass-actor allowlists were cleared by the standard repository transfer (rule definitions survive; actor identities do not, since they were scoped to the previous owner); re-population against `jefahnierocks` org membership is a provider-side follow-up not authorized by this row per charter v1.4.0 inv. 16 (external-control-plane evidence-first) and the substrate-scope/provider-scope split codified in the 2026-05-08 project-secrets directive §Naming Model. The master ADR at `~/Organizations/jefahnierocks/system-config/docs/host-capability-substrate/0001-repo-boundary-decision.md` must be updated in a `system-config` session; `README.md:13` link to `system-config` will follow that sibling-repo move so it does not run through a redirect chain. `hcs-architect` review returned a clean pass with four non-blocking observations folded in at acceptance: inline charter-invariant citations on the §Accepts frozen-evidence bullet, explicit invariant naming on the §Cross-scope follow-ups provider-mutation bullet, a §Rejects parenthetical pointer to the §Cross-scope follow-ups README-link bullet, and `DECISIONS.md` line-number pinning in §References. | ADR 0046 | 2026-05-08 |
| D-035 | `system.cleanup.plan.v1` consumes the `system.workspace.diagnose.v1` summary as an opt-in `discovery_hint_summary_id` only, with full re-derivation of deletion-authority source refs at plan-construction time and gateway re-walk at class-I consumption time. The cleanup-plan operation is `operation_class: cleanup_plan` (NEW) with `mutation_scope: "none"` and target-kind narrowing to `workspace`. Output is a population of typed `OperationShape` records (no reified composite entity) plus a mandatory `DerivedSummary` of `summary_kind: cleanup_plan` (NEW) carrying hint-resolution status as a typed `summary_text` closed-enum annotation (`hint_resolved | hint_ignored_stale | hint_ignored_workspace_mismatch | hint_unresolvable | no_hint_provided`). The summary's `derived_from` cites underlying typed Layer 1 `Evidence` records (NOT the produced `OperationShape` records, which are not in the `derivedSummarySourceRecordKindSchema` four-class closure per inv. 18). Initial `cleanup_scope` enum: `audit_profile_claim_supersession | worktree_lease_completed`; `explicit_target_set` deferred behind redaction-posture work. NEW `Decision.reason_kind`: `cleanup_plan_authority_source_stale`, `cleanup_plan_target_under_active_lease`. Resolves ADR 0036 §Future amendments architect F4. | Charter v1.4.0 inv. 7 binds non-`none` `mutation_scope` to the execute-lane stack; cleanup-plan is read-only by design (produces typed records, does not execute deletions). Inv. 18 chain-walk rejection requires re-derivation through typed evidence path — the discovery hint is consumed for path enumeration only, never as authority. Inv. 19 freshness binding requires gateway re-walk at class-I consumption rather than implicit freshness inheritance from plan-time mint. The opt-in hint pattern composes naturally with `system.workspace.diagnose.v1` while preserving structural inability at the input layer to inherit `DerivedSummary` authority (no authority-bearing summary input field). All three required reviewers (`hcs-architect`, `hcs-ontology-reviewer`, `hcs-policy-reviewer`) returned 11 v1-blocking items resolved in v2 and ready-for-acceptance verdicts on v2; two mechanical tweaks at acceptance (`worktree_lease_completed` enumeration extended to all three terminal-non-active `lease_state` values per ADR 0031 v1; §Future amendments registry-PR bullet expanded to enumerate the typed `summary_text` hint-status closed-enum vocabulary). Schema PR per `.agents/skills/hcs-schema-change` follows; canonical policy YAML for `cleanup_plan` operation_class lands in `system-config/policies/host-capability-substrate/` at Milestone 2 per ADR 0036 reservation. | ADR 0047 | 2026-05-09 |
| D-036 | Phase 2.7 subject-kind grounding evaluation. No `CoordinationFact.subject_kind` enum addition for `machine_identity`, `project_substrate_contract`, or backup-readiness subject_kinds; per-candidate classification dispositions recorded for any future schema PR that promotes a candidate. **Disposition for `machine_identity` (Q-013):** host-observation-backed; joins §Subject-kinds inheriting ADR 0019 v3's chain-promotion rule only when promoted; per-record `authority` requirements jointly enforced by ADR 0019 v3 chain-promotion (rejects `sandbox-observation`) + charter v1.4.0 inv. 18 chain-walk (rejects `self-asserted`); promotion requires `evidence_refs` to include `CredentialAuthorityObservation` or `MachineIdentityBindingObservation` with `authority: "host-observation"` (or `"provider-asserted-kernel-verifiable"` per inv. 16). **Disposition for `project_substrate_contract` (Q-014):** derived/Layer-2-backed; joins §Subject-kinds subject to the grounding requirement list when promoted; promotion requires at least one host-observation (or provider-asserted-kernel-verifiable) `Evidence` record beyond the source contract YAML and structural validation — typed `ProjectSubstrateContractValidationReceipt` with kernel-set producer or `BoundaryObservation` of `boundary_dimension: project_admission_authority`. **Disposition for backup-readiness subject_kinds (Q-015):** mixed/declarative; standard host-observation-grounding rule applies to all promotions; additional lifecycle-state rule when the object payload asserts `state: ready` requires freshness-valid `RestoreDrillReceipt` with boot/service verification per ADR 0042/0045 (other states `pending`/`configured`/`usable`/`expired` need only the standard rule). Cross-context binding (inv. 19 + registry §Cross-context enforcement layer) inherited unchanged for all three. Default for future Phase 2.x+ subject_kind candidates: no enum addition unless a concrete coordination need surfaces. ADR 0036 §Future amendments §Layer 1 grounding rule extensibility principle now fully discharged. | Phase 2.7 schema slices (ADRs 0043/0044/0045) added the candidates as `Evidence.subject_kind` values; the §Subject-kind grounding requirement governs `CoordinationFact.subject_kind` (a distinct enum with nine values, none of the three Phase 2.7 candidates present). No observed coordination need has surfaced for any of the three; YAGNI + charter inv. 1 (canonical-typed-evidence) argue against premature enum widening. Per-candidate dispositions are recorded so that future schema PRs that promote a candidate can cite the recorded classification as a starting point and re-validate at acceptance time per the registry §Procedure for adding a new subject_kind value rule. All four required reviewers (`hcs-architect`, `hcs-ontology-reviewer`, `hcs-policy-reviewer`, `hcs-security-reviewer`) returned no blocking objections on v1 with convergent non-blocking observations on per-record vs subject-kind classification phrasing, cross-context binding inheritance, registry table cell specificity, and lifecycle-state-rule scoping; four mechanical tweaks at acceptance address them. The 2026-05-09 outstanding-work sequencing workflow named ADR 0048 as the evaluation per its §Step 1; canonical policy YAML in `system-config` is the next substantive lane (workflow §Step 2). | ADR 0048 | 2026-05-09 |
| D-038 | `WorkspaceContext` Ring 0 entity introduced as the second foundational-entity ADR per the 2026-05-10 workflow-sequencing investigation §Step 1 (entity #2 of 5; Decision was #1 via ADR 0049 / D-037). 11 schema fields: `workspace_context_id`, `execution_context_id` (kernel-set per ADR 0031 v1 Mechanical Tweak #8 / Security-C; Layer 1 mint API enforces `WorkspaceContext.execution_context_id == Session.execution_context_id` when leases are acquired), `repository_id` (producer-asserted, kernel-verifiable per ADR 0031 v1 §Authority discipline), `worktree_path` (producer-asserted, kernel-verifiable per Mechanical Tweak #2 / Security-D canonicalization), `workspace_context_state` (`'active' | 'retired'`), `kernel_observed_at`, `valid_until` (nullable), `producer` (`workspaceContextProducerSchema = z.enum(['kernel_workspace_diagnose'])`; named enum schema for forward-compatible allowlist widening), `audit_chain_link_hash` (`sha256DigestSchema` covering canonical concatenation of 9 kernel-set/kernel-verifiable fields + `prior_audit_chain_link_hash` for chain integrity; `'GENESIS'` sentinel mirrors ADR 0049), `evidence_refs` (kernel-set producer evidence; supersession-via-evidence_refs pattern mirrors ADR 0049 Decision), `schema_version`. **Envelope-level kernel-set with two field-level exceptions** authority discipline framing per ADR 0031 v1: `repository_id` + `worktree_path` are producer-asserted, kernel-verifiable; all other fields kernel-set. **Cardinality discipline**: `(repository_id, canonical(worktree_path), workspace_context_state == 'active')` is unique; mint of new active WorkspaceContext for already-bound worktree retires prior atomically at Layer 1 mint API (mirrors ADR 0031 v1 Mechanical Tweak #6 / Security-G). **Lifecycle-transition rule**: NEW WorkspaceContext record on execution_context_id change; prior cited via `evidence_refs` (no separate typed `prior_workspace_context_id` FK); prior remains immutable. **CoordinationFact FK-staleness**: existing `subject_ref.workspace_context_id` references to retired WorkspaceContext continue to satisfy schema validation; Ring 1 mint-API + gateway re-derive responsible for re-resolution at promotion time. Closes M1 acceptance criterion #2 (`WorkspaceContext` in canonical 22-entity list). Registry v0.4.12 → v0.4.13 with 5 specific section additions/updates (§WorkspaceContext entity, §Phase 2.4 consolidation update, §Producer-vs-kernel-set authority fields envelope-level-kernel-set framing, §Kernel-trusted producer allowlist scope extension for `kernel_workspace_diagnose`, §Cross-context enforcement layer cross-reference to ADR 0031 v1 §Cross-context binding rules). `kernel_workspace_diagnose` producer scope extended from "diagnostic outputs and manifest projections" to also include `WorkspaceContext` identity records. | Foundational Ring 0 entity that resolves FK references currently dangling in `CoordinationFact.subjectRefSchemas.workspace_context`, `VerificationCommandSpec.workspace_context_id`, and `audit_profile_snapshot.workspace_context_id` (per ADR 0036). ADR 0031 v1 commits 1:1 cardinality with worktree + cross-context-equality enforcement on `WorkspaceContext.execution_context_id` (Mechanical Tweak #8). ADR 0036 §Layer 1 grounding requirement governs `subject_kind: workspace_context` records. ADR 0037 §Out of scope reserves the AgentClient × WorkspaceContext cardinality question. Three-revision cycle: v1 returned 8 blocking from architect + ontology including critical contradiction with ADR 0031 v1; v2 absorbed all 8 plus added `hcs-security-reviewer`; v2 returned 4 blocking + 5 non-blocking (lines 149/182 stale framing convergent across architect + ontology + security; lifecycle FK staleness; producer shape divergence; internal contradiction on inv. 19); v3 absorbed all 4 blockers + cardinality clarification + 3 implementation-detail acknowledgments. v3 returned ready-for-acceptance from architect + ontology with no new blockers; security gave ready-for-acceptance on v2 pending lines 149/182 tweak now applied. Schema PR per `.agents/skills/hcs-schema-change` follows in separate session. **Step 1 progress**: entity #2 of 5 complete (Decision ADR 0049 / D-037 + WorkspaceContext ADR 0050 / D-038); next is entity #3 ApprovalGrant (ADR 0051; will mirror D-037 producer-disjointness rule on the ApprovalGrant side). Then ADR 0052 Lease, ADR 0053 Run. | ADR 0050 | 2026-05-10 |
| D-041 | `Run` Ring 0 entity introduced as the fifth and **final** foundational-entity ADR per the 2026-05-10 workflow-sequencing investigation §Step 1 (entity #5 of 5; Decision was #1 via ADR 0049 / D-037; WorkspaceContext was #2 via ADR 0050 / D-038; ApprovalGrant was #3 via ADR 0051 / D-039; Lease was #4 via ADR 0052 / D-040; **Run is #5 and completes Step 1**). 13 envelope-level schema fields, **all kernel-set** (envelope-only-kernel-set with NO field-level exceptions; cleaner than Lease's mixed split per ADR 0031 v1; Run is purely kernel-observed because it records the kernel's view of what happened, not what the producer asserts): `schema_version` (`runSchemaVersionSchema = z.literal('0.1.0')` — fifth sibling co-commitment after decision/workspaceContext/approvalGrant/lease, may land as single coordinated schema PR), `run_id` (closes long-pending typed FK target for `Evidence.run_id` referenced by 12 Phase 2 evidence subtypes per MT-3), `run_kind` (`runKindSchema = z.enum(['operation_execution'])` — single-value v1 Zod enum; `system_task` and `diagnostic` are registry-canonical reservations pending future schema PRs via §Procedure rule), `scope` (discriminated union on `run_kind`; operation_execution branch carries `operation_shape_ref` + `authorizing_decision_id` typed FKs), `invoker_session_id` (kernel-set FK to Session), `invoker_agent_client_id` (kernel-set FK to AgentClient), `recorded_by` (`runProducerSchema = z.enum(['mint_api', 'kernel_broker'])` — `kernel_gateway` excluded by design; gateway re-derive does not record Runs), `started_at`, `ended_at` (nullable until terminal), `execution_context_id` (kernel-set per inv. 17 + §Forbidden patterns clause v1.3.1 line 138 Run-execution-context-traceability rule), `run_state` (`runStateSchema = z.enum(['active', 'succeeded', 'failed', 'aborted', 'timeout'])` — 5 values, 1 active + 4 terminal), `audit_chain_link_hash` (`sha256DigestSchema` with **length-prefix-encoded canonical-concatenation** per ADR 0051 v4 retroactive posture rule covering ADR 0049/0050/0051/0052/0053 jointly), `evidence_refs` (chain-aware refs via `runSchema` envelope-level superRefine that walks UNCONDITIONALLY — no run_state-style gating since Run records carry execution-receipt authority at all states). **Authorizing-Decision requirement**: every Run cites `authorizing_decision_id` typed FK to a Decision with `outcome: 'allow'`; Decisions with deny/informational outcomes cannot authorize Runs (Layer 1 mint API verifies; rejection emits `run_authorizing_decision_unresolvable`). **Cross-context binding triple equality** (charter inv. 19): `Run.execution_context_id == invoker_session.execution_context_id == authorizing_decision.execution_context_id` (Layer 1 mint API enforces; rejection emits `run_invoker_session_mismatch`). **Charter §Forbidden patterns clause v1.3.1 line 138 (operationalizing inv. 17)** structurally defended: kernel-set `execution_context_id` + Ring 1 mint API resolution verification + `run_execution_context_unresolvable` reason_kind (MT-1 at acceptance: corrected from the v1 draft's "inv. 13" misidentification across 6+ citation sites — invariant 13 is "Deletion authority is not gitignore state"; the Run-execution-context-traceability rule operationalizes invariant 17). **Charter inv. 18 (Run forbidden in DerivedSummary.derived_from)** preserved unchanged. **D-037 producer-disjointness additive cross-step extension** to Run-record-vs-authorizing-Decision (third extension after Decision↔ApprovalGrant per ADR 0051 v4 and Lease-acquire↔authorizing-Decision per ADR 0052); producer equality by class identity, not delegation; walk-depth ≤ 64 + cycle-rejection via `audit_chain_corruption_detected`. v1 forced-binary 3-pair enumeration (MT-5 at acceptance per security B-1): gateway-decided Decisions inherit gateway re-derive non-escalability; mint_api-decided + mint_api-recorded rejects when chain-related; kernel_broker-decided + kernel_broker-recorded rejects when chain-related symmetrically. **Charter inv. 6 forbidden-tier non-escalability** preserved structurally upstream at `OperationShape.operation_class` source-enum closure + canonical policy YAML (no entity-level tier_scope structural defense — ADR 0051 v4 scope-back pattern preemptively applied); `operation_class_scope` documentation-only column. v1 `operation_execution` → all 8 operation_class values (operation-class-polymorphic; `mutation_scope: 'none'` operations produce execution receipts without host mutation). **Lifecycle (5 typed states; supersession-via-evidence_refs)**: `null → active`, `active → succeeded | failed | aborted | timeout`. Each transition emits typed Decision via Layer 1 mint API; mid-run terminal-state mutation rejected via `run_terminal_state_mutation_attempt`. **ONE schema-level refinement accepted** (the only one in any foundational-entity ADR): Zod superRefine on `ended_at == null || ended_at >= started_at` (NOT cross-record; both fields on same Run record); rejection emits `run_started_at_after_ended_at`. **5 NEW Decision.reason_kind reservations** (all deny-only): `run_execution_context_unresolvable`, `run_authorizing_decision_unresolvable`, `run_invoker_session_mismatch`, `run_terminal_state_mutation_attempt`, `run_started_at_after_ended_at`. Extends §Decision.reason_kind status table from 27 (after ADR 0052) to 32 total reservations. **`Evidence.subject_kind: 'run'` already Zod-defined** at `evidence.ts:39` (MT-2 at acceptance: corrected from v1 draft's stale `:43` citation; verified `:43` is actually `'secret_reference'`). **NEW §Procedure rule** (9 steps; MT-4 at acceptance split step 3 into 3a typed scope shape + 3b envelope-level superRefine chain-walk extension commitment per ADR 0052 MT-4 forward-look discipline) for new run_kind values; includes authorizing-Decision requirement + invoker requirement + strengthened sandbox-execution rule per ADR 0052 MT-5 + terminal-state set declarations. **One-revision cycle (continuation of ADR 0052's smoothest-cycle pattern)**: policy + security returned ready-for-acceptance on v1 outright; architect returned 3 mechanical-text-only blocking + 7 non-blocking; ontology returned 4 mechanical-text-only blocking + 6 non-blocking — all blocking items were factual-citation corrections (inv. 13 misnumbering, evidence subtype enumeration undercount by 4 missing subtypes, `'run'` line 43 → 39, §Procedure step 3 split). All blocking + 21 cosmetic items absorbed as mechanical tweaks at acceptance (MT-1 through MT-5 plus consolidated cosmetic items). **Workflow-sequencing investigation §Step 1 COMPLETE**: with ADR 0053 accepted, all 5 foundational Ring 0 entities are landed. Substrate unblocked for Step 2 Phase 2.5 canonical policy YAML in system-config (parallel-OK), Step 3 less-critical M1 entities batched, Step 4 Phase 3 Ring 1 services starting at `packages/kernel/`, Step 5 Ring 2 adapters, Step 6 Ring 3 regression runner. **Cycle-time progression** (preemptive-absorption strategy validated continuation): ADR 0049 = 2 revisions, ADR 0050 = 3 revisions, ADR 0051 = 4 revisions, ADR 0052 = 1 revision, ADR 0053 = 1 revision. Registry v0.4.15 → v0.4.16 with 11 specific section additions/updates pending separate docs commit (after ADR 0049/0050/0051/0052 registry commits land in sequence). Closes M1 acceptance criterion #17 (`Run` in canonical 22-entity list). Schema PR per `.agents/skills/hcs-schema-change` follows separately; the 5 sibling-co-commitment schemaVersion literals (`decisionSchemaVersionSchema`, `workspaceContextSchemaVersionSchema`, `approvalGrantSchemaVersionSchema`, `leaseSchemaVersionSchema`, `runSchemaVersionSchema`) may land as a single coordinated schema PR at workflow-sequencing investigation §Step 4 boundary. | Foundational Ring 0 entity completing the four-leg execute-lane stack (Decision + ApprovalGrant + Lease + Run); closes the long-pending typed FK target for `Evidence.run_id` referenced by 12 Phase 2 evidence subtypes; closes the charter §Forbidden patterns clause v1.3.1 line 138 Run-execution-context-traceability structural defense; closes charter inv. 7 execute-lane discipline typing. Per the workflow-sequencing investigation, with Run accepted, the substrate enters Phase 3 (Ring 1 services starting at `packages/kernel/`). **Step 1 progress: entity #5 of 5 COMPLETE**. | ADR 0053 | 2026-05-10 |
| D-040 | `Lease` Ring 0 entity introduced as the fourth foundational-entity ADR per the 2026-05-10 workflow-sequencing investigation §Step 1 (entity #4 of 5; Decision was #1 via ADR 0049 / D-037; WorkspaceContext was #2 via ADR 0050 / D-038; ApprovalGrant was #3 via ADR 0051 / D-039; Run is #5 via ADR 0053 — pending). 15 envelope-level schema fields (12 kernel-set + 3 producer-asserted-kernel-verifiable per ADR 0031 v1 §Authority discipline): `schema_version` (`leaseSchemaVersionSchema = z.literal('0.1.0')`), `lease_id`, `lease_kind` (`leaseKindSchema = z.enum(['worktree'])` — single-value v1 Zod enum; `credential_audience` and `external_target` are registry-canonical reservations per ADR 0031 v1 §Out of scope pending future schema PRs via §Procedure rule), `scope` (discriminated union on `lease_kind`, worktree branch has 3 producer-asserted-kernel-verifiable sub-fields: `repository_id`, `workspace_context_id`, `worktree_path`), `held_by_session_id` (kernel-set FK), `held_by_agent_client_id` (kernel-set FK), `acquired_by` (`leaseProducerSchema = z.enum(['mint_api', 'kernel_broker'])` — `kernel_gateway` excluded by design; `kernel_dashboard` deferred), `acquired_at`, `valid_until` (non-null per inv. 19; producer-asserted-kernel-verifiable; Phase 1 24h ceiling per ADR 0031 v1), `released_at` (nullable; kernel-set), `execution_context_id` (kernel-set per inv. 19), `lease_state` (`leaseStateSchema = z.enum(['active', 'expired', 'released', 'force_broken'])`), `force_break_grant_id` (nullable; kernel-set; v1 schema permits but Ring 1 mint API rejects any non-null value until coordinated `kernel_dashboard` producer ADR lands), `audit_chain_link_hash` (`sha256DigestSchema` with **length-prefix-encoded canonical-concatenation** per ADR 0051 v4 retroactive posture rule covering ADR 0049/0050/0051/0052), `evidence_refs` (chain-aware refs via `leaseSchema` envelope-level superRefine that walks UNCONDITIONALLY — no `lease_state`-style gating). **Authority-discipline posture**: envelope-level kernel-set with field-level exceptions per ADR 0031 v1; Layer 1 mint API verifies producer-asserted values (filesystem stat for `worktree_path`, WorkspaceContext resolution for `workspace_context_id`); rejection emits `lease_producer_assertion_unverifiable`. **`worktree_path` canonicalization** per ADR 0031 v1 Mechanical Tweak #2 / Security-D (resolves `..` traversal + symlinks before uniqueness check). **Sandbox-derived lease rejection (charter inv. 8)** for `lease_kind: 'worktree'` when requesting session's `ExecutionContext.sandbox != 'none'` per ADR 0031 v1 Security-F. **Cross-context binding** (inv. 19): Layer 1 enforces `Lease.execution_context_id == Session.execution_context_id` per ADR 0031 v1. **Cardinality discipline**: atomic insert with unique constraint on `(repository_id, canonical(worktree_path), lease_state == 'active')` per ADR 0031 v1 Mechanical Tweak #6 / Security-G + pattern 1; TOCTOU defended via atomic-insert (NOT check-then-insert). **Lifecycle (4 transitions, supersession-via-evidence_refs)**: acquire / release / expiry / force-break (per ADR 0031 v1 §Lease lifecycle). **Holder-only release** rule: Layer 1 rejects release unless `requesting_session_id == held_by_session_id` (UUID-byte-equality comparison; distinct from ADR 0051 v4 principal-string-canonicalization form). **Force-break separation of duties** (Security-H): minter ≠ holder structurally enforced; Phase 1 interim posture human-dashboard-only — `worktree_lease_force_break_acknowledgment` grant_kind deferred to coordinated `kernel_dashboard` producer ADR (which also discharges ADR 0051 v4 deferred pre-emptive grant infrastructure). **D-037 producer-disjointness rule additive cross-step extension** to Lease-acquire-vs-authorizing-Decision per ADR 0049 + ADR 0051 v4 pattern (producer equality by class identity, not delegation; walk-depth ≤ 64 + cycle-rejection via `audit_chain_corruption_detected`; v1 forced-binary `mint_api` ↔ `kernel_broker` — gateway-decided Decisions inherit gateway re-derive non-escalability defense). **Charter inv. 6 forbidden-tier non-escalability** preserved structurally **upstream of Lease** at `OperationShape.operation_class` source-enum closure (8-value closed enum admits no `'forbidden'`) + canonical policy YAML (system-config Phase 2.5 lane); per-`lease_kind` `operation_class_scope` column is **documentation-only** (reviewer cross-check, not a structural defense layer per ADR 0051 v4 scope-back pattern). v1 worktree → `{worktree_mutation, destructive_git, merge_or_push}` per ADR 0031 v1 + ADR 0029 v2 + ADR 0030 v2. **6 NEW Decision.reason_kind reservations** (all deny-only; outcome compatibility matches ADR 0049 format): `worktree_lease_held_by_other_session`, `worktree_lease_expired_during_mutation`, `worktree_not_in_workspace_context`, `lease_acquire_sandbox_rejected`, `lease_release_unauthorized`, `lease_producer_assertion_unverifiable`. Extends §Decision.reason_kind status table from 21 (after ADR 0051 v4) to 27 total reservations. **NEW §Procedure for adding a new lease_kind value rule** (8 steps including operation_class_scope identification, typed scope shape + envelope-level superRefine extension, per-`lease_kind` cardinality discipline, **strengthened sandbox-acquire rule requirement** per MT-5 at acceptance — "no sandbox-acquire rule" is NOT a valid outcome, future lease_kinds MUST commit "blocked" or "permitted with rationale"; valid_until ceiling; registry status table update; 4-reviewer dispatch). **`Evidence.subject_kind: 'lease'` already Zod-defined** at `evidence.ts:41` per MT-2 at acceptance (v1 draft incorrectly framed as registry-canonical reserved). **One-revision cycle (smoothest of the foundational-entity train)**: v1 dispatch returned only 2 ontology-mechanical-blocking items (B1 field-count math 12+3=15-not-14; B2 Evidence.subject_kind factual error) + 22 non-blocking; architect + policy + security all returned ready-for-acceptance on v1. All blocking and material non-blocking items absorbed as mechanical tweaks at acceptance (MT-1 through MT-5 plus consolidated cosmetic items). The preemptive-absorption strategy (envelope-level superRefine, length-prefix discipline inheritance, no entity-level tier_scope, operation_class_scope documentation column, D-037 cross-step extension, walk-depth + cycle-rejection, force-break grant_kind coordinated deferral) worked — v1 → accepted in 1 revision compared to ADR 0049 (2 revisions), ADR 0050 (3 revisions), ADR 0051 (4 revisions). Registry v0.4.14 → v0.4.15 with 11 specific section additions/updates pending separate docs commit (after ADR 0049/0050/0051 registry commits land in sequence). Closes M1 acceptance criterion #19. Schema PR per `.agents/skills/hcs-schema-change` follows separately. | Foundational Ring 0 entity referenced by ADR 0031 v1 worktree-ownership composition substantial commitments + `subject_kind: 'worktree'` + `predicate_kind: 'leased_to'` CoordinationFact FK target + ADR 0047 `cleanup_plan_target_under_active_lease` rejection + charter inv. 7 execute-lane stack co-leg + workflow-sequencing investigation §Step 1 entity #4. Per the workflow-sequencing investigation, Lease + Decision + ApprovalGrant + WorkspaceContext together form the execute-lane stack pre-requisite. v1 deferrals are correctly scoped: pre-emptive grant infrastructure (ADR 0051 v4 §Future amendments) + `kernel_dashboard` producer (ADR 0051 v4 + ADR 0052) + `worktree_lease_force_break_acknowledgment` grant_kind (ADR 0031 v1 Phase 1 + ADR 0052) all land as a single coordinated change-set at the future `kernel_dashboard` producer ADR. Step 1 progress: entity #4 of 5 complete; next is ADR 0053 Run (entity #5 — `Evidence.run_id` field semantics). | ADR 0052 | 2026-05-10 |
| D-039 | `ApprovalGrant` Ring 0 entity introduced as the third foundational-entity ADR per the 2026-05-10 workflow-sequencing investigation §Step 1 (entity #3 of 5; Decision was #1 via ADR 0049 / D-037; WorkspaceContext was #2 via ADR 0050 / D-038). 13 schema fields: `schema_version` (`approvalGrantSchemaVersionSchema = z.literal('0.1.0')`), `approval_grant_id`, `grant_kind` (`approvalGrantKindSchema = z.enum(['gate_evidence_acknowledgment', 'worktree_clean_acknowledgment', 'pr_absence_acknowledgment'])` — closes all three registry §Decision.required_grant_kind reservations from ADR 0030 + ADR 0035; reused by `Decision.required_grant_kind` cross-reference), `scope` (discriminated union on `grant_kind`, three typed branches; scope-payload evidence refs reuse `qualityGateEvidenceRefSchema` shape; `pr_absence_acknowledgment.branch_ref` uses `gitBranchRefSchema` regex for content-bounded producer-injection-surface), `minted_for_decision_id` (kernel-set, **non-null at v1**; pre-emptive grants deferred to future `kernel_dashboard` producer ADR), `grantor_principal_ref` (kernel-set; Principal entity forward-reference), `granted_by` (`approvalGrantProducerSchema = z.enum(['mint_api', 'kernel_broker'])` — `kernel_gateway` intentionally excluded by design; `kernel_dashboard` deferred), `granted_at`, `valid_until` (non-null per inv. 19; `<= Decision.valid_until` enforced at Ring 1 mint API only per §Rejects cross-record refinement rule), `execution_context_id` (kernel-set per inv. 19), `grant_state` (`'active' | 'consumed' | 'expired' | 'revoked'`), `audit_chain_link_hash` (`sha256DigestSchema` covering canonical-concatenation of 12 fields excluding `schema_version`; **length-prefix discipline** registered as registry-level posture rule covering ADR 0049/0050/0051 retroactively to defend against concatenation-collision attack class), `evidence_refs` (chain-aware references with **`approvalGrantSchema` envelope-level superRefine** that walks UNCONDITIONALLY across envelope AND scope-payload acknowledged_* refs, rejecting per charter inv. 18). **Envelope-level kernel-set** authority discipline (all 13 fields kernel-set; no producer-asserted-kernel-verifiable exceptions). **D-037 producer-disjointness rule (additive cross-step extension)**: ADR 0049's same-step rule survives as strict-distance case; this ADR extends to cross-step closure case (Layer 1 mint API walks `derived_from` closure bounded by walk-depth budget ≤ 64 records + cycle-rejection via `audit_chain_corruption_detected`; producer equality by class identity, not by delegation chain). v1 producer-disjointness is forced-binary (2 grant-minting producers); structurally complete because gateway-decided Decisions inherit gateway re-derive non-escalability layer per registry §Layer-disagreement tiebreaker. **Charter inv. 6 forbidden-tier non-escalability** preserved structurally **upstream of ApprovalGrant** at `OperationShape.operation_class` source-enum closure (8-value closed enum admits no `'forbidden'`) + canonical policy YAML (system-config Phase 2.5 lane); `operation_class_scope` column on registry §ApprovalGrant.grant_kind status table is documentation-only (reviewer cross-check, not a structural defense layer). **Self-approval rejection rule**: `grantor_principal_ref` MUST NOT equal consuming session's `principal_id` for grants whose underlying `OperationShape.operation_class` is in non-readonly set `{destructive_git, external_control_plane_mutation, worktree_mutation, merge_or_push, cleanup_plan}` (permitted for readonly set `{read_only_diagnostic, agent_internal_state, workspace_verify}`); Ring 1 mint API enforces via **canonicalization-aware string-comparison** (Unicode NFC + lowercase folding + leading/trailing whitespace trim); v1 acknowledges weaker than D-037 closed-enum equality until Principal entity lands (workflow-sequencing investigation §Step 3); zero-width character evasion is a v1 posture limitation that closes structurally when typed-Principal comparison lands. **Revocation race tiebreaker** (revoke-wins): concurrent consume produces TWO typed Decision records — informational `consume_after_revoke_attempt` + paired deny `required_grant_kind_unmet`; both participate in audit chain per registry §Audit-chain coverage of rejections vocabulary extension. **Consumption-time freshness re-check**: gateway re-verifies `valid_until > now()` at consumption time even if mint-time validation passed; `Decision.reason_kind: 'grant_expired_at_consumption'` on stale. **Cardinality discipline**: at most one `active` ApprovalGrant per `minted_for_decision_id`. **6 NEW `Decision.reason_kind` reservations** (registry-canonical pending Ring 1 mint API schema PR): `'grant_expired_at_consumption'` (deny-only), `'producer_disjointness_violation'` (deny-only), `'consume_after_revoke_attempt'` (informational-only), `'required_grant_kind_unmet'` (deny-only), `'self_approval_rejected'` (deny-only), `'audit_chain_corruption_detected'` (deny-only; sub-classification covers `derived_from` cycle detection only, distinct from Milestone 3 audit-event-storage corruption). **Pre-emptive grants entirely out of scope for v1**: deferred to future `kernel_dashboard` producer ADR as coordinated change-set (schema nullability + guardrails + producer authorization + granting-session attribution + 3 pre-emptive-specific reason_kind reservations). **`human_dashboard_grant` deletion-authority FK composition**: class-D destructive Git ops compose with v1 grant_kinds per underlying check class; class-I cleanup defers per ADR 0047 §Future amendments to future grant_kind extension; class-D tier preservation maintained (no grant clears forbidden-tier because no forbidden-tier path can be declared upstream); single-use semantics. **NEW §Procedure for adding a new grant_kind value rule** (7 steps including `operation_class_scope` identification, outcome-compatibility classification, single-use declaration, envelope-level superRefine extension); mirrors ADR 0048 + ADR 0049 procedural pattern. Closes M1 acceptance criterion #16 (`ApprovalGrant` in canonical 22-entity list). Registry v0.4.13 → v0.4.14 with 11 section additions/updates (§ApprovalGrant entity, §ApprovalGrant.grant_kind status table with `operation_class_scope` + outcome-compatibility columns, §ApprovalGrant.grant_state enum mirror, §Procedure for adding a new grant_kind value rule, §Revocation race tiebreaker, §Consumption-time freshness re-check, §Self-approval rejection rule with canonicalization-aware comparison, §Producer-vs-kernel-set authority fields enumeration of all 13 fields, §Audit-chain coverage of rejections cross-reference + length-prefix discipline retroactive posture for ADR 0049/0050/0051, §Decision-ApprovalGrant producer-disjointness rule additive cross-step extension, §Decision.reason_kind status table 6 new reservations). | Foundational Ring 0 entity that completes the typed-grant minting layer charter v1.4.0 inv. 18 chain-walk rejection clause references and that D-037 producer-disjointness rule (ADR 0049) requires for ApprovalGrant-side enforcement. **Four-revision cycle** v1→v4: v1 returned 15+ blockers (convergent producer name drift `kernel_mint_api` → canonical `mint_api`, same-step-only D-037 scope, missing pre-emptive guardrails, undefined revocation race, etc.); v2 absorbed v1 but surfaced new convergent issues (`consume_after_revoke_attempt` audit-only vs typed-Decision contradiction across architect + security, schrödinger nullable `minted_for_decision_id` schema-vs-enforcement state across security + ontology, silent supersession of ADR 0049 same-step framing, `kernel_gateway` missing, tier_scope kebab-case, scope-payload chain-walk leak); v3 removed pre-emptive grant infrastructure entirely (collapsed ~6 v2 blockers), reframed D-037 cross-step as ADDITIVE (not supersession), but surfaced 8 new blockers (tier_scope vocabulary collision with non-existent `OperationShape.tier` field, `inherited_from_gate` dynamic-dispatch escalation, chain-walk attribution misattributed to `qualityGateEvidenceRefSchema` instead of envelope, `valid_until` cross-record boundary missing §Rejects entry, hash-input concatenation collision via variable-length `branch_ref`, self-approval string-normalization evasion); v4 scope-back: removed `tier_scope` column entirely (grounds forbidden-tier defense on `OperationShape.operation_class` enum closure + canonical policy YAML), committed `approvalGrantSchema` envelope-level superRefine for chain-walk rejection (mirrors `decisionSchema` + `qualityGateSchema` precedents; walks unconditionally — no gate_state-style gating since ApprovalGrants authorize override at all states), added canonical-concatenation length-prefix discipline (retroactive registry posture rule covering ADR 0049/0050/0051), bounded `branch_ref` content with `gitBranchRefSchema` regex, tightened self-approval framing (dropped "principal-side analog to D-037" framing, committed canonicalization-aware comparison, acknowledged v1 string-comparison weaker than closed-enum equality + zero-width-character v1 posture limitation), added `valid_until` cross-record refinement §Rejects entry (mirrors ADR 0050 pattern), registered walk-depth budget ceiling (≤ 64 records). All four required reviewers ready-for-acceptance on v4. Five mechanical tweaks at acceptance: MT-1 PLAN.md §References line `578` → `672`; MT-2 registry §Naming-discipline §Sub-rule 9 line `206` → `203`; MT-Sec-1 §Compliance §Cross-step chain-walk bounds citation precision (ADR 0019 v3 §Acceptance defers cycle-detection rules to Ring 1 mint API); MT-Sec-2 §Self-approval rejection zero-width-character v1 posture limitation acknowledgment; MT-Sec-3 `audit_chain_corruption_detected` sub-classification note. Schema PR per `.agents/skills/hcs-schema-change` follows in separate session. **Step 1 progress**: entity #3 of 5 complete (Decision ✓ + WorkspaceContext ✓ + ApprovalGrant ✓); next is ADR 0052 Lease (entity #4), then ADR 0053 Run (entity #5). | ADR 0051 | 2026-05-10 |
| D-037 | `Decision` Ring 0 entity introduced as the first foundational-entity ADR per the 2026-05-10 workflow-sequencing investigation §Step 1 (entity #1 of `Decision` → `WorkspaceContext` → `ApprovalGrant` → `Lease` → `Run`). Schema fields: `decision_id`, `operation_shape_ref` (typed FK), `outcome` (`'allow' | 'deny' | 'informational'`), `reason_kind` (closed enum, initial 15 Zod-defined values out of 35+ registered reservations; extensible per a NEW §Procedure for adding a new reason_kind value rule registered in registry v0.4.12), `reason_text` (bounded by `decisionRedactionModeSchema = evidenceRedactionModeSchema.exclude(['none'])`; max length 256 chars), `reason_text_redaction_mode`, `evidence_refs` (chain-aware refs with Ring 0 chain-walk Zod refinement mirroring `qualityGateSchema` superRefine — rejects direct `KnowledgeChunk` references and `Evidence` refs with `authority: 'sandbox-observation' | 'self-asserted'`), `decided_by` (kernel-set producer; allowlist uses existing `mint_api` + `kernel_broker` and adds NEW `kernel_gateway`), `decided_at`, `valid_until` (non-null per inv. 19), `execution_context_id` (kernel-set per inv. 19), `audit_chain_link_hash` (`sha256DigestSchema` covering canonical concatenation of 11 envelope fields including `prior_audit_chain_link_hash`; genesis Decision policy uses `'GENESIS'` sentinel), `required_grant_kind` (advisory only, optional, kernel-set; charter inv. 6 forbidden-tier non-escalable preserved). NEW outcome-compatibility classification per `reason_kind`: 14 `deny`-only + 1 `informational`-only (`gate_provisional`) in v1; mismatched pairings rejected at schema layer. NEW producer-disjointness rule: a Decision with non-null `required_grant_kind` MUST NOT be co-minted with a satisfying `ApprovalGrant` by the same producer in the same audit-chain step (registered as posture commitment for future Ring 1 mint API enforcement; the future ApprovalGrant ADR (entity #3 per workflow-sequencing investigation §Step 1; ADR 0051 after WorkspaceContext ADR 0050) will mirror the rule on the ApprovalGrant side). Decision is kernel-set throughout; producer-supplied Decisions rejected at future Ring 1 mint API. Decision immutability: supersession via NEW Decision record citing prior in `evidence_refs`. Closes M1 acceptance criterion #15 (`Decision` in canonical 22-entity list). Registry v0.4.11 → v0.4.12 with 7 specific section additions/updates (§Decision entity, §Decision.reason_kind status table, §Decision.outcome enum mirror, §Decision.required_grant_kind enum mirror, §Procedure for adding a new reason_kind value, §Kernel-trusted producer allowlist + `kernel_gateway`, §Producer-vs-kernel-set authority fields update). | Foundational Ring 0 entity referenced by 35+ `reason_kind` reservations across 7 ADRs (ADR 0030, 0032, 0033, 0035, 0036, 0037, 0047), charter v1.4.0 inv. 4 (audit logging is internal side effect), inv. 18 chain-walk rejection clause, registry §Audit-chain coverage of rejections rule. Without `Decision` Ring 0 entity, all those rules are registry-canonical-only and structurally unenforceable. Per the 2026-05-10 workflow-sequencing investigation, 7 of 9 trigger-detection failures trace to the missing Decision entity + missing Ring 1 mint API. v1 dispatch returned 9-10 blocking items across 4 reviewers (architect: Evidence schema-version-bump factually wrong, gate_target_already_active omitted, producer allowlist drift, registry change-set under-specified; ontology: same Evidence issue, §Procedure step 2 outcome classification, cross-context refinement explicit rejection; security: `audit_chain_link_hash` shape under-specified, `reason_text` schema-layer secrets-leak guard, Ring 0 chain-walk defense-in-depth, Decision-ApprovalGrant chain-mint defense). v2 absorbed all 9-10 plus 5 policy non-blocking items. All four required reviewers returned ready-for-acceptance on v2 with two non-blocking caveats (line-number citations + layer-name vs producer-name vocabulary cross-reference); both addressed as mechanical tweaks at acceptance. Schema PR per `.agents/skills/hcs-schema-change` follows; canonical policy YAML for Decision-mediated gates lives in `system-config` at Phase 2.5 per workflow-sequencing investigation §Step 2. Ring 1 mint API implementation (per ADR 0019 v3 §Three-layer enforcement model) lives at `packages/kernel/src/mint/` per workflow-sequencing investigation §Step 4. | ADR 0049 | 2026-05-10 |

| D-042 | Foundational ADR train truth clarification: D-037 through D-041 accepted the `Decision`, `WorkspaceContext`, `ApprovalGrant`, `Lease`, and `Run` designs, but did not land their Zod source schemas, generated JSON Schemas, tests, ontology docs, or registry sections. Treat "Step 1 complete" as ADR/design completion only. Ring 1 work remains blocked until the accepted foundational designs are implemented as Ring 0 schema source and the selected Ring 1 service has its required policy/schema prerequisites. | Prevents next-agent overclaim that accepted ADRs equal implemented Ring 0 entities or that `packages/kernel/` is ready for service work. Keeps PLAN, ADRs, and the workflow-sequencing memo aligned with live source truth. | ADR 0049-0053 / PLAN truth alignment | 2026-05-10 |

| D-045 | Current source-schema truth supersedes the temporary D-042 blocker: ADRs 0049-0055 are now landed as Ring 0 source for `Decision`, `WorkspaceContext`, `ApprovalGrant`, `Lease`, `Run`, `Principal`, and `Session`, alongside the earlier canonical M1 source entities `AgentClient`, `OperationShape`, `Evidence`, and `ExecutionContext`. The source-schema baseline is **11 of 22** canonical M1 entities built; the remaining M1 gap is 11 lower-coupling entities (`HostProfile`, `ToolProvider`, `ToolInstallation`, `ResolvedTool`, `Capability`, `CommandShape`, `PolicyRule`, `Artifact`, `Lock`, `SecretReference`, `ResourceBudget`). Ring 1 work may begin only for a selected service whose policy/schema prerequisites are explicit; Phase 2.5 canonical policy YAML in `system-config/policies/host-capability-substrate/` remains the cross-repo policy gate. | Prevents D-042 from being read as current after the Step 1 + Principal + Session source landings. Keeps current PLAN status aligned with live source and preserves the distinction between Ring 0 contract readiness and Ring 1 enforcement readiness. | PLAN current-focus truth alignment / ADR 0049-0055 source landing | 2026-05-12 |

| D-046 | ADR 0056 v3 accepted: promote `operation_class_unregistered` and `audit_chain_corruption_detected` into `decisionReasonKindSchema` in a follow-up schema PR. Both values are deny-only. `operation_class_unregistered` is non-clearable: `required_grant_kind` must be `null`, and any non-null grant kind rejects. `Decision.operation_shape_ref` remains required; missing, null, or invalid refs reject for this reason kind. `Decision.schema_version` remains `0.1.0` because this is additive enum widening, not a breaking shape change. | Closes the Phase 2.5 reason-kind drift without a system-config transitional allowlist as the durable posture. Keeps ADR acceptance separate from implementation: the schema PR must update Zod source, generated JSON Schema, tests, ontology docs, and registry status tables together. Hook-local policy-copy cleanup, split policy-lint placement, live `tiers.yaml` activation, snapshot vendoring, and scoped Ring 1 mint/audit work remain separate follow-on slices. | ADR 0056 | 2026-05-12 |

| D-047 | Hook-local forbidden-pattern arrays are removed from `.claude/hooks/hcs-hook` and `.codex/hooks/hcs-hook`. Project hook bodies are thin wrappers only: delegate to `scripts/dev/hcs-hook-cli.sh` in the current Phase 0b measurement posture, and later delegate to Ring 1 RPC or an authorized generated/hash-bound runtime policy/cache sourced from system-config live policy. The interim classifier is non-authoritative, data-driven measurement code and must not be copied into hook bodies or treated as HCS policy. | Implements the approved hook cleanup slice without folding it into ADR 0056. Preserves ADR 0007's thin-hook/RPC shape and D-007's Codex-advisory boundary while removing duplicated policy from hook bodies. Claude permission-layer deny entries remain tool-native guardrails, not canonical HCS live policy. | ADR 0007 / hook cleanup | 2026-05-12 |

| D-048 | Policy lint is split by authority boundary. `system-config` owns lint for canonical live policies under `policies/host-capability-substrate/`: activation metadata, provenance, forbidden-no-approval posture, grant scope/reuse, provider evidence, sandbox lease-acquire posture, structured cross-record policy rules, and no secret material. HCS owns only generated-snapshot compatibility lint once a snapshot is vendored: source commit/path/hash binding, schema-ref compatibility against generated HCS schemas, `operation_class_defaults` coverage, reason-kind compatibility, and fixture/snapshot path validation. | Implements ADR 0006's policy-source boundary for the Phase 2.5 lint placement decision. It keeps live policy authority with system-config while allowing HCS CI to reject stale or schema-incompatible vendored snapshots. This does not activate live policy, vendor a snapshot, or make `policies/generated-snapshot/` an authoring surface. | ADR 0006 / policy-lint split | 2026-05-12 |

| D-049 | HCS agent-facing contract surfaces must restate workstation authority in HCS-local wording before the rule becomes default session context. The accepted local vocabulary for the 2026-05-17 restatement pass is: actor identity, managed resource, authorization binding, and runtime evaluation. HCS work roles are local workflow roles: design owner, implementer, reviewer, maintainer, and evidence gatherer. HCS Cloudflare identity references use `jeffreyverlynjohnson@gmail.com` as the interim identity and `guardian@thenash.group` as the target after the Phase 2 Cloudflare account email migration. HCS Cloudflare MCP baseline is `cloudflare-api` at `https://mcp.cloudflare.com/mcp`, per-agent OAuth, no shared bearer, no bearer-in-argv wrapper, read-mostly grant. | Implements the operator-relayed 2026-05-17 workstation-contract restatement without changing ontology, policy, hooks, provider state, or sibling repos. Historical ADRs, decision rows, and research notes may preserve older external vocabulary as provenance, but current operating guidance must live in HCS-owned contract docs. The first local artifact is `docs/host-capability-substrate/workstation-surface-contract.md`; the inventory is `docs/host-capability-substrate/restatement-inventory-2026-05-17.md`; the relayable readout is `docs/host-capability-substrate/usable-state-readout-2026-05-17.md`; `just agent-contract-identity-scan` guards the default and near-default agent-facing surfaces during `just verify`. | docs-only restatement / ADR 0001 / ADR 0006 / ADR 0007 | 2026-05-17 |

| D-050 | The HCS restatement guard applies to default and near-default agent-facing surfaces, not to every historical, research, runbook, ontology, ADR, or decision-ledger record. Those non-default records may preserve source labels, external infrastructure names, and technical terms such as parent process/context or upstream dependency ordering when they are provenance or implementation vocabulary rather than active HCS operating authority. Future promotion from those records into `AGENTS.md`, `README.md`, `CLAUDE.md`, `IMPLEMENT.md`, the workstation contract, the tooling matrix, setup docs, policy fixture docs, or project agent config must be restated in HCS-owned wording first. | Implements the 2026-05-18 second-pass scan recorded in `docs/host-capability-substrate/restatement-inventory-2026-05-17.md`. This prevents destructive bulk rewrites of evidence history while keeping the default session contract clean and guarded by `just agent-contract-identity-scan`. | docs-only restatement / D-049 | 2026-05-18 |

| D-051 | HCS opens the generated-snapshot lane by vendoring the first live HCS policy snapshot from `system-config` commit `136dbaa` into `policies/generated-snapshot/tiers.yaml`. The HCS live-to-vendor binding is external to the vendored YAML and recorded in `policies/generated-snapshot/snapshot-binding.json` with source path `policies/host-capability-substrate/tiers.yaml` and source policy SHA-256 `sha256:e06442e02db50604e8ae8cbc1572a4ecec91ae87bfac6705e52161fd450ae68b`. `scripts/ci/snapshot-binding-check.sh` owns the HCS-side D-048 generated-snapshot checks: binding triple presence, vendored-file digest match, schema-ref compatibility with current Zod literals, `operation_class_defaults` coverage against `operationShapeOperationClassSchema`, reason-kind compatibility against `decisionReasonKindSchema`, and snapshot path validation. | Implements the HCS side of D-048's split after `system-config` activated the live Phase 2.5 tiers file. This does not edit `system-config`, change HCS schema source, activate Ring 1 service behavior, create an execution broker, add mutating endpoints, draft a Ring 1 ADR, or perform provider operations. | D-048 / generated-snapshot lane | 2026-05-18 |

| D-052 | ADR 0057 accepts the first Ring 1 service boundary: a mint/audit-only service for authoritative record minting and audit-chain validation. The accepted scope covers the six audit-chain-committed entities Decision, ApprovalGrant, Lease, Run, Principal, and Session; AgentClient minting is blocked pending a future AgentClient canonical-hash amendment while `kernel_agent_client_resolver` remains a forward producer reservation. The producer allowlist has eight producer classes across entity-envelope and evidence-only scopes, with `kernel_dashboard` deferred. The service owns producer allowlist enforcement, KnowledgeChunk transitive authority-graph rejection, sandbox-source rejection, cycle-only use of `audit_chain_corruption_detected`, fail-closed depth overflow pending a distinct reason_kind, kernel-resolved audit attribution, service-wide fail-closed reason-kind discipline, ApprovalGrant grant-kind clearing rules, and a standalone FK-closure inventory. | Accepts ADR 0057 after three review cycles: round 1 produced seven blockers; round 2 produced one architect blocker plus one unresolved ontology finding; round 3 returned five of five `yes` verdicts with zero new findings. This does not authorize Ring 1 service implementation code, schema source edits, live policy or `tiers.yaml` changes, generated-snapshot changes, `system-config` edits, execution broker behavior, gateway behavior, capability registration, tool resolution, host-state service behavior, dashboard human-in-the-loop flows, agent-direct mutation endpoints, provider operations, or follow-up ADR drafting. | ADR 0057 / Ring 1 mint/audit service | 2026-05-18 |

| D-053 | ADR 0058 accepts `authority_chain_walk_depth_exceeded` as the depth-overflow `Decision.reason_kind` to add to `decisionReasonKindSchema` in the next schema slice. The reason kind is deny-only and non-clearable: `required_grant_kind` must be `null`. Producer scope is limited to `mint_api` and `kernel_broker`; `kernel_gateway` is explicitly excluded pending a future gateway ADR. Typed Decision emission is allowed only when the rejected mint request has a valid `operation_shape_ref` and can produce a schema-valid Decision envelope; non-operation mint overflows and pure chain-validation overflows remain on the audit rejection path without typed Decision emission. Cycle-vs-depth separation is preserved: `audit_chain_corruption_detected` remains cycle-only. | Closes ADR 0057's depth-overflow future-amendment item and mirrors the ADR 0056 / D-046 additive enum-widening precedent: the follow-up schema PR per `.agents/skills/hcs-schema-change` adds the enum member without bumping `Decision.schema_version`, regenerates schema artifacts, updates ontology/registry docs, and adds focused tests. This commit does not edit schema source, reflect live policy or `tiers.yaml`, change `policies/generated-snapshot/`, implement Ring 1 mint/audit behavior, authorize broker implementation or emission before a future broker ADR, edit `system-config`, or perform provider operations. | ADR 0058 / depth-overflow reason_kind promotion | 2026-05-19 |

| D-044 | `Session` Ring 0 entity introduced as the seventh foundational-entity ADR — second of two highest-coupling remaining M1 entities per the post-Step-1-source-landing workflow-sequencing investigation §Step 3 priority order (entity #2 of 2 highest-coupling complete with this ADR). 12 envelope-only-kernel-set fields with NO field-level exceptions (mirrors Decision/ApprovalGrant/Run/Principal posture): `schema_version` (`sessionSchemaVersionSchema = z.literal('0.1.0')`; thirteenth entity-specific literal counting boundary-observation's sibling), `session_id`, `session_kind` (`sessionKindSchema = z.enum(['agent_invocation'])` — single-value v1 Zod enum; `dashboard` paired with future kernel_dashboard producer ADR + `system_task` paired with system_principal Zod-defined extension per ADR 0054 are registry-canonical reservations), `session_state` (`sessionStateSchema = z.enum(['active', 'ended'])` — cardinality matches AgentClient/WorkspaceContext/Principal long-lived precedents (1 active + 1 terminal at v1) but value-name diverges intentionally: Session uses `ended` not `retired` because Session is a transient invocation rather than a long-lived identity), `agent_client_id` (kernel-set FK to AgentClient), `principal_id` (kernel-set FK to Principal; closes ADR 0051 v4 + ADR 0054 self-approval rejection consuming-session typed-FK target), `execution_context_id` (kernel-set per ADR 0031 v1 Mechanical Tweak #8 / Security-C — Session is execution-context-BOUND at the entity layer mirroring WorkspaceContext, NOT Principal), `started_at`, `ended_at` (nullable until terminal), `producer` (`sessionProducerSchema = z.enum(['kernel_session_resolver'])` — NEW kernel-trusted producer mirroring ADR 0037 `kernel_agent_client_resolver` + ADR 0054 `kernel_principal_resolver` precedents; `kernel_dashboard` excluded by design pending future coordinated change-set), `audit_chain_link_hash` (`sha256DigestSchema` with length-prefix discipline retroactive posture rule extended from ADRs 0049-0054 to ADRs 0049-0055), `evidence_refs` (plain `evidenceRefSchema`; NO chain-walk envelope superRefine per typed-identity-envelope precedent — AgentClient + WorkspaceContext + Principal; inv. 8 + inv. 18 deferred to Ring 1 mint API via producer-allowlist closure on `kernel_session_resolver`). **Closes 4 forward-reference typed FK targets**: `Lease.held_by_session_id` (ADR 0052), `Run.invoker_session_id` (ADR 0053), ADR 0030 v2 `owning_session_id`, and the "consuming session" + "requesting session" references in ADRs 0031 v1 / 0051 v4 / 0052 / 0054. **Execution-context-binding-at-entity-layer divergence**: Session carries `execution_context_id` (mirrors WorkspaceContext) vs Principal (NO `execution_context_id` because principal identity is execution-context-independent); sessions that cross contexts are NEW Session records, NOT the same Session re-bound. Cross-context substitution defense structurally complete at Session layer (forge-able only by minting a forged Session record, which requires `kernel_session_resolver`). **Same-record refinements** (Zod superRefine): `session_state == 'active' iff ended_at == null` (mirrors WorkspaceContext + Run + Lease state↔nullable-timestamp correlation patterns) + `ended_at == null || ended_at >= started_at` (mirrors Run.ended_at >= started_at temporal refinement per ADR 0053). **Lifecycle (2 transitions, supersession-via-evidence_refs)**: `null → active → ended`. Each transition emits typed Decision via Layer 1 mint API; mid-session in-place mutation rejected (mirrors ADRs 0049-0054 immutability discipline). **4 NEW Decision.reason_kind reservations** (all deny-only): `session_agent_client_unresolvable`, `session_principal_unresolvable`, `session_execution_context_unresolvable` (operationalizes charter §Forbidden patterns line 138 / inv. 17 analogous to `run_execution_context_unresolvable` per ADR 0053), `session_started_at_after_ended_at` (schema-level same-record temporal refinement rejection). Extends §Decision.reason_kind status table from 32 (after ADR 0053) to 36 total reservations. **NEW §Procedure for adding a new session_kind value rule** (7 steps including source-ADR citation + invocation-evidence-source identification + required-FK-fields-per-kind + lifecycle transitions + per-kind producer attribution + registry status table update + all-4-reviewer dispatch). **`Evidence.subject_kind: 'session'` already Zod-defined** at `evidence.ts:28` per existing Phase 2 schema train; ADR does NOT modify `evidenceSubjectKindSchema` or bump `Evidence.schema_version`. **Ring 1 `kernel_session_resolver` sandbox-source rejection guard (security N1 v2 absorption)**: when Ring 1 implementation lands at `packages/kernel/src/session/`, the resolver MUST enforce that invocation evidence cited by Session.evidence_refs carries non-sandbox `authority` (i.e., NOT `'sandbox-observation'` and NOT `'self-asserted'`); chain-walk rejection extends transitively via `derived_from` walk-depth ≤ 64 with cycle-rejection via `audit_chain_corruption_detected` (security N4 v2 absorption). **Two-revision cycle**: v1 dispatched (commit `4e8fe23`); architect returned 4 blockers (B1 lifecycle naming-and-precedent divergence — v1 claimed `['active', 'ended']` "mirrors AgentClient/WorkspaceContext/Principal" but those use `['active', 'retired']`; B3 schema-source line citations stale; B4 registry line citations stale) + ontology 7 stale-citation blockers (B-2 lease.ts:101→103; B-3 run.ts:110→104; B-4 producer allowlist line 867→899 + kernel_principal_resolver row 893→910; B-6 enum mirrors 2493→2504; B-7 self-approval rejection rule 2408→2419; B-8 audit-chain coverage 671→686; B-9 schema-version ledger 871→882); policy + security returned ready-for-acceptance outright. v2 (commit `1ba8a2c`) absorbed all 11 blockers + security N1 + N4 non-blocking implementation-detail acknowledgments. v2 ready-for-acceptance from all 4 reviewers with 2 mechanical tweaks at acceptance (MT-1 architect N-1: §Status v2 absorption block arrow notation clarified; MT-2 architect F-2 + ontology F-1: D-044 row recording v1+v2 cycle). Cycle-time progression matches ADR 0049 + ADR 0052 + ADR 0054 efficiency tier (2 revisions). Registry v0.4.17 → v0.4.18 with 14-item change-set pending separate docs commit (after source landing). Closes M1 acceptance criterion #5 (`Session` in canonical 22-entity list). Source schema PR per `.agents/skills/hcs-schema-change` follows separately; the source-landing slice also includes `lease.ts` + `run.ts` + ADR 0030 v2 direct-Evidence-subtype `.describe()` text updates referencing Session as typed FK target (NO shape change to `entityIdSchema`; no schema_version bumps on consuming entities). **Step 3 progress: entity #2 of 2 highest-coupling COMPLETE** at the ADR/design layer; source landing follows. With Session designed, remaining source-schema M1 gap after Session lands is 11 lower-coupling canonical entities (HostProfile/ToolProvider/ToolInstallation/ResolvedTool/Capability/CommandShape/PolicyRule/Artifact/Lock/SecretReference/ResourceBudget). Source-schema baseline shifts 10 of 22 → 11 of 22 when this ADR lands as Ring 0 source. | Foundational Ring 0 entity closing 4 forward-reference typed FK targets (Lease.held_by_session_id, Run.invoker_session_id, ADR 0030 v2 owning_session_id, ADR 0051 v4 + ADR 0054 self-approval rejection consuming-session principal_id surface). Per workflow-sequencing investigation §Step 3, Session is the second of two highest-coupling remaining M1 entities; with Principal landed at commit `8382194` and Session designed at this ADR, both highest-coupling forward-references in the just-landed five (Decision/WorkspaceContext/ApprovalGrant/Lease/Run) are closed at the design layer. v1 deferrals correctly scoped: detailed session-kind taxonomy beyond `agent_invocation` (future schema PRs via §Procedure rule); session-level lifecycle state extensions beyond `active`/`ended` (future ADRs); `valid_until` ceiling at entity layer (future amendment if operational evidence warrants); `kernel_dashboard` producer + `dashboard` session_kind (coordinated future ADR); `system_task` session_kind + `system_principal` Zod-defined extension (coordinated future ADRs); detailed cross-record refinements (Ring 1 mint API). Source landing follows per the established Step 3 entity #1 (Principal) coordinated-schema-PR pattern at commit `8382194`. | ADR 0055 | 2026-05-11 |
| D-043 | `Principal` Ring 0 entity introduced as the sixth foundational-entity ADR — first entity drafted after the workflow-sequencing investigation §Step 1 schema train landed as Ring 0 source (commit `7fb7e05`). Principal is the highest-coupling remaining M1 entity per the post-landing PLAN.md §Current Focus: forward-referenced by `ApprovalGrant.grantor_principal_ref` (currently `entityIdSchema` untyped-semantic forward-reference per ADR 0051 v4) and the future `Session.principal_id` (when Session lands). 9 envelope-level kernel-set fields with NO field-level exceptions (mirrors Decision/ApprovalGrant/Run envelope-only-kernel-set posture; cleaner than WorkspaceContext/Lease mixed splits per ADR 0031 v1): `schema_version` (`principalSchemaVersionSchema = z.literal('0.1.0')`; twelfth entity-specific literal counting boundary-observation's sibling literal), `principal_id` (closes typed FK target for ApprovalGrant.grantor_principal_ref + future Session.principal_id + ADR 0025 requesting_principal_id + ADR 0036 cycle-history.md ratification verifier-identity), `principal_kind` (`principalKindSchema = z.enum(['human', 'service_principal'])` — 2-value v1 Zod enum; `pseudo_principal` (cycle-history.md ratification per ADR 0036 future Q-row) and `system_principal` (kernel-emitted record attribution) are registry-canonical reservations pending future schema PRs via §Procedure rule), `principal_state` (`principalStateSchema = z.enum(['active', 'retired'])` — mirrors AgentClient + WorkspaceContext lifecycle pattern), `kernel_observed_at`, `valid_until` (nullable; non-null when retired), `producer` (`principalProducerSchema = z.enum(['kernel_principal_resolver'])` — NEW kernel-trusted producer mirroring ADR 0037 `kernel_agent_client_resolver` precedent; `kernel_dashboard` deferred to coordinated future ADR per ADRs 0051 v4 / 0052 / 0053 scope discipline), `audit_chain_link_hash` (`sha256DigestSchema` with length-prefix-encoded canonical-concatenation per ADR 0051 v4 retroactive posture rule, now extended from ADRs 0049-0053 to ADRs 0049-0054), `evidence_refs` (plain `evidenceRefSchema`; NO chain-walk envelope superRefine per typed-identity-envelope precedent — AgentClient + WorkspaceContext; inv. 8 + inv. 18 deferred to Ring 1 producer-allowlist closure on `kernel_principal_resolver`). **NO `execution_context_id` field on Principal envelope** (Principal identity is execution-context-independent at the entity layer; mirrors AgentClient framing — a human signing a git commit is the same human across terminal/IDE/dashboard surfaces; cross-context-substitution defense trivially satisfied at the Principal layer because there's no binding to forge; binding evidence cited in evidence_refs carries its own execution_context_id per inv. 19). **Same-record refinement**: `principal_state == 'active' iff valid_until == null` (mirrors WorkspaceContext per ADR 0050). **Lifecycle (2 transitions, supersession-via-evidence_refs)**: `null → active → retired`. **Structural closure of ADR 0051 v4 §Self-approval rejection MT-Sec-2 zero-width-character evasion posture limitation**: typed-FK self-approval comparison (`ApprovalGrant.grantor_principal_ref == consuming_session.principal_id`) becomes UUID-byte-equality (mirrors ADR 0052 §Identity comparison form for session_id) on principal_id surface IDs canonicalized at Principal mint by `kernel_principal_resolver` via 4-step recipe: (1) Unicode NFC normalization, (2) **Unicode general-category `Cf` (Format Control) strip** (closes the invisibles surface — ZWSP/ZWNJ/ZWJ/LRM/RLM/BOM/word joiner/soft-hyphen/Arabic format controls/bidirectional controls/LRI/RLI/FSI/PDI/Mongolian vowel separator/interlinear annotation anchors/supplementary-plane Cf), (3) Unicode-aware lowercase fold, (4) leading/trailing whitespace trim (embedded whitespace preserved as distinct surface ID by design). Canonicalization at MINT, not at COMPARE. `Cf`-strip step is the load-bearing change vs v1 (v1 recipe was identical to ADR 0051 v4 MT-Sec-2's acknowledged-inadequate recipe; v2 absorbed via security B1). **TR39 confusable defense reserved as future amendment (security N2 v2)** — ASCII-vs-Cyrillic homoglyph posture limitation acknowledged. **Unicode version pinning reserved as future amendment (security N1 v2)**. **Coordinated schema PR commitment**: follow-up `approval-grant.ts` `grantor_principal_ref` retains `entityIdSchema` field shape (NO shape change) and gains typed-Ring-0 FK target; `approvalGrantSchema.schema_version` remains `'0.1.0'`. **`Evidence.subject_kind: 'principal'` already Zod-defined** at `evidence.ts:23`; ADR does NOT modify `evidenceSubjectKindSchema` or bump `Evidence.schema_version`. **NEW §Procedure rule** (6 steps: source ADR citation, binding-evidence source identification, authority-binding posture classification with synthetic-identity rejection rule, lifecycle transitions, registry status table update, all-4-reviewer dispatch). **Two-revision cycle**: v1 dispatched 4 reviewers in parallel; all 4 returned blockers (Security B1 most serious: v1 §Compliance §Principal-id canonicalization rule was IDENTICAL to ADR 0051 v4 MT-Sec-2's acknowledged-inadequate recipe; "structurally closed" claim was false). v2 absorbed all blockers (Cf-category strip added; §Self-approval rejection rule registry section ADD reframed from UPDATE; registry line citations corrected — §ADR 0049-0053 enum mirrors line 2386 vs v1's 2317; schema-version literal count corrected from 8 to 11+1 sibling = 12 with Principal joining as 12th; "string-shape forward-reference" framing reframed as "untyped-semantic `entityIdSchema` forward-reference"; schema-version ledger update added as item 12). All 4 reviewers returned ready-for-acceptance on v2 with 3 mechanical tweaks at acceptance (MT-1 line 940 11-item → 12-item count alignment auto-applied by architect during re-review; MT-2 embedded-whitespace clarity note per security N1; MT-3 coordinated-PR registry-section creation commitment confirmed). Registry v0.4.16 → v0.4.17 with 12 specific section additions/updates pending separate docs commit. Closes M1 acceptance criterion #3 (`Principal` in canonical 22-entity list). Source-schema baseline shifts 9 of 22 → 10 of 22 when this ADR lands as Ring 0 source (next coordinated schema PR per `.agents/skills/hcs-schema-change`). | Foundational Ring 0 entity referenced by ApprovalGrant.grantor_principal_ref + future Session.principal_id + ADR 0025 requesting_principal_id + ADR 0036 cycle-history.md ratification verifier-identity binding + ADR 0019 v3 synthetic-identity-rejection rule + ADR 0036 §Layer 1 grounding requirement. Per the workflow-sequencing investigation §Step 3, Principal is the highest-coupling remaining M1 entity given the post-Step-1 forward-references. v1 deferrals correctly scoped: detailed binding-evidence-source taxonomy + commit-signature-to-principal mapping rules (ADR 0036 future Q-row); `kernel_dashboard` producer + dashboard-initiated retirement workflow (coordinated future ADR per ADRs 0051 v4 / 0052 / 0053); `pseudo_principal` + `system_principal` Zod-defined extensions (registry-canonical reservations); detailed cross-record refinements (Ring 1 mint API per registry §Cross-context enforcement layer). Step 3 progress: Principal entity #1 of 2 highest-coupling complete; next is Session (entity #2 of 2; ADR forthcoming). | ADR 0054 | 2026-05-11 |
| D-054 | Tool-baseline re-baseline (supersedes D-029), triggered by the Opus 4.7→4.8 model change per D-029's re-baseline-on-material-change clause + charter inv. 12 + Q-010(e); CLI/app version bumps alone stay acceptable without re-baselining. Observed current baseline (2026-05-28): Claude Code CLI `2.1.156`; Claude model posture Opus 4.8 (`claude-opus-4-8`, 1M context; `opus` short-name in settings); Codex macOS app `26.519.81530 (3178)` with Workspace dependencies `26.521.10419`; host macOS Tahoe `26.5`. Codex CLI recorded as verify-via-`codex --version` (not asserted in this packet); Codex model posture stays GPT-5.5/GPT-5.4-compatible HCS profiles; Claude macOS app build not re-observed here. Public CLI semver and app-build identifiers remain separate authority facts (D-029); the ≥ `2.1.120` / ≥ `0.125.0` floors and charter inv. 12's named-Opus-4.7 text are unchanged. AGENTS.md and CLAUDE.md §Tool baseline updated to match. | Charter inv. 12 calls for a ledger re-baseline after material version changes; the model change is the material trigger (Q-010(e): tool updates can invalidate sandbox/containment evidence and trigger re-baseline), not the CLI/app bumps. Versions recorded as observed evidence per charter inv. 14 authority order; the Codex CLI number is deliberately not asserted because it was not observed. Modeling/ledger only — no `.claude/settings.json`, charter, tooling-surface-matrix, kernel/adapter, live-config, or provider change. Residual same-fact drift (charter inv. 12 text; tooling-surface-matrix §Tool baseline + anti-pattern; AGENTS.md/CLAUDE.md subagent "Opus 4.7" lines) flagged for follow-up via their own lanes (charter via amendment ADR; `.claude/agents/**` via hcs-architect). | supersedes D-029 (charter inv. 12/14; Q-010(e)); 2026-05-28 config-substrate alignment | 2026-05-28 |
| D-055 | Per ADR 0017 §Future amendments (re-review on material Codex app bundle / Workspace Dependencies change; re-observe stale capability rows before reuse) + charter inv. 14: the prior `codex_app_sandboxed` capability rows — Keychain, filesystem, network, shell carrier, MCP startup timing, and toolchain PATH (already `pending`, observed against Codex app bundle `26.422.30944` and the later `26.422.71525` / `26.429.20946` refreshes) — are marked `stale` (ADR 0017 / dashboard-contracts capability-state vocabulary) given the jump to app `26.519.81530 (3178)` / Workspace dependencies `26.521.10419`, and require fresh per-surface re-observation before reuse as current evidence. The now-visible Codex macOS GUI posture (Permissions Default + Auto-review + Full access all ON; Sandbox = Read only; Approval = On request; config selection `the-nash-group`) is recorded as SOURCE/app-posture evidence only — explicitly NOT runtime proof (ADR 0017 §Rejects: UI labels do not prove backing storage or effective runtime behavior). Effective `~/.codex/config.toml` is unverified from this repo. Recommended operator posture: Full access OFF (standing); config selection at the jefahnierocks entity scope rather than `the-nash-group` parent scope (boundary mismatch). | Exercises ADR 0017's existing re-review/stale machinery through the ledger rather than amending the accepted ADR; no materialized capability table exists in-repo (Ring 1 unimplemented; runtime state lives outside the repo), so staleness is a modeling statement. Keeps app UI labels as posture/source evidence and never runtime receipts, preserving charter inv. 8/15. Posture recommendation is advisory only; GUI toggles and effective config are operator-owned and unchanged here. | ADR 0017 (charter inv. 14); 2026-05-28 config-substrate alignment | 2026-05-28 |
| D-056 | Credential-plane seam confirmation for the system-config proximate fix (a 1Password service-account / `OP_SERVICE_ACCOUNT_TOKEN` branch behind the `host_secret_*` wrappers): it changes only the wrapper backend secret-resolution path and preserves the D-028 caller-facing contract (`host_secret_*` functions + `HCS_SECRET_*` namespace incl. `$HCS_BROKER_SOCKET`), so the future ADR 0012 broker subsumes it behind the same socket without caller churn. The SA token is a distinct cloud-API `CredentialSource` (ADR 0018 scoped-durable-source preference) and must NOT be conflated with the desktop-app `op` integration `s.sock` IPC path or app Keychain: the CLI-helper / GUI-OAuth / app-Keychain execution contexts stay separate (ADR 0012 §Decision/§Rejects; ADR 0016; charter inv. 15/17). Broker implementation remains blocked (ADR 0040/0043); contract/posture confirmation only — no HCS code, no live config, no secret-store action. | D-028 shipped the stable caller contract so the backend can evolve (direct `op read` → SA branch → eventual broker) without caller churn; the SA branch lands inside that backend, not at the caller surface. Confirms the proximate fix does not collapse the credential execution-context separation the broker exists to preserve (ADR 0012 rejects treating apiKeyHelper / GUI-OAuth / app-Keychain as one portable auth surface). The SA path aligns with ADR 0018 Option C (scoped, expiry/rotation/healthchecked durable source); env materialization stays a per-process compatibility rendering, not the durable source. | ADR 0012/0018 (D-028; ADR 0016; charter inv. 15/17); 2026-05-28 config-substrate alignment | 2026-05-28 |
| D-057 | ADR 0060 accepts `PolicyRule` as the first of the remaining-11 M1 canonical Ring-0 entities, landed in the resumed M1 forward train (2026-05-29 coordinator directive). PolicyRule is a **non-minted** typed shape of a single policy rule, keyed to `OperationShape.operation_class`, expressing tier (`policyRuleTierSchema`: read-safe/write-local/write-project/write-destructive/forbidden), an approval discriminated union (reusing `approvalGrantKindSchema` + `approvalGrantProducerSchema`), lease/deletion/typed-provider-evidence flags, `valid_until_ceiling` (+ source_ref; new narrow `isoDurationSchema`), and `source_provenance` bound to the operator-approved live policy. It carries NO `audit_chain_link_hash`/producer-mint field and stays absent from the ADR 0057 six-entity mint scope. Charter inv. 6 forbidden non-escalability is enforced by a `nonEscalableTiers` set-membership superRefine (no approval path/grant detail on a forbidden rule); the schema encodes only structural invariants, never the `operation_class → tier` / `tier → approval` mappings (live-policy content, inv. 1/10). Landing the schema flips the live policy's `policy_rule_schema_version: null → '0.1.0'` (operator-gated system-config edit + coordinated byte-identical re-vendor). Two named dependencies of the non-minted posture: **(B-1)** a required follow-up `Decision` schema amendment adding `policy_rule_ref` + the resolved `source_policy_sha256` for audit attribution (`Decision` lacks it today) — its own ADR; **(B-2)** a Ring-1 loader obligation to verify `source_provenance.source_policy_sha256` against the bound, verified snapshot digest before a rule influences a Decision (the `authority` literal confers no authority absent the match). | Accepts ADR 0060 after a two-round four-reviewer cycle: round 1 = 3 `yes-with-mechanical-tweaks` (architect/ontology/policy) + 1 `no` (security; B-1 audit-attribution claim unsupported by `decision.ts`, B-2 self-assertable provenance); v2 absorbed both blockers + all mechanical tweaks; round 2 = 4/4 `yes`, zero new blockers. Design-only acceptance — authorizes no Zod source, generated JSON Schema, tests, or ontology/registry edits (the schema PR per `.agents/skills/hcs-schema-change` follows), no `Decision` amendment (its own ADR), no live-policy byte change or snapshot re-vendor (operator + system-config lane), no Ring-1 gateway/provenance-verification implementation (charter inv. 7), and does not add PolicyRule to ADR 0057 mint scope. | ADR 0060 / PolicyRule Ring-0 entity | 2026-05-29 |
| D-058 | ADR 0059 (AgentClient canonical-hash amendment) accepted: commits AgentClient canonical field order + GENESIS handling + length-prefix / fixed-slot evidence-ref encoding for the existing AgentClient envelope, with no `schema_version` bump and no new producer field (`kernel_agent_client_resolver` stays the sole trusted producer path). Closes ADR 0057's AgentClient canonical-hash future-amendment item at the design layer; AgentClient minting stays blocked until the follow-up schema PR lands the source descriptions/generated schema/docs/tests, after which AgentClient joins Decision/ApprovalGrant/Lease/Run/Principal/Session as the 7th audit-chain-committed mint entity. | Accepts ADR 0059 after the operator-authorized round-2 reviewer dispatch returned unanimous `yes` with zero new blockers; both round-1 blockers (security: nondeterministic `canonical(evidence_refs)` for absent/null optional slots → fixed-slot encoding; eval: service-path producer-spoofing trap overclaim → split to an implementation-test obligation) confirmed absorbed cleanly. Schema-PR-targeted round-2 non-blockers folded into the implementation plan (assert `evidenceRefSchema` field set/order; explicit AgentClient schema-version ledger row; shortest-form unsigned varint codec; present-empty-vs-absent hash vector). Design-only acceptance: no schema source, generated JSON Schema, ontology/registry edits, live policy/snapshot/system-config change, or Ring 1 implementation in this slice. | ADR 0059 / AgentClient canonical-hash amendment | 2026-05-29 |
| D-059 | ADR 0061 (Decision rule-attribution amendment) accepted: discharges ADR 0060 / D-057's B-1 dependency by adding, in a follow-up schema PR, an additive nullable-optional attribution pair to the `Decision` envelope — `policy_rule_ref` (`entityIdSchema`) + `resolved_policy_sha256` (`sha256DigestSchema`) — pair-consistent on attributed-ness (present AND non-null; reject iff exactly one attributed) with NO `Decision.schema_version` bump, so the live snapshot `decision_schema_version: '0.1.0'` binding stays valid (no operator re-vendor). The Ring 0 schema enforces only field format + pair consistency; deciding which decisions must carry attribution, populating the pair, and the B-2 digest-vs-bound-snapshot trust check are Ring 1 mint/audit obligations routed to the mint/audit implementation ADR open-items. | Accepts ADR 0061 after a five-reviewer round-1 pass returned zero blockers (policy + security `yes`; architect/ontology/eval `yes-with-mechanical-tweaks`); v2 folded every tweak so no confirming round 2 was required (mechanical-tweaks-at-acceptance, ADR 0058 precedent). Design-only acceptance: no Zod source, generated JSON Schema, tests, or ontology/registry edits (the schema PR per `.agents/skills/hcs-schema-change` follows, sequenced after the PolicyRule schema PR so `policy_rule_ref`'s FK target exists), no live-policy/snapshot/system-config change, and no Ring 1 implementation. | ADR 0061 / Decision rule-attribution amendment | 2026-05-29 |
| D-060 | ADR 0062 (Capability Ring-0 entity) accepted: the second of the remaining-11 M1 entities, in the PolicyRule → Capability → CommandShape policy-registry order. Capability is the NON-MINTED Ring-0 registry declaration of a known kernel operation ("a declared kernel operation, e.g. service.activate"), a structural peer of PolicyRule. Envelope: schema_version, capability_id, operation_name (dotted lowercase regex, forbids path/URI/secret shapes), operation_class (reuses operationShapeOperationClassSchema), capability_state (active|deprecated|retired), source_provenance (authority literal capability_registry + source_registry_path + source_registry_sha256 + basis + observed_at, mirrors PolicyRule). NO audit_chain_link_hash / producer / evidence_refs / observation-state / containment-class; no superRefine. The ADR's central work is disambiguating the entity from its two namesakes — capability-CLASS (AgentClient.containment_mechanism) and capability-STATE (BoundaryObservation.observation_state). | Accepts ADR 0062 after a four-reviewer round-1 pass returned ZERO blockers (architect + policy yes; ontology + security yes-with-mechanical-tweaks); v2 folded every tweak so no confirming round 2 was required (mechanical-tweaks-at-acceptance). Design-only acceptance: no Zod source, generated JSON Schema, tests, or ontology/registry edits (the schema PR per `.agents/skills/hcs-schema-change` follows), no live-policy / capability-registry-blob / snapshot / system-config change, and no Ring 1 implementation. Ring-1 obligations (inv. 6 registration refusal, inv. 11 render refusal, source_registry_sha256 digest verification) carried to the future capability-registration ADR. | ADR 0062 / Capability Ring-0 entity | 2026-05-29 |
| D-061 | ADR 0063 (CommandShape Ring-0 entity) accepted: the third and last entity in the PolicyRule → Capability → CommandShape chain. CommandShape is a NON-MINTED Ring-0 typed PLAN (argv + env + cwd + timeout) rendered from an OperationShape (operation_shape_ref provenance). Central boundary: "a typed plan, NOT an execution authorization" — no execution semantics, no execute lane; the broker stays blocked (charter inv. 7). argv is a typed string VECTOR, never a shell string (inv. 2; a command:string field is the rejected Option C anti-pattern); env carries NAMES + value-SOURCE references only, never inline resolved values (inv. 5; discriminated value_source: secret_reference forward-ref to unbuilt SecretReference | execution_context_inherited); cwd is traversal/scheme-free; timeout_seconds is bounded (24h hard ceiling); superRefine enforces unique env names. NO audit_chain_link_hash / producer / evidence_refs; operation_class NOT echoed (derivable via the FK); execution_lane deferred (reachable via operation_shape_ref → OperationShape.execution_context_id). | Accepts ADR 0063 after a four-reviewer round-1 pass returned ZERO blockers (architect + policy yes; ontology + security yes-with-mechanical-tweaks); v2 folded every tweak so no confirming round 2 was required (mechanical-tweaks-at-acceptance). Design-only acceptance: no Zod source, generated JSON Schema, tests, or ontology/registry edits (the schema PR per `.agents/skills/hcs-schema-change` follows), no live-policy / snapshot / system-config change, and no Ring 1 implementation. Ring-1 obligations carried forward to the gateway/renderer/broker ADRs: the typed argument-class distinction (charter line 98 — ProviderObjectReference/PublicClientId/PolicySelectorValue/SecretReference/raw secret in argv, recorded + seeded-trapped at Ring 0), cwd absolute-root confinement, inv. 11 deprecated-verb render refusal, SecretReference FK closure + env value resolution, operation_shape_ref FK closure, and lease/approval/dashboard gating. | ADR 0063 / CommandShape Ring-0 entity | 2026-05-30 |
| D-062 | ADR 0064 (Ring-1 mint/audit service interface contracts) accepted: the ADR 0057 follow-up that TYPES the mint/audit service's interface contracts — design-only (no runtime/storage/persistence/SQLite/launchd/agent-callable endpoints/Zod source/policy edits; charter inv. 7; class-I unmergeable until M4). Defines the MintRequest/MintResult contract (resolved_context is service-path-constructed, NEVER payload-deserialized), the AuditEvent append/rejection contract (kernel-resolved attribution typed-FK to AgentClient/Session/Principal; shape only — atomic per-chain-root append + unique-genesis + persistence deferred to the audit-events/storage ADR per ADR 0057 rule 7), the fail-closed rejection/reason-kind resolution contract (cycle → audit_chain_corruption_detected; depth-overflow → authority_chain_walk_depth_exceeded when an operation_shape_ref exists else fail-closed audit-only; never invent/borrow a reason_kind), and the per-entity mint contract for the seven audit-chain-committed entities. LIFTS ADR 0057's AgentClient mint-block (now that ADR 0059/D-058 landed) so AgentClient is the 7th audit-chain mint entity via kernel_agent_client_resolver. HOMES ADR 0061's routed B-1 (mandatory attribution population) + B-2 (resolved_policy_sha256 verified against the bound, verified snapshot DIGEST, intermediate-checkpoint) obligations. Only the bound snapshot DIGEST is referenced, never policy content (inv. 1/10). | Accepts ADR 0064 after a five-reviewer round-1 pass returned ZERO blockers (architect/ontology/policy/security/eval all yes-with-mechanical-tweaks); v2 folded every tweak — the load-bearing one being the transitive sandbox-observation/self-asserted/unpromoted/KnowledgeChunk rejection restatement on the ≤64 walk (ADR 0057 §Sandbox-source rejection) that v1 omitted — so no confirming round 2 was required (mechanical-tweaks-at-acceptance, ADR 0058 precedent). Design-only acceptance: no Zod source, generated JSON Schema, tests, runtime, storage, endpoints, or live-policy/snapshot/system-config change. Carries forward: a follow-on schema PR for any contract Zod types (AuditEvent envelope; typed MintRequest/MintResult), the audit-events/storage ADR (persistence + atomic append + unique-genesis), and the broker/gateway/dashboard ADRs that consume this contract. | ADR 0064 / Ring-1 mint/audit service interface contracts | 2026-06-07 |
| D-063 | ADR 0065 (SecretReference Ring-0 entity) accepted: the next lower-coupling M1 entity, closing CommandShape's forward secret_reference_ref FK (ADR 0063). NON-MINTED Ring-0 entity (structural peer of PolicyRule/Capability/CommandShape): a self-contained typed secret reference — reference_kind enum (op_uri|hcs_uri|keychain_item|env_var_name|broker_handle) + an opaque reference_locator validated per kind to a reference shape, NEVER a value (charter inv. 5) — plus an OPTIONAL credential_source_ref FK to the durable CredentialSource (ADR 0018) and a .strict() source_provenance (disjoint authority literal + observed_at; lighter than the registry-bound PolicyRule/Capability provenance, since a SecretReference is constructed per-use not read from a hash-bound blob). NO value field anywhere; kept DISJOINT from ProviderObjectReference/PublicClientId/PolicySelectorValue/raw secret material (charter inv. 16/line 98). The never-the-value guarantee is structural for the STRUCTURED kinds (op_uri/keychain_item); for the PERMISSIVE kinds (env_var_name/broker_handle/hcs_uri tails) Ring 0 cannot distinguish a token-shaped value from a reference, so it ACCEPTS — a recorded accept-and-trap gap backstopped by forbidden-string-scan + the deferred Ring 1 deep check (mirrors ADR 0063 argv-secret-inlining). Closes the CommandShape FK with NO CommandShape shape change (secret_reference_ref stays entityIdSchema; ADR 0055 Session precedent). | Accepts ADR 0065 after a five-reviewer round-1 pass returned ZERO blockers (architect/ontology/policy/security/eval all yes-with-mechanical-tweaks; hcs-security-reviewer load-bearing for the never-the-value boundary); v2 folded every tweak — the load-bearing one being the accept-and-trap honesty fix for the permissive locator kinds — so no confirming round 2 was required (mechanical-tweaks-at-acceptance, ADR 0058 precedent). Design-only acceptance: no Zod source, generated JSON Schema, tests, ontology/registry, live-policy/snapshot/system-config, or Ring 1 implementation change. Carries forward: the SecretReference schema PR (secret-reference.ts + generated + tests + ontology/registry, incl. the accept-and-trap fixture + the _ref-suffix registry clarification + the CommandShape describe()-only FK-target note), and the Ring 1 broker value-resolution / FK-existence / reference-vs-secret-deep-check / resolution-precedence obligations. | ADR 0065 / SecretReference Ring-0 entity | 2026-06-07 |
| D-064 | ADR 0066 (HostProfile Ring-0 entity) accepted: the next lower-coupling M1 entity (per PLAN order after SecretReference), closing RunnerHostObservation.host_id (ADR 0032) with the canonical durable host record. NON-MINTED Ring-0 entity (structural peer of Capability/SecretReference): host_profile_id + host_state (active/retired) + os_name/os_version (bounded version regex)/arch + host_identity (a sha256 digest of a stable identifier: kind {platform_uuid_sha256|install_id_sha256|unknown} + sha256DigestSchema digest, digest always present) + a .strict() source_provenance (disjoint host_profile_declaration authority + observed_at). NO audit_chain_link_hash/producer/evidence_refs; absent from the ADR 0057 mint scope. The never-the-raw-identifier guarantee is STRUCTURAL for host_identity.digest (a raw IOPlatformUUID/serial cannot match ^sha256:[a-f0-9]{64}$) but NOT for host_profile_id (entityIdSchema accepts a raw-UUID shape) — so host_profile_id-opaque is a Ring 1 producer obligation (recorded accept-and-trap). Distinct from the transient RunnerHostObservation; closes the FK with NO RunnerHostObservation shape change (ADR 0055/0065 precedent). | Accepts ADR 0066 after a five-reviewer round-1 pass returned ZERO blockers (policy + eval ready_for_acceptance; architect/ontology/security yes-with-mechanical-tweaks; hcs-security-reviewer load-bearing for the never-the-raw-identifier posture); v2 folded every tweak — the load-bearing one being the overclaim fix scoping the raw-id structural guarantee to host_identity.digest and making host_profile_id-opaque a Ring-1 obligation; also renamed the provenance authority host_profile_observation→host_profile_declaration and tightened os_version — so no confirming round 2 was required (mechanical-tweaks-at-acceptance, ADR 0058 precedent). Design-only acceptance: no Zod source, generated JSON Schema, tests, ontology/registry, live-policy/snapshot/system-config, or Ring 1 implementation change. Carries forward: the HostProfile schema PR (host-profile.ts + generated + tests, incl. the host_profile_id raw-UUID accept-and-trap + os_version regex + the RunnerHostObservation.host_id→host_profile_id FK note), and the Ring 1 host-state/resolver obligations (host_profile_id non-raw-id, installed-runtime + non-sandbox observation per charter inv. 8, digest computation, FK existence, supersession). | ADR 0066 / HostProfile Ring-0 entity | 2026-06-07 |
| D-065 | ADR 0067 (ToolProvider Ring-0 entity) accepted: the next lower-coupling M1 entity (per PLAN order after HostProfile), the head of the tool-resolution chain ToolProvider → ToolInstallation → ResolvedTool. NON-MINTED Ring-0 entity (structural peer of Capability/SecretReference/HostProfile): tool_provider_id + manager_kind (mise\|homebrew\|system\|project_local\|unknown) + provider_state (active\|retired) + an OPTIONAL root_path (a NEW toolProviderRootPathSchema provider-ROOT primitive) + a .strict() source_provenance (disjoint tool_provider_declaration authority + observed_at). NO audit_chain_link_hash/producer/evidence_refs; absent from the ADR 0057 mint scope. The tool-source field is named manager_kind, NOT provider_kind, to avoid BOTH existing provider_kind field names — the SHIPPED PullRequestReceipt.payload.provider_kind (VCS-host axis) and the charter-inv-16-RESERVED-but-not-yet-shipped Capability.provider_kind (external-control-plane axis) — and is a distinct AXIS from ADR 0034 install_source_kind (install-mechanism). root_path uses a deliberate SIBLING of ADR 0034's tool-FILE-path toolProvenanceCanonicalPathSchema: the new provider-ROOT primitive accepts bare provider roots (incl. /usr/local) and genuinely forbids '..' via a (?!.*\\.\\.) lookahead. tool_provider_id is entityIdSchema (accepts a raw-shape id — a recorded accept-and-trap routed to Ring 1, mirroring HostProfile.host_profile_id). | Accepts ADR 0067 after a five-reviewer round-1 pass returned TWO real blockers — (B1, ontology) provider_kind same-NAME collision with the shipped PullRequestReceipt.provider_kind + the charter-inv-16-reserved Capability.provider_kind, which v1's distinct-axis proof addressed only against the differently-named install_source_kind; (B2, architect/security/eval) root_path reused the tool-FILE-path toolProvenanceCanonicalPathSchema, which rejects the bare /usr/local root and does not structurally reject '..'. v2 folded both (rename → manager_kind with the three-way cross-reference; new toolProviderRootPathSchema sibling) plus every mechanical tweak; because round 1 carried blockers, v2 was dispatched for a CONFIRMING round 2, which returned all five lenses 'accept' with confirms_round1_fixes=true and ZERO blockers (each verified against source). The lone round-2 non-blocking factual slip — a §References row calling Capability.provider_kind 'shipped' when it is charter-reserved — was folded. Design-only acceptance: no Zod source, generated JSON Schema, tests, ontology/registry, live-policy/snapshot/system-config, or Ring 1 implementation change. Carries forward: the ToolProvider schema PR (tool-provider.ts + generated + tests + ontology/registry, incl. the manager_kind axis/name-collision rejection tests, the bare-/usr/local-accepts + '..'-rejects root_path tests, and the tool_provider_id accept-and-trap note), and the Ring 1 tool-resolution obligations (provider observation per charter inv. 8, FK existence, supersession, tool_provider_id opacity). | ADR 0067 / ToolProvider Ring-0 entity | 2026-06-08 |
| D-066 | ADR 0068 (ToolInstallation Ring-0 entity) accepted: the next entity in the tool-resolution chain (per PLAN order after ToolProvider), the MIDDLE of ToolProvider → ToolInstallation → ResolvedTool. NON-MINTED Ring-0 entity (structural peer of ToolProvider/HostProfile/Capability/SecretReference): tool_installation_id + a REQUIRED tool_provider_id FK to ToolProvider (ADR 0067) + tool_name + version (both pinned to the hostProfileOsVersionSchema charset precedent) + a NEW install_surface_kind enum (host_path\|manager_shim\|app_bundled\|devcontainer\|cloud_image\|setup_script\|unknown) + an OPTIONAL install_path (REUSES ADR 0034 toolProvenanceCanonicalPathSchema — the correct grain: an install path IS a tool FILE path, the INVERSE of ADR 0067's bare-root case) + installation_state (active\|retired) + a .strict() source_provenance (disjoint tool_installation_declaration authority + observed_at). NO audit_chain_link_hash/producer/evidence_refs; absent from the ADR 0057 mint scope. Core work: the THREE-AXIS disambiguation — ToolProvider.manager_kind (source) vs ToolProvenance.install_source_kind (mechanism) vs install_surface_kind (authority surface). Fulfills the pre-reserved Evidence.subject_kind 'tool_installation' (evidence.ts) with NO evidenceSubjectKindSchema change and NO Evidence.schema_version bump. tool_installation_id is entityIdSchema (accepts a raw-shape id — recorded accept-and-trap routed to Ring 1, mirroring ToolProvider/HostProfile). | Accepts ADR 0068 after a five-reviewer round-1 pass returned ZERO blockers (architect/ontology/policy/eval yes; security yes-with-mechanical-tweaks; hcs-ontology-reviewer load-bearing for the three-axis disambiguation); v2 folded every mechanical tweak — pin version/tool_name to the shipped hostProfileOsVersionSchema charset; carry the three-axis contrast into the install_surface_kind .describe(); enum-order-verbatim registry mirror; named accept cases per new primitive; a §Fields clause that install_surface_kind is a descriptive authority-surface FACT (read-only Ring 1 input, never policy content, inv. 1); install_path canonicalization-parity with ToolProvenance — so no confirming round 2 was required (mechanical-tweaks-at-acceptance, ADR 0058 precedent). Each load-bearing claim verified against source (the axis distinctness, the install_surface_kind name-collision check, the install_path reuse correctness, the Evidence subject-kind pre-reservation). Design-only acceptance: no Zod source, generated JSON Schema, tests, ontology/registry, live-policy/snapshot/system-config, or Ring 1 implementation change. Carries forward: the ToolInstallation schema PR (tool-installation.ts + generated + tests + ontology/registry, incl. the install_surface_kind enum-mirror + the three-axis note + the tool_installation_id accept-and-trap), then ResolvedTool (last in the chain), and the Ring 1 tool-resolution obligations (tool_provider_id FK existence, install_path deep canonicalization, installed-runtime + non-sandbox observation per inv. 8, tool_installation_id opacity, supersession). | ADR 0068 / ToolInstallation Ring-0 entity | 2026-06-08 |
| D-067 | ADR 0069 (ResolvedTool Ring-0 entity) accepted: the LAST entity in the tool-resolution chain (per PLAN order after ToolInstallation), the TAIL of ToolProvider → ToolInstallation → ResolvedTool — the authoritative "what tool X resolves to in this context" answer; completes the chain at the Ring-0 design layer. NON-MINTED Ring-0 entity (structural peer of ToolInstallation/ToolProvider/HostProfile): resolved_tool_id + tool_name (the query, pinned to the hostProfileOsVersionSchema charset) + a REQUIRED tool_installation_id FK to the winning ToolInstallation (ADR 0068) + a REQUIRED execution_context_id FK to ExecutionContext (ADR 0031; the resolving surface) + an OPTIONAL workspace_id FK to WorkspaceContext (ADR 0050; project scope) + a NEW resolution_basis_kind enum (path_order\|workspace_pin\|explicit_override\|single_candidate\|fallback\|unknown) + resolution_state (active\|retired) + a .strict() source_provenance (disjoint resolved_tool_declaration authority + observed_at). NO audit_chain_link_hash/producer/evidence_refs; absent from the ADR 0057 mint scope. Core work: the FOUR-AXIS disambiguation — ToolProvider.manager_kind (source) vs ToolProvenance.install_source_kind (mechanism) vs ToolInstallation.install_surface_kind (authority surface) vs the NEW resolution_basis_kind (WHY this install won). The basis enum is value-disjoint from install_surface_kind on the SUBSTANTIVE values (the drafter dropped manager_shim/app_bundled, which are surface values); the two share ONLY the universal `unknown` house sentinel (present on all four axis enums), which is a shared-sentinel convention, NOT an axis overlap. Fulfills the pre-reserved Evidence.subject_kind 'resolved_tool' (evidence.ts) with NO evidenceSubjectKindSchema change and NO Evidence.schema_version bump. resolved_tool_id is entityIdSchema (accepts a raw-shape id — recorded accept-and-trap routed to Ring 1, mirroring ToolProvider/ToolInstallation/HostProfile). | Accepts ADR 0069 after a five-reviewer round-1 pass returned ZERO blockers (architect/policy/security yes; ontology/eval yes-with-mechanical-tweaks; hcs-ontology-reviewer load-bearing for the four-axis disambiguation); v2 folded every mechanical tweak — the load-bearing one (raised by four lenses): the `unknown`-shared-sentinel carve-out, so the disjointness test rejects SURFACE-ONLY values (manager_shim/app_bundled/host_path/devcontainer/cloud_image/setup_script) and must NOT assert `unknown` is rejected (a valid basis value); plus both-direction enum test pinning, the inv-1 "descriptive FACT, not a trust verdict" .describe() wording, the registry shared-sentinel note, and a §Follow-up note that workspace_pin-carries-workspace_id vs path_order-omits-it is a descriptive FACT both accepted at Ring 0 (basis↔context cross-consistency a Ring 1 obligation) — so no confirming round 2 was required (mechanical-tweaks-at-acceptance, ADR 0058 precedent). Each load-bearing claim verified against source (the four-axis distinctness, the basis/surface value-disjointness, the three FK targets + execution_context_id/workspace_id FK-name conventions, the Evidence subject-kind pre-reservation). Design-only acceptance: no Zod source, generated JSON Schema, tests, ontology/registry, live-policy/snapshot/system-config, or Ring 1 implementation change. Carries forward: the ResolvedTool schema PR (resolved-tool.ts + generated + tests + ontology/registry, incl. the resolution_basis_kind enum-mirror with the four-axis + unknown-sentinel note, the both-direction enum coverage, and the resolved_tool_id accept-and-trap) — COMPLETING the tool-resolution chain at source — and the Ring 1 tool-resolution obligations (the three FKs' existence, sandbox-non-promotion per inv. 8, resolved_tool_id opacity, basis↔context cross-consistency, supersession). | ADR 0069 / ResolvedTool Ring-0 entity | 2026-06-08 |
| D-068 | ADR 0070 (Artifact Ring-0 entity) accepted: the first of the remaining lower-coupling M1 "storage primitive" entities (Artifact / Lock / ResourceBudget). Artifact is "a run's structured output (diff, log chunks, exit code, signed summary)", modeled as a NON-MINTED, DIGEST-ADDRESSED, IMMUTABLE descriptor (structural peer of ToolInstallation/HostProfile, minus their active/retired lifecycle — immutability replaces in-place state): artifact_id + a REQUIRED run_id FK to Run (ADR 0053) + an artifact_kind enum (command_diff\|log_chunk\|exit_code\|signed_summary\|unknown) + content_sha256 (sha256DigestSchema — the content ADDRESS, NOT the bytes) + byte_size (z.number().int().min(0)) + a .strict() source_provenance (disjoint artifact_declaration authority + observed_at). NO inline content, NO storage-location pointer, NO inline value, NO lifecycle-state field; NO audit_chain_link_hash/producer/evidence_refs; absent from the ADR 0057 mint scope. Charter discipline: Ring 0 holds no runtime blobs (they live under ~/Library/Application Support/host-capability-substrate/, not the repo) and no secret-shaped values at rest (inv. 5) — the sha256 digest IS the artifact's content identity; the bytes + storage location are Ring 1 runtime state. IMMUTABLE: a corrected/re-run output is a NEW Artifact with a NEW digest (so no active/retired state — also the policy-cleaner choice). Fulfills the pre-reserved Evidence.subject_kind 'artifact' (evidence.ts) with NO evidenceSubjectKindSchema change and NO Evidence.schema_version bump. artifact_id is entityIdSchema (accepts a raw-shape id — recorded accept-and-trap routed to Ring 1, mirroring the tool-chain peers). | Accepts ADR 0070 after a five-reviewer round-1 pass returned ZERO blockers (security/ontology/eval yes; policy/architect yes-with-mechanical-tweaks; hcs-security-reviewer load-bearing for the no-bytes/no-secret-at-rest posture); v2 folded every mechanical tweak — the non-minted-peer claim qualified re: the omitted active/retired lifecycle (immutability replaces in-place state, also policy-cleaner); the run_id _id-not-_ref suffix rationale; the byte_size↔content_sha256 cross-consistency + the canonical-encoding rule added to the Ring-1 obligations; the artifact_kind inv-1 "descriptive FACT, not a verdict" .describe() clause; the .strict() reject-list extended with value/payload probes; the registry enum-mirror unknown-sentinel + §Procedure-widening note — so no confirming round 2 was required (mechanical-tweaks-at-acceptance, ADR 0058 precedent). Each load-bearing claim verified against source (no content/value field; content_sha256=sha256DigestSchema "not reversible secret material"; Evidence.subject_kind 'artifact' pre-reserved; run_id the established Run FK; collision check clean). Design-only acceptance: no Zod source, generated JSON Schema, tests, ontology/registry, live-policy/snapshot/system-config, or Ring 1 implementation change. Carries forward: the Artifact schema PR (artifact.ts + generated + tests + ontology/registry, incl. the artifact_id accept-and-trap + the .strict() content/content_ref/value/payload/artifact_state reject probes + byte_size range checks), and the Ring 1 obligations (run_id FK existence, digest verification + byte_size↔digest cross-consistency, the canonical-encoding rule, sandbox-non-promotion per inv. 8, artifact_id opacity, retention/GC). | ADR 0070 / Artifact Ring-0 entity | 2026-06-08 |
| D-069 | ADR 0071 (Lock Ring-0 entity) accepted: the second of the remaining lower-coupling M1 "storage primitive" entities (Artifact / Lock / ResourceBudget). Lock is "a coarser mutex (e.g., package-manager global)", modeled as a NON-MINTED coordination record — the coarse, lighter cousin of the MINTED Lease (ADR 0052): lock_id + a CLOSED lock_kind enum (package_manager_global\|host_mutation_global\|unknown) + a REQUIRED held_by_session_id FK to Session (ADR 0055; reuses Lease's FK NAME for the same semantic — held by a session) + a lock_status enum (held\|released) + a .strict() source_provenance (disjoint lock_declaration authority + observed_at). NO audit_chain_link_hash/producer/evidence_refs; absent from the ADR 0057 mint scope (Lease IS minted; Lock carries none of Lease's minting/authorization machinery — no scope/force_break_grant_id — so it cannot stand in for or bypass a Lease). TWO disambiguations: (1) Lock (coarse, non-minted) vs Lease (per-resource, minted); (2) the lifecycle field is lock_status, NOT lock_state, because lock_state is ALREADY a shipped field — GitWorktreeObservation.payload.lock_state (gitWorktreeLockStateSchema = unlocked/locked/held_by_other_session, a semantically DIFFERENT git-worktree posture) — the ADR 0067 same-name lesson. Fulfills the pre-reserved Evidence.subject_kind 'lock' (evidence.ts) with NO evidenceSubjectKindSchema change and NO Evidence.schema_version bump. lock_id is entityIdSchema (accepts a raw-shape id — recorded accept-and-trap routed to Ring 1). | Accepts ADR 0071 after a five-reviewer round-1 pass returned ALL yes, ZERO blockers (hcs-ontology-reviewer load-bearing for the Lock-vs-Lease + lock_status-collision calls); v2 folded the mechanical tweaks — tier/approval_required_for named in §Rejects; the inv-8 sandbox-non-promotion posture extended to lock_status (not just held_by_session_id); the §Context note that the shipped lock_state is a semantically-DIFFERENT enum (reinforcing the rename); and the schema-PR plan additions (the no-Lease-bypass .strict() reject probes scope + force_break_grant_id; the verbatim disjoint-from-evidenceAuthoritySchema provenance .describe() clause; the inline six-required-field enumeration; the registry released-value-overlap + lower_snake_case mirror notes) — so no confirming round 2 was required (mechanical-tweaks-at-acceptance, ADR 0058 precedent). Each load-bearing claim verified against source (lock_state is genuinely shipped; held_by_session_id is the real Lease FK; Evidence.subject_kind 'lock' pre-reserved; Lease minted vs Lock non-minted; collision check clean). Design-only acceptance: no Zod source, generated JSON Schema, tests, ontology/registry, live-policy/snapshot/system-config, or Ring 1 implementation change. Carries forward: the Lock schema PR (lock.ts + generated + tests + ontology/registry, incl. the lock_id accept-and-trap + the .strict() lock_state/scope/force_break_grant_id/mint/policy reject probes), and the Ring 1 obligations (mutual-exclusion enforcement, acquire/release, holder-only release, held_by_session_id FK existence, holder + lock_status sandbox non-promotion per inv. 8, lock_id opacity, supersession). | ADR 0071 / Lock Ring-0 entity | 2026-06-08 |
| D-070 | ADR 0072 (ResourceBudget Ring-0 entity) accepted: the third and LAST of the remaining lower-coupling M1 "storage primitive" entities (Artifact / Lock / ResourceBudget) — accepting + landing it closes the M1 22-entity Ring-0 set. ResourceBudget is "per-session CPU/memory/network/sandbox-concurrency allocation", modeled as a NON-MINTED, per-dimension allocation record (one per session×resource): resource_budget_id + a REQUIRED session_id FK to Session (ADR 0055) + a resource_kind enum (cpu\|memory\|network\|sandbox_concurrency\|unknown) + a limit_value (z.number().int().min(0)) + a limit_unit enum (cores\|bytes\|bytes_per_sec\|count\|percent\|unknown) + a budget_state enum (active\|retired) + a .strict() source_provenance (disjoint resource_budget_declaration authority + observed_at). NO audit_chain_link_hash/producer/evidence_refs; absent from the ADR 0057 mint scope. Central disambiguation: ResourceBudget (the durable per-session ALLOCATION / limit) vs the shipped ResourceBudgetObservation (a direct Evidence subtype observing runner PRESSURE — cpu_pressure_pct etc. — keyed to runner_host_id, which the ontology says "feeds the durable ResourceBudget entity rather than duplicating it"); distinct export names (resourceBudgetSchema vs resourceBudgetObservationSchema). Per-dimension (uniform + extensible via §Procedure enum-widening). Fulfills the pre-reserved Evidence.subject_kind 'resource_budget' (evidence.ts) with NO evidenceSubjectKindSchema change and NO Evidence.schema_version bump. Ring 0 does NOT cross-constrain resource_kind↔limit_unit (a cpu+bytes combination ACCEPTS at Ring 0 — a recorded accept-and-trap routed to Ring 1, mirroring the ResolvedTool basis↔context non-cross-constraint). resource_budget_id is entityIdSchema (accepts a raw-shape id — recorded accept-and-trap routed to Ring 1). | Accepts ADR 0072 after a five-reviewer round-1 pass returned 3 yes + 2 yes-with-mechanical-tweaks (policy/security/eval yes; ontology/architect yes-with-tweaks), ZERO blockers; v2 folded every mechanical tweak — the load-bearing one (architect): the lifecycle field is renamed budget_status → budget_state for peer consistency (every landed lifecycle field uses the _state suffix; unlike Lock's lock_status there is NO budget_state collision forcing _status; the operator's substantive active\|retired choice is unchanged); plus the three enum-schema export identifiers pinned (resourceBudgetResourceKindSchema / resourceBudgetLimitUnitSchema / resourceBudgetStateSchema), the field-name collision sweep recorded, the synthetic-UUID-only accept-and-trap fixture + sibling-declaration-authority reject probe added to the schema-PR test plan, the stale ontology 'future ResourceBudget' CommandShape forward-reference flagged for the schema-PR flip, and the no-secret-slot scan note — so no confirming round 2 was required (mechanical-tweaks-at-acceptance, ADR 0058 precedent). Each load-bearing claim verified against source (ResourceBudgetObservation is a genuine distinct shipped Evidence subtype; Evidence.subject_kind 'resource_budget' pre-reserved; field/export names collision-free incl. budget_state; session_id the established Session FK). Design-only acceptance: no Zod source, generated JSON Schema, tests, ontology/registry, live-policy/snapshot/system-config, or Ring 1 implementation change. Carries forward: the ResourceBudget schema PR (resource-budget.ts + generated + tests + ontology/registry, incl. the resourceBudget* enum exports, the resource_kind↔limit_unit accept-and-trap, the resource_budget_id accept-and-trap, the budget_state lifecycle, and the flip of the stale ontology future-ResourceBudget CommandShape forward-reference) — which CLOSES the M1 22-entity Ring-0 set — and the Ring 1 obligations (session_id FK existence, resource_kind↔limit_unit consistency, sandbox non-promotion per inv. 8, resource_budget_id opacity, supersession, budget enforcement against ResourceBudgetObservation pressure). | ADR 0072 / ResourceBudget Ring-0 entity | 2026-06-08 |
| D-071 | Tool-baseline re-baseline (supersedes D-054), triggered by the Opus 4.8→Fable 5 main-session model change per charter inv. 12's re-baseline-after-material-version-changes clause; CLI/app version bumps alone stay acceptable without re-baselining. Observed current baseline (2026-06-10): Claude Code CLI `2.1.172` (`claude --version`); main-session Claude model posture Fable 5 (`claude-fable-5`, 1M context), operator-selected 2026-06-10 via `/model` as the default for new sessions. SPLIT POSTURE recorded: the repo `.claude/settings.json` `model` key still pins the `opus` short-name (resolving to Opus 4.8, `claude-opus-4-8`), which applies on session restart and now diverges from the operator-selected main-session model; the six reviewer subagents pinning `model: opus` remain on Opus 4.8. Whether to move the settings model pin and/or the subagent alias to Fable 5 is a SEPARATE operator decision (`.claude/settings.json` is an hcs-security-reviewer surface per IMPLEMENT.md §Required subagent reviews) — this packet records the split, it does not resolve it. Host macOS Tahoe `26.5.1` (`sw_vers`). Codex posture unchanged from D-054 (not re-observed in this packet): CLI verify-via-`codex --version`, GPT-5.5/GPT-5.4-compatible HCS profiles, app builds tracked separately. Public CLI semver and app-build identifiers remain separate authority facts. The ≥ `2.1.120` / ≥ `0.125.0` floors and charter inv. 12's named-Opus-4.7 text are unchanged — the deferred charter-amendment ADR now trails the observed main-session model by two generations (4.7 named → 4.8 → Fable 5). AGENTS.md, CLAUDE.md, and README.md §Tool baseline updated to match (the README current-baseline citation repointed per the D-054-landing precedent, caught by the hcs-architect lens). | Charter inv. 12 calls for re-baseline after material version changes; the model change is the material trigger, cited directly from inv. 12 (not via D-029, which is two supersessions back). Versions recorded as observed installed-runtime evidence per charter inv. 14 authority order: `claude --version` 2.1.172, `sw_vers` 26.5.1, the live session harness model ID (`claude-fable-5`, 1M context), and the repo `.claude/settings.json` read at HEAD (`"model": "opus"`, line 2). Modeling/ledger only — no `.claude/settings.json`, charter, tooling-surface-matrix, `.claude/agents/**`, kernel/adapter, live-config, or provider change. Residual same-fact drift flagged for follow-up via its own lanes (carried forward from D-054, now one generation wider): charter inv. 12 named-model text (amendment ADR); tooling-surface-matrix §Tool baseline line 20 + anti-pattern line 247 (still name Opus 4.7); the settings/subagent model-pin choice (operator decision, security-lens surface). | supersedes D-054 (charter inv. 12/14); 2026-06-10 Fable 5 session-default switch | 2026-06-11 |
| D-072 | Settings model pin resolved (closes D-071's recorded split posture): repo `.claude/settings.json` `model` key flipped `opus` → `claude-fable-5[1m]`, so repo-session restarts match the operator-selected Fable 5 default instead of reverting to Opus 4.8. The six reviewer subagents keep `model: opus` (Opus 4.8) — review calibration deliberately unchanged (operator pick 2026-06-11: "main→Fable, reviewers stay Opus"). The pinned value is the exact string the Claude Code CLI itself wrote to the user-level settings `model` key when the operator selected Fable 5 via `/model` (installed-runtime evidence per charter inv. 14, read 2026-06-11) — not a guessed alias. | D-071 recorded the split but deferred the pin choice as a separate operator decision; the operator resolved it. `.claude/settings.json` is an hcs-security-reviewer surface (IMPLEMENT.md §Required subagent reviews) — security lens dispatched on this packet; only the `model` key changes (no permissions, hooks, or deny-list change). Same-fact sweep applied (the D-071 lens's re-baseline-sweep lesson): AGENTS.md/CLAUDE.md/README split-posture prose updated to the resolved pin. Floors, charter inv. 12 text, and `.claude/agents/**` definitions unchanged. | resolves the D-071 open fork; operator pick 2026-06-11 | 2026-06-11 |
| D-073 | ADR 0073 (charter amendment: generated-snapshot location + enforcement-tooling authoring) accepted: two charter v1.5.0 edits to land in their own charter-only PR per §Change policy. (1) The §Package boundary enforcement YAML-location bullet gains `policies/generated-snapshot/` as the third permitted location, mechanically qualified — the binding's snapshot file is the ONLY YAML permitted under the path (`snapshot-binding-check` fails on any additional YAML and any digest mismatch, every verify run), never hand-edited, never live policy (canonical stays in `system-config`; invariant 10 untouched) — conforming charter text to the operator-approved D-048/D-051 lane and to `policy-lint.sh`'s existing enforcement (fails `policies/` ROOT YAML, lints the snapshot dir). (2) §Authoring rules gains the enforcement-tooling mandate: `scripts/ci/**`, `.github/workflows/**`, `justfile`, verify wiring → `hcs-architect` objections before human review, escalating to `hcs-security-reviewer` for any invariant-enforcing gate (`no-live-secrets`; `forbidden-string-scan` incl. its audit-write-exposure/universal-shell/hook-thinness stanzas; `no-runtime-state-in-repo`; `snapshot-binding-check` — the integrity gate backing edit 1; sandbox/permission-adjacent gates; hook installation); restated as IMPLEMENT.md change class `J: enforcement tooling` + a §Required-subagent-reviews row with the surface/trigger lists TEXTUALLY IDENTICAL across both docs. The mandate COMPOSES with — never displaces — the operation-classifying-file `hcs-policy-reviewer` mandate (`policy-lint.sh`, `snapshot-binding-check`) and the `hcs-ontology-reviewer` mandate. Invariant text untouched; the inv. 12 named-model amendment stays separately deferred. | Accepts ADR 0073 after the §Change-policy-mandated lenses (policy + security) plus hcs-architect (ADR-edit mandate): round 1 returned ONE blocker (policy B1 — the class-J wording could be read as displacing the operation-classifying-file policy-lens mandate) + 5 non-blocking; v2 folded EVERY finding (compose-not-displace clause; explicit invariant-gate trigger set; single-file exact-allowlist qualifier; charter↔IMPLEMENT textual-identity mandate; suggested regression coverage; stale in-file `snapshot_binding` provenance note — authoritative binding is external per D-051); because round 1 carried a blocker, a CONFIRMING round 2 was dispatched — all three lenses accept-ready, confirms_round1_fixes=true, zero blocking, each re-verifying gate-script line behavior against source. Proposal merged as PR #62 (`e378a3e`); operator directed proceed 2026-06-11. Design-only acceptance: no charter/settings/policy/schema byte change. Carries forward: the charter-only v1.5.0 PR (both edits + the IMPLEMENT.md class-J restatement sync + carry-forward wording notes: resolve "verify gate wiring" to a concrete path; preserve I/J class ordering; consider a prose-equivalence guard for the twin trigger lists) and the four eval-lane regression traps (unbound-second-YAML, digest-mismatch, hook-non-consumption, gate-class lens-coverage meta-check). | ADR 0073 / charter amendment (snapshot location + enforcement tooling) | 2026-06-11 |

## Reversed

| ID | Old decision | New decision | Why | Date |
|----|--------------|--------------|-----|------|
| D-017 | Repo name: `host-capability-substrate` (no prefix); path: `~/Organizations/jefahnierocks/host-capability-substrate/`; GitHub: `verlyn13/host-capability-substrate`. | Repo name and local path unchanged; GitHub slug now `jefahnierocks/host-capability-substrate`. Repo name, local filesystem path, workspace boundary, ring boundaries, and the `.subsidiary.yaml`-precursor rationale are preserved. | GitHub UI ownership transfer 2026-05-08 from personal `verlyn13` account to a dedicated `jefahnierocks` GitHub organization aligns the GitHub-side slug with the long-standing `README.md` §Ownership declaration ("Owned by **jefahnierocks** as a host-scoped HCS project"). Sibling `system-config` migrating in parallel under that repo's own scope. See D-034 / ADR 0046. | 2026-05-08 |

## How to use this file

- Before proposing a design change, scan **Accepted** to see if the question is settled.
- If settled but you believe it should change: open an amendment ADR, not a PR that contradicts.
- If unsettled: add a row to **Pending** with an owner and due-before date.
- When a decision is accepted, add the ADR link and move from **Pending** to **Accepted**.
- When a decision is reversed, move the old row to **Reversed** and add a new accepted row.

Agents should treat this file as authoritative for "is this settled?" even when AGENTS.md or CLAUDE.md is silent.
