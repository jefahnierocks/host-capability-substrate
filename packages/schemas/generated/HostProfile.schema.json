{
  "$id": "https://jefahnierocks.local/host-capability-substrate/schemas/HostProfile.schema.json",
  "title": "HostProfile",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "0.1.0",
      "description": "HostProfile schema version (ADR 0066; non-minted Ring 0 entity; D-064). New entities start at 0.1.0."
    },
    "host_profile_id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]*$",
      "description": "Stable HostProfile id. MUST be opaque/derived, NOT the raw machine identifier — Ring 0 cannot reject a raw-UUID shape (`entityIdSchema` accepts it), so non-raw-id is a Ring 1 producer obligation (recorded accept-and-trap)."
    },
    "host_state": {
      "type": "string",
      "enum": [
        "active",
        "retired"
      ],
      "description": "HostProfile lifecycle. A materially-changed host produces a NEW `active` HostProfile and retires the prior (a Ring 1 supersession obligation); `retired` is a valid historical record, NOT a policy-denied state."
    },
    "os_name": {
      "type": "string",
      "enum": [
        "macos",
        "linux",
        "windows",
        "unknown"
      ],
      "description": "Host OS family (ADR 0066). Widening via the registered §Procedure rule."
    },
    "os_version": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[A-Za-z0-9._+-]+$",
      "description": "Observed host OS version string (e.g. `26.5`). No whitespace, no `/`, no `:` (so no `://` / `op://`), no path/secret shape; bounded length. A version FACT, never an identity field. Ring 0 cannot bar a UUID/hex shape here without a policy denylist (inv. 1) — `host_identity.digest` is the `sha256:`-locked identity field."
    },
    "arch": {
      "type": "string",
      "enum": [
        "arm64",
        "x86_64",
        "unknown"
      ],
      "description": "Host CPU architecture (ADR 0066). Widening via the registered §Procedure rule."
    },
    "host_identity": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "platform_uuid_sha256",
            "install_id_sha256",
            "unknown"
          ],
          "description": "What stable identifier `host_identity.digest` was computed over: `platform_uuid_sha256` = SHA-256 of the platform UUID; `install_id_sha256` = SHA-256 of a per-install HCS id (the always-derivable fallback); `unknown` = unclassified, but still a real digest."
        },
        "digest": {
          "type": "string",
          "pattern": "^sha256:[a-f0-9]{64}$",
          "description": "A NON-REVERSIBLE sha256 digest of the stable identifier — NEVER the raw serial / UUID / machine-id. The `sha256:` shape structurally bars a raw identifier from landing here (charter sensitive-data discipline)."
        }
      },
      "required": [
        "kind",
        "digest"
      ],
      "additionalProperties": false,
      "description": "The host fingerprint: a `kind` + a non-reversible `sha256:` digest, always present. NEVER stores a raw machine identifier."
    },
    "source_provenance": {
      "type": "object",
      "properties": {
        "authority": {
          "type": "string",
          "const": "host_profile_declaration",
          "description": "Declaration-site marker (a durable record, NOT an observation); deliberately DISJOINT from evidenceAuthoritySchema and conferring no authority by itself. Installed-runtime sourcing + non-sandbox authority (charter inv. 8) are Ring 1 producer obligations, not encoded here."
        },
        "observed_at": {
          "type": "string",
          "format": "date-time",
          "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$",
          "description": "RFC 3339 timestamp with an explicit UTC offset."
        }
      },
      "required": [
        "authority",
        "observed_at"
      ],
      "additionalProperties": false,
      "description": "Binds a HostProfile declaration to its declaration site. Carries no secret/sensitive material."
    }
  },
  "required": [
    "schema_version",
    "host_profile_id",
    "host_state",
    "os_name",
    "os_version",
    "arch",
    "host_identity",
    "source_provenance"
  ],
  "additionalProperties": false,
  "description": "Ring 0 HostProfile entity from ADR 0066 (D-064). A NON-MINTED canonical host identity + stable facts record (structural peer of Capability/SecretReference), closing `RunnerHostObservation.host_id` (ADR 0032) with NO RunnerHostObservation shape change. The host fingerprint lives only as a non-reversible `sha256:` digest in `host_identity.digest` (a raw serial/UUID cannot land there); `host_profile_id`-opaque, installed-runtime + non-sandbox observation (inv. 8), digest computation, FK existence, and supersession are Ring 1 obligations. No `audit_chain_link_hash`, no producer-mint field, no `evidence_refs`; absent from the ADR 0057 mint scope. Host facts are read-only policy INPUTS to Ring 1, never policy content (inv. 1)."
}
