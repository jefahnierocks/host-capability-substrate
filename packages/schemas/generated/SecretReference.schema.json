{
  "$id": "https://jefahnierocks.local/host-capability-substrate/schemas/SecretReference.schema.json",
  "title": "SecretReference",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "0.1.0",
      "description": "SecretReference schema version (ADR 0065; non-minted Ring 0 entity; D-063). New entities start at 0.1.0."
    },
    "secret_reference_id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]*$",
      "description": "Stable local entity identifier. It is not a secret value."
    },
    "reference_kind": {
      "type": "string",
      "enum": [
        "op_uri",
        "hcs_uri",
        "keychain_item",
        "env_var_name",
        "broker_handle"
      ],
      "description": "The form of the secret reference (ADR 0065). `op_uri` (`op://vault/item[/field]`) and `keychain_item` (`keychain://service/account`) are STRUCTURED — Ring 0 structurally rejects a mis-shaped or raw-value locator. `env_var_name` (an env var NAME), `hcs_uri` (`hcs://<opaque>`), and `broker_handle` (`hcs-broker:<opaque>`) are PERMISSIVE — a token-shaped value can satisfy the grammar, so Ring 0 ACCEPTS it (a recorded accept-and-trap gap; the deep reference-vs-secret check is a Ring 1 broker obligation, charter inv. 5). The locator NEVER holds a resolved value."
    },
    "reference_locator": {
      "type": "string",
      "minLength": 1,
      "description": "The opaque secret REFERENCE, validated per reference_kind by the envelope superRefine. NEVER a resolved value (charter inv. 5): `op://` is a reference URI, `env_var_name` is a NAME, etc. For the permissive kinds a token-shaped value can pass (recorded accept-and-trap); the deep reference-vs-secret check and resolution are Ring 1 broker obligations."
    },
    "credential_source_ref": {
      "description": "Optional FK to a durable CredentialSource (ADR 0018) backing this reference. The `_ref` suffix follows the optional/forward cross-record-FK precedent (`grantor_principal_ref` / `policy_rule_ref` / `operation_shape_ref`); intentionally distinct from the kernel-set `BoundaryObservation.credential_source_id`. FK existence and the `reference_locator`-vs-bound-source resolution precedence are Ring 1 obligations.",
      "anyOf": [
        {
          "type": "string",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]*$",
          "description": "Stable local entity identifier. It is not a secret value."
        },
        {
          "type": "null"
        }
      ]
    },
    "source_provenance": {
      "type": "object",
      "properties": {
        "authority": {
          "type": "string",
          "const": "secret_reference_declaration",
          "description": "Declaration-site marker; deliberately DISJOINT from evidenceAuthoritySchema and conferring no authority by itself."
        },
        "observed_at": {
          "type": "string",
          "format": "date-time",
          "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$",
          "description": "RFC 3339 timestamp with an explicit UTC offset."
        }
      },
      "required": [
        "authority",
        "observed_at"
      ],
      "additionalProperties": false,
      "description": "Binds a SecretReference declaration to its declaration site. Lighter than the registry-bound PolicyRule/Capability provenance (no source-blob digest): a SecretReference is constructed per-use, not read from a hash-bound blob. Carries no secret material."
    }
  },
  "required": [
    "schema_version",
    "secret_reference_id",
    "reference_kind",
    "reference_locator",
    "source_provenance"
  ],
  "additionalProperties": false,
  "description": "Ring 0 SecretReference entity from ADR 0065 (D-063). A NON-MINTED typed secret reference (structural peer of PolicyRule/Capability/CommandShape): a `reference_kind` plus an opaque `reference_locator` that NEVER holds a value (charter inv. 5), an optional `credential_source_ref` FK to the durable CredentialSource (ADR 0018), and a `.strict()` `source_provenance`. Closes `CommandShape.secret_reference_ref` (ADR 0063). The secret-reference slot is DISJOINT from ProviderObjectReference / PublicClientId / PolicySelectorValue / raw secret material (charter inv. 16 / line 98). Ring 0 validates only the structural reference shape per kind: STRUCTURED kinds (op_uri, keychain_item) reject mis-shaped/raw-value locators; PERMISSIVE kinds (env_var_name, hcs_uri, broker_handle) accept token-shaped locators (recorded accept-and-trap, backstopped by forbidden-string-scan + the Ring 1 deep check). It does NOT embed a policy-tier denylist (charter inv. 1). No audit_chain_link_hash, no producer-mint field, no evidence_refs; absent from the ADR 0057 mint scope. Value resolution, FK existence, the deep reference-vs-secret check, and locator-vs-bound-CredentialSource precedence are Ring 1 broker obligations."
}
